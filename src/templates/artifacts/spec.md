# {capability_name}

<!-- Merge target: the H1 above must name an existing capability (see existing_specs in your instructions). To create a NET-NEW capability instead, add an HTML comment line containing exactly new-capability immediately under the H1 to confirm it. -->

## {ADDED|MODIFIED|REMOVED}: Requirement: {requirement_name}

{requirement_text using RFC 2119 keywords}

### Scenario: {scenario_name}
- GIVEN {precondition}
- WHEN {action}
- THEN {expected_outcome}
