<!-- Generated by Metta — do not edit directly -->
<!-- Sources: spec/specs/adaptive-workflow-tier-selection/spec.md, spec/specs/artifact-store/spec.md...and 23 more -->
<!-- Run `metta docs generate` to regenerate -->
# API Reference

## Adaptive Workflow Tier Selection

### ComplexityScoreComputation

Scenarios:
- score_computed_from_impact_section
- zero_file_intent_is_no_signal
- single_file_intent_still_scores
- zero_files_at_summary_time_remains_real_signal
- score_absent_before_intent_written
- score_not_recomputed_on_intent_edit
- score_uses_actual_files_from_summary_for_post_impl_recompute

### ComplexityScoreStorage

Scenarios:
- schema_accepts_full_complexity_block
- schema_accepts_legacy_file_without_fields
- actual_score_does_not_overwrite_original

### TierThresholds

Scenarios:
- single_file_maps_to_trivial
- two_files_maps_to_quick
- four_files_maps_to_standard
- eight_files_maps_to_full

### StatusCommandSurface

Scenarios:
- human_output_shows_complexity_line
- json_output_includes_complexity_object
- json_output_includes_both_scores_when_present
- absent_score_renders_without_error

### InstructionsAdvisoryBanner

Scenarios:
- banner_agreement_state
- banner_downscale_state
- banner_upscale_state
- banner_suppressed_when_score_absent

### AutoDownscalePromptAtIntent

Scenarios:
- non_tty_downscale_fails_closed
- json_mode_downscale_fails_closed
- auto_accept_opt_in_still_collapses_non_interactively
- interactive_unlocked_shows_yes_default
- locked_change_defaults_to_no

### AutoUpscalePromptAtIntent

Scenarios:
- upscale_prompt_appears_on_undersized_quick
- upscale_yes_mutates_workflow_and_inserts_artifacts
- upscale_prompt_suppressed_when_workflow_exceeds_recommendation
- upscale_auto_accept_skips_prompt

### PostImplementationUpscalePromptAccept

Scenarios:
- post_impl_prompt_appears_when_recomputed_tier_exceeds_workflow
- post_impl_yes_spawns_agents_and_updates_metadata
- post_impl_yes_uses_promoted_fan_out
- post_impl_no_research_design_tasks_authored

### PostImplementationUpscalePromptDecline

Scenarios:
- decline_persists_actual_score_and_prints_warning
- decline_does_not_create_stories_or_spec
- decline_exits_zero_and_verification_proceeds
- non_tty_defaults_to_decline

### AutoAcceptRecommendationFlag

Scenarios:
- auto_flag_persists_field
- auto_flag_skips_all_three_prompts
- accept_recommended_alias_behaves_identically
- auto_with_workflow_honours_initial_choice

### OverrideRemainsAuthoritative

Scenarios:
- workflow_flag_alone_preserves_initial_choice_with_prompt
- workflow_without_auto_shows_intent_prompts_normally
- workflow_with_auto_combination_is_predictable

### IntraQuickDownsizeRule

Scenarios:
- trivial_quick_run_uses_reduced_fan_out
- trivial_fan_out_excludes_correctness_security_goalcheck
- non_trivial_quick_run_keeps_standard_fan_out
- tests_and_tsc_run_regardless_of_tier

### ScoringRubricSpec

Scenarios:
- rubric_document_exists_with_required_sections
- rubric_names_deferred_signals_as_extension_points
- claude_md_active_specs_table_updated

### EscalationSchema

Scenarios:
- schema_accepts_populated_escalation
- schema_accepts_legacy_file_without_escalation

### EscalationRecording

Scenarios:
- escalation_recorded_on_locked_keep
- escalation_recorded_on_interactive_decline
- no_escalation_on_downscale_accept

### StatusEscalationSurface

Scenarios:
- human_output_shows_escalation
- json_output_includes_escalation
- status_renders_without_escalation

### SkillRoutingPreStep

Scenarios:
- small_description_routes_to_quick
- explicit_workflow_flag_deferred

### EscalationJustificationGuidance

Scenarios:
- claude_md_states_default_routing
- skill_documents_justification_requirement

### ProgressCeremonyRatioMetric

Scenarios:
- human_output_reports_ceremony_ratio
- json_output_includes_ceremony_ratio

### ProgressArtifactsPerSmallChangeMetric

Scenarios:
- reports_mean_artifact_count
- no_data_renders_without_misleading_zero

### DownscaleDecisionSchema

Scenarios:
- schema_accepts_populated_downscale_decision
- schema_accepts_legacy_file_without_downscale_decision
- downscale_decision_coexists_with_escalation_semantics

### DownscaleDecisionRecording

Scenarios:
- auto_accept_collapse_writes_decision_record
- interactive_yes_collapse_writes_decision_record
- decline_path_unchanged_writes_escalation_not_decision

## Artifact Store

### Change Creation

Scenarios:
- Successful change creation
- Slug strips stop words and special characters
- Duplicate change rejected
- Base versions recorded

### Change Listing

Scenarios:
- Lists active changes
- Empty list when no changes

### Artifact Status Tracking

Scenarios:
- Mark artifact complete
- Mark next artifact ready advances current_artifact

### Artifact File I/O

Scenarios:
- Write and read artifact
- Existence check

### Change Archival

Scenarios:
- Archive moves directory

### Change Abandonment

Scenarios:
- Abandon appends suffix

## ci-test-infrastructure

### Deterministic CLI Test Execution Path

Scenarios:
- Cold runner passes with only declared dependencies
- Test runtime is declared, not implicit

### CI Ordering Consistent With Execution Path

Scenarios:
- Required artifacts exist before tests run in CI
- CI and local runs exercise the same path

### Fresh-Clone Test Runs Without Manual Pre-Steps

Scenarios:
- Fresh clone passes with scripts alone
- Stale build cannot mask source changes

### Actionable Failures and Consistent Runtime Policy

Scenarios:
- Timeout budget covers only CLI work
- Timeout failure is diagnosable
- No contradictory guidance survives

### Fail-Fast Setup Helper With Full Diagnostics

Scenarios:
- Non-zero exit throws with full diagnostics
- Signal kill throws with signal named
- Successful invocation does not throw

### Install Fixture Verifies Resulting State

Scenarios:
- Zero exit but missing config throws
- Successful install passes the post-check

### runCli Return-Value Contract Preserved

Scenarios:
- Deliberate failure assertion still receives a return value

### Setup-Phase Call Sites Fail Fast

Scenarios:
- No discarded setup result in the minimum files
- Setup failure surfaces at the failing command
- Happy-path suite behavior is unchanged

### CI Vitest Concurrency Cap

Scenarios:
- Heavy fixture files do not run concurrently in CI
- Gates job passes on a constrained runner
- Residual CI failure self-diagnoses

### Local Test Runs Retain Current Parallelism

Scenarios:
- Local run is not constrained by the CI cap
- Full local suite passes after the change

## claude-statusline

### Statusline script stdin contract

Scenarios:
- Valid stdin with both fields present
- Empty stdin treated as empty payload
- Malformed JSON on stdin treated as empty payload
- Stdin contains valid JSON but fields are wrong type

### Context window resolution

Scenarios:
- Payload declares the window size
- 1M-family model id resolves to 1M without payload window
- Haiku model id resolves to 200k
- Model id contains [1m] substring
- Unrecognized model id falls back to 200k
- Model field absent
- Model field contains [1m] as part of a longer string

### Context utilization calculation

Scenarios:
- Harness-computed percentage preferred
- Percentage above 100 renders the overflow marker
- Transcript present with assistant usage entry
- Transcript present but no assistant usage entry found
- Transcript file does not exist
- Most recent assistant usage entry is used

### Metta artifact resolution

Scenarios:
- metta status returns active artifact
- aggregated changes array yields the active change
- worktree-hosted change is detected when root status shows none
- metta status returns no active change and no worktrees exist
- metta binary not found on PATH
- metta status --json times out

### Output format

Scenarios:
- Full output with artifact and context percentage
- Output with idle artifact and no context percentage
- Unrecoverable error fallback
- Exit code is always 0

### Deterministic change-slug coloring

Scenarios:
- Active change slug produces ANSI-wrapped label
- Same slug produces same color on repeated runs
- Idle artifact is not colored
- Unknown fallback is not colored

### Install auto-registration

Scenarios:
- Fresh install writes script and updates settings
- Re-run install is a no-op
- Existing unrelated keys in settings.json are preserved
- User-authored statusLine pointing at a different path is not overwritten

### Install failure isolation

Scenarios:
- Script copy fails due to permissions error
- settings.json is unparseable JSON
- Failure does not suppress guard hook installation
- Warning message identifies the failed component

### Template build copy

Scenarios:
- Build copies statusline.mjs to dist
- Copied file is executable
- Build succeeds when statusline source directory is empty

## ConfigLoader — Specification

## config-writer

### Shared setProjectField config-writer helper

Scenarios:
- idempotent re-write produces no diff
- comment above mutated key is preserved

### metta install writes stacks via the shared helper

Scenarios:
- re-run idempotency — single stacks key after two installs
- three pre-existing duplicate stacks lines become one after install

### config-loader hard-fails on YAML parse errors

Scenarios:
- corrupt config blocks metta status with actionable message
- metta doctor is not blocked by the same corrupt config

### metta doctor --fix repairs duplicate-keys and schema-invalid config

Scenarios:
- three duplicate stacks lines collapsed to one with auto-commit
- schema-invalid key is dropped with a reported line

### ProjectConfigSchema carries a verification section

Scenarios:
- valid verification block is accepted by Zod
- invalid strategy enum value is rejected with a field-level error

### /metta-init Round 4 captures verification strategy

Scenarios:
- selecting tmux_tui with instructions writes verification block
- the two SKILL.md copies are byte-identical after the update

### metta instructions verification injects strategy into agent context

Scenarios:
- both fields populated when config has a full verification block
- both fields are null when verification block is absent

### verifier agents error on missing verification.strategy in non-default projects

Scenarios:
- legacy project config triggers a hard error with copy-pasteable remediation
- first-run verification with no changes defaults to tests_only with informational note

## constitution-check

### Contract Emission Without API Credentials

Scenarios:
- Contract emitted with no credential present
- Machine-readable contract via --json
- Nonexistent change fails with a clear error, not an auth error

### No Direct AI Provider Invocation

Scenarios:
- No provider client is constructed during contract emission
- No provider client is constructed during verdict recording
- Full check runs end-to-end with no credential in the environment

### Verdict Schema Validation

Scenarios:
- Malformed verdict is rejected without persisting anything
- Unparseable verdict input is rejected
- Well-formed empty verdict passes validation

### Verdict Recording and Blocking-Violation Exit Semantics

Scenarios:
- Verdict with no blocking violations exits 0
- Critical violation is always blocking
- Unjustified major violation is blocking
- Justified major violation is not blocking

### Violations Report Format and Location Preserved

Scenarios:
- Clean verdict produces the no-violations report
- Violations report renders each violation with its fields

### Skill-Driven Two-Step Check Flow

Scenarios:
- Skill completes a full check with no manual workaround
- Skill surfaces a blocking result rather than masking it
- Skill template and deployed copy stay byte-identical

### Idempotent Re-Check Replaces the Prior Verdict

Scenarios:
- Re-running the check overwrites the prior report
- Re-running with an unchanged clean verdict still overwrites

## Context Engine — Specification

## Finalize and Ship

### Spec Delta Merge

Scenarios:
- New capability created from a confirmed ADDED delta
- Re-applying an ADDED delta does not duplicate an existing requirement
- ADDED idempotency holds without a base_versions entry
- Dry-run reports requirement not found for a MODIFIED delta against an absent requirement
- Dry-run classifies an ADDED duplicate as a noop, matching apply

### Finalizer Orchestration

Scenarios:
- Incomplete artifact blocks finalize before gates or merge run
- Gate failure leaves the target capability spec untouched
- Retry after a fixed gate applies the merge exactly once

### Merge Safety Pipeline

Scenarios:
- Successful merge
- Dry-run skips git writes
- Conflict detected
- Base drift does not block merge

### Finalize Lock Contention Error Message

Scenarios:
- Lock held by a live process recommends a retry, not manual deletion
- Re-running finalize reclaims a dead-pid lock without manual cleanup

### Finalize Lock Staleness Fallback Via Mtime

Scenarios:
- Ambiguous pid liveness with an expired mtime is reclaimed
- Fresh lock with a confirmed live owner is respected
- Dead-pid reclaim path is preserved alongside the mtime fallback

### Stale Finalize Lock Surfaced In Status

Scenarios:
- Status reports a detected stale lock
- Status is unchanged when no stale lock is present

### Stale Finalize Lock Surfaced In Next Routing

Scenarios:
- Next surfaces the stale lock instead of routing into a failing finalize
- Next routing is unchanged when no stale lock is present

### Explicit Capability Target Selection In Spec Authoring

Scenarios:
- Instructions surface existing capabilities before authoring
- Generated scaffold carries an explicit target field, not an implicit slug default

### Merge Target Confirmation At Completion

Scenarios:
- Unconfirmed self-slug target is refused
- Explicit new-capability marker allows completion
- Non-ADDED operations against a nonexistent capability still hard-fail

### Trivial Workflow Verification Artifact Contract Agreement

Scenarios:
- Declared contract and instructed behavior agree
- Following instructions lets completion succeed
- Finalize completeness gate passes without manual patching

### UAT Script Generation At Finalize

Scenarios:
- Successful finalize writes UAT.md into the change directory before archive
- Archive sweep carries UAT.md into the archive directory
- Generation is deterministic with no AI call

### No Stray UAT On Failed Finalize Paths

Scenarios:
- Incomplete artifacts abort before UAT generation
- Merge conflict aborts before UAT generation
- Gate failure aborts before UAT generation
- Dry-run finalize writes no UAT.md

### UAT Source Material Assembly

Scenarios:
- Stories and spec scenarios feed the generated steps
- Machine-verified annotation applied when derivable
- Annotation absent when not derivable, without error

### UAT Document Format

Scenarios:
- Header is self-describing for later audit
- Steps are numbered, story-grouped, and checkable

### UAT Tier Fallback Chain

Scenarios:
- Parsed stories produce the full story-grouped script
- Sentinel stories fall back to spec scenarios
- No stories and no spec scenarios fall back to intent plus summary
- Generation is never skipped by tier when enabled

### UAT Configuration Toggle

Scenarios:
- Disabled toggle skips generation cleanly
- Omitted uat key defaults to enabled
- Invalid uat config is rejected strictly

### UAT Path In Finalize Output

Scenarios:
- JSON success payload carries the UAT path
- Human output reports the UAT path
- Disabled generation yields null path and no human line
- Error JSON shapes are unchanged

### UAT Template Externality

Scenarios:
- Rendering goes through the external template
- Template ships to dist via the existing copy step

### UAT Generation Failure Degradation

Scenarios:
- Assembly error degrades to a warning, finalize still succeeds
- Degraded run reports the failure in output

### Token Tracking Delta Scope Note

Scenarios:
- Delta merges into finalize-ship despite spanning adjacent surfaces

### Token Usage Record Schema

Scenarios:
- Hook-sourced record passes strict validation
- Invalid source values are rejected strictly
- Legacy record without source remains valid and reads as prose-sourced

### Tokens Record CLI Command

Scenarios:
- Source flag persists provenance
- Legacy invocation without source stays backward compatible
- Invalid source value writes nothing

### Tokens Guard Hook Allowlist Entry

Scenarios:
- Skill-issued tokens record passes the guard
- Hook copies stay byte-identical and syntactically valid
- Other guard classifications are unchanged

### Lifecycle Skill Token Recording Instruction

Scenarios:
- No skill mandates per-subagent recording
- Template and deployed skill pairs are byte-identical
- Demotion changes nothing about routing or the guard allowlist

### Tokens Report Generation At Finalize

Scenarios:
- Successful finalize writes TOKENS.md before archive
- Assembly is deterministic, template-driven, and AI-free
- No token records still yields a report

### Tokens Report Content

Scenarios:
- Provenance is distinguishable per record
- A gap reads as a hook coverage miss
- Report structure is otherwise unchanged
- Complete coverage reports no gaps

### Tokens Report Configuration Toggle

Scenarios:
- Disabled toggle skips the report cleanly
- Omitted tokens key defaults to enabled
- Invalid tokens config is rejected strictly

### No Stray Tokens Report On Failed Finalize Paths

Scenarios:
- Gate failure leaves no TOKENS.md behind
- Dry-run finalize writes no TOKENS.md
- Incomplete artifacts and merge conflicts abort before report generation

### Tokens Report Failure Degradation

Scenarios:
- Assembly error degrades to a warning, finalize still succeeds
- Degraded run reports the failure in both output modes

### Tokens Path In Finalize Output

Scenarios:
- JSON success payload carries the tokens path additively
- Human output reports the tokens path
- Disabled generation yields null path, no human line, and untouched error shapes

### Progress Average Tokens Per Change By Tier

Scenarios:
- Tier-grouped averages render from recorded data
- No-data tier is distinct from zero and null in JSON
- Pre-feature archives aggregate gracefully

### Token Recording SubagentStop Hook

Scenarios:
- Subagent stop is recorded automatically with the exact transcript-summed count
- Missing or usage-free transcript records nothing rather than fabricating a count
- Registration targets the SubagentStop event and leaves existing hooks untouched
- Hook copies stay byte-identical and syntactically valid

### Worktree-Aware Change Resolution For Token Recording

Scenarios:
- Recording from inside a change worktree attributes to that change
- Repo-root recording keeps the existing single-active-change behavior
- Unresolvable change fails safely without misattribution

### Non-Blocking Token Recording Hook Failure

Scenarios:
- CLI unavailable leaves the subagent run unaffected
- Recording command failure is swallowed
- Missing transcript is swallowed without blocking the subagent
- Guard hook behavior is unchanged with the recording hook installed

### Token Record Provenance Deduplication In Tokens Report

Scenarios:
- Duplicate hook and prose records count once, preferring the hook value
- Prose-only records are retained
- Dedupe never mutates persisted state

### Dry-Run And Apply Merge Result Parity

Scenarios:
- Dry-run and apply return identical results for the same fixture set
- Preflight dry-run catches an apply-time-only conflict class

### All-Or-Nothing Spec Merge Apply

Scenarios:
- Conflicting delta N leaves the spec store byte-identical
- Zero-conflict multi-delta merge commits every staged result

### Staged Composition Of Same-Capability Deltas

Scenarios:
- A later delta composes with an earlier delta against the same capability
- Dry-run classifies composed same-capability deltas identically

## fix-issues-command

### fix-issue-cli-command

Scenarios:
- no-args prints usage
- single-slug prints details and delegate hint
- single-slug not found exits non-zero
- --all lists issues sorted severity-first
- --all --severity filters to matching tier
- --remove-issue archives and commits

### issues-store-archival

Scenarios:
- archive moves content to resolved directory
- archive on missing slug throws
- archive is idempotent when resolved copy already exists
- remove deletes the open issue file

### skill-template

Scenarios:
- template file exists with correct frontmatter name
- deployed skill is byte-identical to template
- skill body references all four CLI invocation modes

### cli-registration

Scenarios:
- command appears in --help output
- registerFixIssueCommand is called in index.ts

## gate-runner

### Gate execution MUST honor all three `on_failure` policies uniformly

Scenarios:
- retry_once retries on first fail
- retry_once does not retry on initial pass
- continue_with_warning downgrades fail to warn
- stop on fail causes remaining gates to be skipped
- stop does not affect gates earlier in the batch

### Gate `warn` status MUST be treated as pass across CLI commands

Scenarios:
- verify exits 0 when a gate returns warn
- verify exits non-zero when any gate returns fail
- finalize accepts warn results

### Workflow YAMLs MUST NOT reference unimplemented gates

Scenarios:
- standard.yaml does not reference unimplemented gates
- full.yaml does not reference unimplemented gates
- quick.yaml does not reference unimplemented gates

### GateRegistry tests MUST cover each policy branch

Scenarios:
- test suite asserts each on_failure branch

## install-init

### init-command-drives-discovery

Scenarios:
- init after install in a brownfield project
- init on a greenfield project
- init before install is blocked
- init does not mutate the repository

### init-skill-invokes-init-command

Scenarios:
- skill template references init command
- skill propagates to installed projects

### install-command-scaffolds-only

Scenarios:
- fresh install in a git repo
- install on a project that already has .metta
- install without a git repository
- human-readable install output points at init

### install-stamps-installed-version

Scenarios:
- fresh install stamps the running version
- re-running install overwrites a stale stamp

### init-stamps-installed-version

Scenarios:
- init stamps the running version
- init refreshes a stale stamp

### project-config-schema-accepts-installed-version

Scenarios:
- stamped config validates
- legacy config without the field remains valid
- non-string stamp is rejected

### invocation-time-drift-check

Scenarios:
- upgrade drift warns once on stderr
- downgrade drift also warns
- matching versions stay silent
- install and init skip the check
- absent stamp stays silent
- missing or corrupt config skips silently
- drift never changes exit codes

### json-output-carries-template-version-mismatch

Scenarios:
- mismatch appears in JSON payload
- no mismatch means no key
- absent stamp means no key
- stderr warning does not corrupt stdout JSON

### doctor-template-freshness-check

Scenarios:
- matching stamp passes
- mismatched stamp warns with both versions
- missing stamp warns without failing doctor

## instruction-contracts

### Persona Text Is Derived At Runtime From The Agent Definition

Scenarios:
- Editing an agent definition file changes the emitted persona
- No persona string literals remain in the instruction-generating source

### Every Referenced Agent Name Resolves To An Existing Agent Definition

Scenarios:
- All agent references across shipped workflows resolve
- A newly introduced agent reference without a definition file is detectable

### Agent Resolution Failure Fails Loudly, Never Silently Substitutes

Scenarios:
- Unresolvable agent name produces a named error instead of a fallback persona
- A resolvable agent name never triggers the failure path

### Agent Aliases Are Explicit And Resolve To The Real Agent's Identity

Scenarios:
- A declared alias resolves to its mapped agent's own persona
- An undeclared name is never treated as an implicit alias

### Emitted Instructions Contract Carries Complete Agent Identity

Scenarios:
- Instructions output includes name, persona, tools, and model sourced consistently
- A tool list change in the agent definition is reflected in the next generation
- No models configuration yields inherit for every emitted model field

### Source And Deployed Agent Definitions Remain Byte-Identical

Scenarios:
- Source and deployed agent definitions match after an edit
- A divergence between source and deployed copies is detectable

### Planning Cohort Requires Top-Tier Model

Scenarios:
- Shipped planning-cohort agent definitions carry no downgraded model
- Planning-cohort roles resolve to inherit under every configuration and tier
- The configuration schema exposes no planning-cohort role key

### Tier-Coupled Executor Routing

Scenarios:
- Quick-tier executor invocation resolves to the configured cheap-executor model
- Standard-tier executor invocation resolves to inherit regardless of profile
- A tier change since intent time is reflected at generation time

### Safety-Net Immunity For Reviewer And Verifier

Scenarios:
- A configuration assigning a downgraded model to reviewer is rejected
- A configuration assigning a downgraded model to verifier is rejected identically
- Every shipped named profile resolves reviewer and verifier to top-tier

### Rung-1 Model Escalation On STOP Or Verify-FAIL

Scenarios:
- An in-tier STOP report escalates the next run to top-tier
- An in-tier verify-FAIL escalates the fix run to top-tier
- A Rung-1 escalation is recorded in a durable audit record

### Rung Discrimination Between Model And Workflow Escalation

Scenarios:
- A Rung-1 escalation leaves the workflow tier unchanged
- Scope overflow routes to workflow escalation, not model escalation
- A workflow-tier upscale brings planning artifacts to top-tier

### Escalation-Rate Metric In Progress Reporting

Scenarios:
- Both output modes report a computed escalation rate
- Zero escalations among recorded invocations reports as zero, not omitted
- No recorded cheap-tier invocations renders an explicit no-data indicator

### Model Vocabulary Validated At Config Load

Scenarios:
- A model value within the documented vocabulary validates successfully
- A model value outside the documented vocabulary is rejected
- A rejected configuration is never silently substituted

## issue-logging

### Metta-issue skill performs root cause analysis before writing the ticket

Scenarios:
- Happy path — structured ticket written after RCA
- Interactive path — description not provided as argument

### Metta-issue skill supports --quick escape hatch

Scenarios:
- --quick skips RCA and writes a shallow ticket

### Metta-issue skill falls back to shallow log when RCA fails

Scenarios:
- RCA fails mid-session and fallback body is written

### Metta-issue skill writes a structured body with fixed section order

Scenarios:
- Full structured body validates section order and Evidence citation

### Metta issue CLI auto-detects piped stdin as body

Scenarios:
- Structured body piped via stdin becomes the issue body
- Interactive TTY stdin leaves behavior unchanged

### Issues-store parseIssue tolerates both freeform and structured bodies

Scenarios:
- Both freeform and structured bodies round-trip through metta issues show

### Metta-fix-issues skill surfaces structured issue sections at step 1

Scenarios:
- Structured sections displayed before fix planning begins

### Issue frontmatter schema is Zod-validated and strict

Scenarios:
- Partial frontmatter is accepted with documented defaults
- Invalid priority value is rejected with a clear error
- Unknown frontmatter key is rejected

### Frontmatter-less issue files parse exactly as before

Scenarios:
- Legacy issue lists and resolves unchanged
- Legacy issue is excluded from backlog and milestone views

### Backlogging an issue mutates the existing issue file in place

Scenarios:
- Existing issue gains backlog frontmatter with no new file
- Re-backlogging is an idempotent no-op
- Later body edits never drift from the backlog

### New ideas are minted as typed entries in the issue store

Scenarios:
- New idea minted with idea type and backlog flag
- Mistyped slug without --new fails instead of minting
- Idea entries are distinguishable in issue listings

### Backlog list is a sorted view over issue frontmatter

Scenarios:
- Mixed priorities and orders sort deterministically
- Only flagged issues appear and spec/backlog/ is never read
- Missing optional fields render with defaults

### Backlog promote hands off to fix-issues

Scenarios:
- Promote emits a fix-issues handoff
- Promoting an unknown slug fails cleanly

### Backlog done resolves through the issue store archive

Scenarios:
- Done moves the issue to resolved and off the backlog
- Shipped-in stamp survives the new archive path
- Done commits only the archived pair of paths

### Milestone store with Zod-validated frontmatter and CLI

Scenarios:
- Milestone created with defaults
- Creating a duplicate milestone is refused
- Invalid milestone status is rejected

### Milestone and priority assignment via issue frontmatter

Scenarios:
- Log an issue with milestone and priority in one step
- Invalid priority at log time is rejected
- Dangling milestone reference warns but succeeds

### Milestone show reports resolved-vs-open progress

Scenarios:
- Rollup counts resolved against open
- Empty milestone renders without failing

### Status and progress surfaces include milestone rollups

Scenarios:
- Progress shows per-milestone counts
- No milestones means no milestone section

### Idempotent migration of legacy backlog data into the issue store

Scenarios:
- Active and done items convert with content preserved
- Second run is a no-op
- Slug collision is reported, not overwritten

### Frontmatter writes round-trip the body and untouched fields

Scenarios:
- Targeted frontmatter update leaves everything else intact
- Archive preserves frontmatter end to end

### Backlog auto-commits stage only the files the command wrote

Scenarios:
- Unrelated dirty file survives a backlog add
- Done and migrate stage only their own moved or updated files
- No directory pathspecs at any commitPaths call site

### Backlog and milestone list renderers sanitize titles

Scenarios:
- ANSI escape in a backlog title is stripped
- Control characters in a milestone issue title are stripped
- Plain titles render unchanged
- Sanitization does not rewrite the issue file

### No stale spec/backlog references in generated CLAUDE.md, docs, or guard-edit allowlist

Scenarios:
- Refresh emits a TOC without a spec/backlog row
- Docs describe the frontmatter-over-issues model
- Guard-edit denies edits under the retired path

### Bare metta backlog is allowed by the guard-bash hook

Scenarios:
- Bare backlog invocation is allowed
- Both hook copies stay byte-identical
- Write forms remain gated

### Single issues-store test file with no compiled test code in dist

Scenarios:
- Build output contains no compiled test module
- Unique test cases survive consolidation
- Exactly one issues-store test file in the tree

### Tier advisory recommendation is capped at standard

Scenarios:
- Full-scored change renders a capped advisory
- Standard-over-quick recommendation is unchanged
- Scoring values are untouched by the cap

### CLI JSON output escapes DEL and C1 control characters

Scenarios:
- Raw CSI byte in a stored issue title is escaped in issues show --json
- DEL in a user-influenced field never reaches stdout raw
- JSON error envelopes receive the same escaping
- Boundary neighbors and multi-byte characters pass through unchanged

### JSON escaping preserves parsed-value fidelity and never mutates stored data

Scenarios:
- JSON.parse round-trips escaped output to the exact stored strings
- Existing byte-faithful test suites pass unmodified
- Stored files are untouched by JSON emission

### Shared pure escape helper applied at every CLI stdout JSON edge

Scenarios:
- config get escapes C1 controls identically to outputJson
- Tasks --json rendering routes through the shared helper
- Helper is idempotent and precise at range boundaries

## orchestration-guard

### Inline Command-Text Tokens Never Authorize a Blocked Subcommand

Scenarios:
- A bare inline token with no external signal is rejected
- No code path treats inline command text as sufficient on its own

### Fork-Dispatched Subcommands Require Verified Caller Identity

Scenarios:
- Fork-dispatched subcommand with verified caller identity is accepted
- Fork-dispatched subcommand without verified caller identity is rejected

### Main-Session Lifecycle Subcommands Require a Non-Forgeable Session Credential

Scenarios:
- Sanctioned skill-driven call with a valid credential is accepted
- Expired, out-of-scope, or malformed credential is rejected
- Credential forgery requires an audit-visible act outside command text
- No credential present is rejected with skill guidance
- Concurrent skill credentials do not interfere

### Unrecognized metta Subcommands Fail Closed

Scenarios:
- An unrecognized subcommand is blocked, not silently allowed
- A newly added unknown subcommand does not silently pass until classified

### Every Rejection and Every Tier-2 Acceptance Is Recorded

Scenarios:
- A session-tier rejection is recorded with tier and reason
- A session-tier acceptance is recorded
- A fork-tier rejection is recorded with tier and reason

### Skill Contracts Reference Only the Sanctioned Authorization Mechanism

Scenarios:
- No skill contract instructs typing a self-authored token
- A newly added skill contract that reintroduces a self-authored token is a detectable violation

### Forked Agents Are Blocked From Running Background Bash

Scenarios:
- Background Bash from a verified fork-tier caller is rejected
- Background Bash from a caller without verified fork identity is unaffected by this rule

### The Trust Model Is Documented Where Operators and Contributors Will Read It

Scenarios:
- Header documentation describes both tiers and the emergency bypass
- Generated workflow guidance matches the documented model

### Fork Dispatch Completion Guarantee

Scenarios:
- Enforcement fires when a fork attempts to end its turn with a pending dispatch
- A fork whose dispatched work is complete ends its turn normally
- Non-fork callers are unaffected by this guarantee

### Truthful Fork Results

Scenarios:
- A terminal fork result never narrates in-progress work under enforcement
- The enforcement reason instructs the fork to wait rather than return early
- The skill-host contract treats in-progress narration as a non-terminal, failed result

### Residual Orphaning Recovery Protocol Is Codified in Every Fork Contract

Scenarios:
- Contract inspection finds the recovery protocol in every fork contract
- The protocol explicitly forbids duplicate dispatch of in-flight work
- Fresh work is only dispatched after the orphan is confirmed dead or complete

### Fork-Dispatch Enforcement Events Are Recorded

Scenarios:
- A blocked turn-end is recorded with tier and reason
- A rejected async dispatch shape is recorded with tier and reason
- A recovery-protocol invocation is discernible from recorded evidence

### Worktree Edits Are Allowed Under the Inverted-Hosting Topology

Scenarios:
- Inverted-hosting topology edit is allowed
- Empty answer from the target root alone does not block
- Subagents no longer need the heredoc fallback

### Canonical Worktree Topology Remains Allowed

Scenarios:
- Canonical topology edit is still allowed

### Guard Still Blocks When No Change Is Active in Either Root

Scenarios:
- No active change anywhere still blocks

### Probe Failures Continue to Fail Open

Scenarios:
- Each probe-failure mode fails open

### Init-Phase and Issues Allow-Lists Are Unchanged

Scenarios:
- Allow-listed paths still pass without an active change

### Regression Tests Exercise Real Discovery Semantics for the Inverted Topology

Scenarios:
- Inverted-topology test catches the original defect
- Topology coverage does not come from a cwd-answering shim
- Both topologies are covered alongside existing cases

## propose-stop-after

### `metta propose` MUST accept a `--stop-after <artifact>` option

Scenarios:
- option appears in CLI help
- option is accepted with a valid value
- option is omitted, full-lifecycle behavior preserved

### `--stop-after` MUST be validated against the resolved workflow

Scenarios:
- unknown artifact id is rejected before any side effects
- execution-phase artifact id is rejected
- planning-phase id from a non-default workflow is accepted

### change-record schema MUST persist `stop_after` as an optional field

Scenarios:
- schema accepts records with `stop_after`
- schema accepts records without `stop_after`
- schema rejects non-string `stop_after`

### `ArtifactStore.createChange` MUST accept and persist a `stopAfter` argument

Scenarios:
- `createChange` writes `stop_after` when supplied
- `createChange` omits `stop_after` when not supplied

### propose skill MUST honor the `stop_after` boundary

Scenarios:
- skill parses and forwards `--stop-after` from `$ARGUMENTS`
- skill exits cleanly at the stop-after boundary for `tasks`
- skill exits cleanly at the stop-after boundary for `spec`
- skill behaves identically when no `stop_after` is set

### `metta status` MUST surface `stop_after` in JSON output

Scenarios:
- `metta status --json` reflects `stop_after` when set
- `metta status --json` omits or nulls `stop_after` when not set

### `--stop-after` MUST compose with all existing propose flags

Scenarios:
- `--stop-after` composes with `--workflow` and `--auto`
- `--stop-after` composes with `--from-issue`

### handoff message MUST be deterministic and matchable

Scenarios:
- tests can assert the handoff line shape
- no implementation-implying lines appear

## release-versioning

### Release Configuration Schema

Scenarios:
- Valid semver config accepted
- Unsupported scheme rejected with key named
- Malformed version-file path rejected
- Defaults applied for omitted optional keys

### Product Version Distinct From Installed Version

Scenarios:
- Current product version read from configured file
- Version file missing yields distinguishing error
- Version drift stamp untouched by release operations

### Purely Additive When Unconfigured

Scenarios:
- Existing lifecycle unchanged without release config
- Release command without config fails actionably

### Bump Derivation From Shipped Changes

Scenarios:
- Only fixes recommend patch
- Feature present recommends minor
- Breaking marker recommends major
- Derivation is deterministic and pure

### User Override Of Recommended Bump

Scenarios:
- Override recommendation with explicit level
- Accepting the recommendation

### Release Cut Operation

Scenarios:
- One invocation produces consistent release artifacts
- Tag annotation carries release identity

### Release Cut Safety Constraints

Scenarios:
- Nothing pushed without explicit confirmation
- Existing tag aborts without force
- Mid-cut failure is reported with the failing step

### First Release Without Prior Tag

Scenarios:
- First release derives from all shipped changes
- Manual tag treated as release boundary

### Version-Anchored Changelog Generation

Scenarios:
- Changes split across release boundary
- Multiple versions render in release order
- Versioned shape survives finalize regeneration

### Pre-Existing Manual Release History Rendering

Scenarios:
- Manual tags render without losing entries

### Opt-In GitHub Release Publication

Scenarios:
- No confirmation means no gh invocation
- Confirmed opt-in publishes release notes

### Graceful Degradation When gh Unavailable

Scenarios:
- Missing gh binary degrades gracefully
- Unauthenticated gh degrades gracefully

### Release CLI Command Surface

Scenarios:
- Human runs release CLI directly
- Version status command reports version and recommendation

### Release Skill And Guard Authorization

Scenarios:
- Skill-mediated release is authorized
- Unauthorized AI invocation is blocked
- Skill delivered as template file

## roadmap-feature

### Roadmap persists as a single ordered markdown file managed by RoadmapStore

Scenarios:
- Entries round-trip through format and parse in order
- Missing roadmap file reads as an empty roadmap
- Unsafe slug is rejected at the store boundary

### Default roadmap command is a read-only ordered status view

Scenarios:
- Populated roadmap listed in order
- JSON view mirrors the text view
- Read-only view runs on any branch without a guard
- Empty roadmap renders a friendly empty state

### Dangling entries are surfaced, never fatal

Scenarios:
- Deleted backlog item shows as dangling
- Dangling flag present in JSON output

### roadmap add appends an existing backlog item to the end of the queue

Scenarios:
- Valid slug appended with a note and auto-committed
- Unknown backlog slug is rejected as not_found
- Duplicate roadmap entry is rejected

### roadmap reorder accepts only a complete non-interactive permutation

Scenarios:
- Full permutation rewrites the order
- Omission, addition, and duplicate are each rejected with no partial write

### roadmap next activates the top entry with a propose handoff and pops it

Scenarios:
- Top entry activated and removed from the queue
- Dangling top entry fails not_found without popping
- Empty roadmap is a friendly no-op

### Mutating roadmap operations enforce main-branch and auto-commit discipline

Scenarios:
- Non-main branch blocks each mutation
- Escape hatch permits a deliberate off-main mutation

### Roadmap failures use the standard error contract

Scenarios:
- Envelope shape is consistent across failure types
- Text mode reports the same failures on stderr

### Roadmap wiring is additive to the CLI context and barrel exports

Scenarios:
- Command group and store are reachable through standard wiring
- Backlog behavior is untouched

### Guard hook tiers roadmap forms — mutations Tier 2, status view unguarded

Scenarios:
- Uncredentialed AI session is blocked from roadmap mutations
- Read-only view passes the guard without a credential
- Existing guard entries are unchanged

### The /metta-roadmap skill wraps all mutating roadmap operations

Scenarios:
- Skill invocation mints the credential and the guard authorizes the mutation
- Skill offers add and reorder against CLI-emitted slugs

### Orchestrators answer "what next?" from the roadmap top entry

Scenarios:
- Populated roadmap answers routing without user re-litigation
- Empty roadmap yields a clean fallback signal

## Schemas Specification

## Spec Model

### Spec Markdown Parsing

Scenarios:
- Full spec parsed
- MAY keyword extracted
- Empty spec handled

### Requirement Content Hashing

Scenarios:
- Stable hashes
- Different content produces different hash

### Delta Spec Parsing

Scenarios:
- Mixed delta operations parsed
- RENAMED operation parsed
- ADDED scenario prefix stripped

### Spec Lock Management

Scenarios:
- Lock created from spec
- Lock defaults to draft status and change source
- Custom source via createFromParsed
- Version incremented on update
- getBaseVersion returns null when no lock

## StateStore — Specification

## uat-execution

### UAT Runner Skill

Scenarios:
- Skill template and deployed copy are byte-identical
- Invoking the skill on an active change spawns the runner
- Skill introduces no CLI, guard, or Tier-2 surface

### UAT Document Location Rules

Scenarios:
- Active change UAT is preferred
- Named archive entry is resolved when a change name is given
- Fallback to newest archive entry
- No UAT found fails clearly

### UAT Runner Agent

Scenarios:
- Agent template and deployed copy are byte-identical with correct frontmatter
- Agent registry discovers the runner by filename
- Instruction-like step text is treated as data

### UAT Step Execution Semantics

Scenarios:
- Matching observation checks the box
- Contradicting observation leaves the box unchecked with a recorded discrepancy
- Environment-impossible step is skipped with a note
- Generated step content is never altered
- Header wording sanctions honest checkbox flips only

### UAT Run Record

Scenarios:
- Completed run appends a dated record
- Prior run sections survive later runs untouched

### UAT Idempotent Re-Runs

Scenarios:
- Checkboxes reset before evaluation
- Two runs yield two records and latest-run checkbox state
- Pass-then-fail step reflects the latest run while history keeps the pass

### UAT Failure-To-Issue Loop

Scenarios:
- Failed steps become logged issues via the orchestrator
- Runner never invokes fork-tier skills

### UAT Commit Ownership

Scenarios:
- Orchestrator commits after the run
- Agent contract forbids git

### UAT Model Routing Deferral

Scenarios:
- Runner always spawns at the inherited session model
- No fake instructions artifact is introduced

### Archived UAT Run Recording

Scenarios:
- Archived run changes only sanctioned regions
- Archive answers the acceptance question months later

## user-stories

### Stories Artifact Phase in Standard Workflow

Scenarios:
- Standard workflow lists stories between intent and spec
- Spec artifact requires stories in standard workflow
- Quick workflow has no stories artifact

### Stories Document Format

Scenarios:
- Well-formed multi-story document is accepted
- Internal sentinel document is accepted
- Missing required field causes rejection

### Stories Zod Schema and Parser

Scenarios:
- Parser returns three stories from valid document
- Parser returns sentinel document
- Parser throws StoriesParseError on missing field
- Parser throws on duplicate US-N
- Parser throws on non-monotonic IDs

### validate-stories CLI Command

Scenarios:
- Valid stories exits 0
- Invalid stories exits 4 with error detail
- Missing change directory exits 4 not_found

### Spec Parser Fulfills Field

Scenarios:
- Requirement with Fulfills line exposes fulfills array
- Requirement without Fulfills line has empty array
- Malformed Fulfills produces warning not error

### Finalize Stories Gate

Scenarios:
- Clean change with valid stories passes gate
- Missing stories.md fails gate
- Broken Fulfills reference fails gate
- Drift warning does not block finalize

### metta-product Agent

Scenarios:
- Agent file exists with correct frontmatter
- Deployed copy is byte-identical to source template

## WorkflowEngine — Specification

## workflow-parallelism-discipline

### SkillParallelismSelfCheck

Scenarios:
- self_check_block_present_in_propose_skill
- self_check_uses_imperative_language
- self_check_requires_file_listing

### SkillAntiExamples

Scenarios:
- anti_example_block_present_and_labeled
- anti_example_uses_stable_markers
- quick_skill_mirrors_propose_anti_example

### TasksPlanCommand

Scenarios:
- plan_command_exits_zero_on_valid_tasks_file
- plan_output_groups_disjoint_tasks_together
- plan_output_separates_conflicting_tasks
- plan_output_is_tty_safe

### TasksPlanJsonOutput

Scenarios:
- json_flag_produces_parseable_output
- json_wave_groupings_match_human_output
- json_each_wave_contains_required_fields

### ParallelWaveAlgorithm

Scenarios:
- all_independent_tasks_produce_one_wave
- all_conflicting_tasks_produce_sequential_waves
- mixed_batch_produces_correct_wave_grouping
- depends_on_directive_respected

### TasksPlanAbsentTasksFile

Scenarios:
- missing_tasks_file_exits_with_code_4
- missing_tasks_file_message_is_actionable

### ByteIdenticalSkillMirrors

Scenarios:
- propose_skill_pairs_are_byte_identical
- quick_skill_pairs_are_byte_identical
- existing_sync_test_still_passes
