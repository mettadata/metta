# metta propose slug truncation too aggressive: change names like 'drag-card-different-list-via-m' and 'checklist-bulk-actions-mark-al' lose meaningful context at the tail. With 40+ archived changes, slugs are hard to distinguish in metta progress and spec/archive/. Fix: allow longer slugs (60+ chars) or use smarter abbreviation (drop stop words, keep key nouns).

**Captured**: 2026-04-16
**Status**: logged
**Severity**: minor

metta propose slug truncation too aggressive: change names like 'drag-card-different-list-via-m' and 'checklist-bulk-actions-mark-al' lose meaningful context at the tail. With 40+ archived changes, slugs are hard to distinguish in metta progress and spec/archive/. Fix: allow longer slugs (60+ chars) or use smarter abbreviation (drop stop words, keep key nouns).
