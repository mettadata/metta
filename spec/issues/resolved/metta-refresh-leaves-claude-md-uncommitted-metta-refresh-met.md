# metta-refresh leaves CLAUDE.md uncommitted — metta refresh / /metta-refresh regenerates CLAUDE.md from spec/project.md and spec/specs/ but does not commit the result. Observed on zeus project 2026-04-18: after running /metta-refresh, git status showed modified: CLAUDE.md (68 lines changed). This pollutes any subsequent change working-tree diff and conflicts with the conventional-commits workflow where every artifact mutation should land as its own commit. The CLI has no --commit flag and the skill (src/templates/skills/metta-refresh/SKILL.md) makes no mention of committing. Expected: refresh either auto-commits with a conventional chore(refresh): regenerate CLAUDE.md message, or the skill instructs the orchestrator to commit after running. Either is fine, but the current behavior (silent working-tree drift) is surprising and risks CLAUDE.md updates getting bundled into unrelated commits.

**Captured**: 2026-04-18
**Status**: logged
**Severity**: critical

metta-refresh leaves CLAUDE.md uncommitted — metta refresh / /metta-refresh regenerates CLAUDE.md from spec/project.md and spec/specs/ but does not commit the result. Observed on zeus project 2026-04-18: after running /metta-refresh, git status showed modified: CLAUDE.md (68 lines changed). This pollutes any subsequent change working-tree diff and conflicts with the conventional-commits workflow where every artifact mutation should land as its own commit. The CLI has no --commit flag and the skill (src/templates/skills/metta-refresh/SKILL.md) makes no mention of committing. Expected: refresh either auto-commits with a conventional chore(refresh): regenerate CLAUDE.md message, or the skill instructs the orchestrator to commit after running. Either is fine, but the current behavior (silent working-tree drift) is surprising and risks CLAUDE.md updates getting bundled into unrelated commits.
