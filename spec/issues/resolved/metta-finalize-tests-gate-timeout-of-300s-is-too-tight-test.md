# metta finalize tests gate timeout of 300s is too tight — test suite takes ~305-315s at default concurrency. Defined in src/templates/gates/tests.yaml:4 timeout: 300000. Observed 2026-04-18 on change centralize-slugify-utility-strip-non-ascii-truncate-at-word: finalize tests gate timed out three times in a row despite all 576 tests passing (last two runs confirmed 576/576 when the gate was allowed to finish). The gate timeout sits right on the edge of the test duration — any transient CPU contention pushes it over. Combined with issue metta-finalize-tests-gate-leaks-vitest-worker-processes-on (orphans from timed-out runs compete with retries for CPU), this creates a cascading failure mode that blocks every non-trivial finalize. Fix options: (1) bump tests.yaml timeout to 600000 (10 min), (2) make timeout per-project overridable via .metta/gates/tests.yaml, (3) use an adaptive timeout based on previous observed duration. Option 1 is the minimal fix. Severity: major — blocks finalize on every change that crosses the 5-min test duration boundary, forcing either scope-creep gate bumps or manual archive/merge bypass.

**Captured**: 2026-04-18
**Status**: logged
**Severity**: major

metta finalize tests gate timeout of 300s is too tight — test suite takes ~305-315s at default concurrency. Defined in src/templates/gates/tests.yaml:4 timeout: 300000. Observed 2026-04-18 on change centralize-slugify-utility-strip-non-ascii-truncate-at-word: finalize tests gate timed out three times in a row despite all 576 tests passing (last two runs confirmed 576/576 when the gate was allowed to finish). The gate timeout sits right on the edge of the test duration — any transient CPU contention pushes it over. Combined with issue metta-finalize-tests-gate-leaks-vitest-worker-processes-on (orphans from timed-out runs compete with retries for CPU), this creates a cascading failure mode that blocks every non-trivial finalize. Fix options: (1) bump tests.yaml timeout to 600000 (10 min), (2) make timeout per-project overridable via .metta/gates/tests.yaml, (3) use an adaptive timeout based on previous observed duration. Option 1 is the minimal fix. Severity: major — blocks finalize on every change that crosses the 5-min test duration boundary, forcing either scope-creep gate bumps or manual archive/merge bypass.
