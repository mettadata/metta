# metta-product agent body still instructs git commit, contradicting the new orchestrator-owned-commits skill rule. After fix-metta-fix-issues-skill-contract-correct-cli-typo-metta-i landed (2026-04-18), all five skill files state: "Planning-artifact subagents (proposer, researcher, architect, planner, product) write files only — they do not run git." But src/templates/agents/metta-product.md:55 still tells the agent: `git add spec/changes/<change>/stories.md && git commit -m "docs(<change>): add user stories"`. Same drift likely exists in other planning agent bodies (metta-proposer, metta-architect, metta-planner, metta-researcher) which have Bash and may still carry legacy commit prose. Fix: audit all agent .md files in src/templates/agents/ and .claude/agents/; remove or rephrase any instructions that tell the agent to run git commit — the orchestrator owns those commits now. Severity: minor — subagents that commit anyway still produce correct state (the orchestrator catches up with a no-op commit) but the drift invites future contradictions if agent bodies and skill prose diverge.

**Captured**: 2026-04-18
**Status**: logged
**Severity**: minor

metta-product agent body still instructs git commit, contradicting the new orchestrator-owned-commits skill rule. After fix-metta-fix-issues-skill-contract-correct-cli-typo-metta-i landed (2026-04-18), all five skill files state: "Planning-artifact subagents (proposer, researcher, architect, planner, product) write files only — they do not run git." But src/templates/agents/metta-product.md:55 still tells the agent: `git add spec/changes/<change>/stories.md && git commit -m "docs(<change>): add user stories"`. Same drift likely exists in other planning agent bodies (metta-proposer, metta-architect, metta-planner, metta-researcher) which have Bash and may still carry legacy commit prose. Fix: audit all agent .md files in src/templates/agents/ and .claude/agents/; remove or rephrase any instructions that tell the agent to run git commit — the orchestrator owns those commits now. Severity: minor — subagents that commit anyway still produce correct state (the orchestrator catches up with a no-op commit) but the drift invites future contradictions if agent bodies and skill prose diverge.
