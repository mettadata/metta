---
priority: medium
---
# spec/specs/roadmap-feature/spec.md normative drift: lines ~25,50,53,65,68,73,138 still require the deleted BacklogStore and spec/backlog/<slug>.md resolution, but roadmap.ts was repointed to issuesStore in the backlog/milestones rework (PR #85). Living spec contradicts shipped code in a normative document. Route through spec reconciliation (fix-gap or a dedicated change) rather than a docs sweep.

**Captured**: 2026-08-17
**Status**: logged
**Severity**: minor

spec/specs/roadmap-feature/spec.md normative drift: lines ~25,50,53,65,68,73,138 still require the deleted BacklogStore and spec/backlog/<slug>.md resolution, but roadmap.ts was repointed to issuesStore in the backlog/milestones rework (PR #85). Living spec contradicts shipped code in a normative document. Route through spec reconciliation (fix-gap or a dedicated change) rather than a docs sweep.
