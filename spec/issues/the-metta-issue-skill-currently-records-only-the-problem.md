# The /metta-issue skill currently records only the problem statement — it calls metta issue with the description and stops. Issues logged this way are shallow: they name a symptom, not a root cause. Upgrade the skill to run a short debugging session before writing the ticket. The AI should (1) read relevant source files and recent git history around the symptom, (2) trace the call path to identify the most likely root cause, (3) propose 1-3 candidate solutions with tradeoffs, (4) write all three sections (Symptom, Root Cause Analysis, Candidate Solutions) into the issue body. The CLI currently creates a file with just the description — either extend it to accept a long-form body via stdin / --body-file, or have the skill post-process the file after creation. Emergency simple-logging escape: --quick flag that keeps today's behavior for obvious symptoms. Motivation: logged issues today are often just 'X is broken' headlines; fixers then redo the debugging work from scratch. Pre-work at log time is cheaper because the AI still has the full observation context.

**Captured**: 2026-04-19
**Status**: logged
**Severity**: major

The /metta-issue skill currently records only the problem statement — it calls metta issue with the description and stops. Issues logged this way are shallow: they name a symptom, not a root cause. Upgrade the skill to run a short debugging session before writing the ticket. The AI should (1) read relevant source files and recent git history around the symptom, (2) trace the call path to identify the most likely root cause, (3) propose 1-3 candidate solutions with tradeoffs, (4) write all three sections (Symptom, Root Cause Analysis, Candidate Solutions) into the issue body. The CLI currently creates a file with just the description — either extend it to accept a long-form body via stdin / --body-file, or have the skill post-process the file after creation. Emergency simple-logging escape: --quick flag that keeps today's behavior for obvious symptoms. Motivation: logged issues today are often just 'X is broken' headlines; fixers then redo the debugging work from scratch. Pre-work at log time is cheaper because the AI still has the full observation context.
