# Summary: fix-statusline-spec-duplicated-requirements

## What changed

Resolves issue `spec-specs-claude-statusline-spec-md-contains-a-full` (legacy spec corruption — duplicated requirements).

1. **`spec/specs/claude-statusline/spec.md`** — deleted the byte-identical duplicate block (former lines 257–510). File went from 510 lines / 18 `## Requirement:` headers to 256 lines / 9 headers. No requirement content was added, removed, or reworded.
2. **`spec/specs/claude-statusline/spec.lock`** — regenerated through `SpecLockManager.update()` + `parseSpec()` (the project's own lock path, run via tsx). New lock: version 19, 9 requirement entries (was 18 duplicated entries), hash `sha256:711b478bca16`. Per-requirement hashes unchanged, confirming content identity.
3. **`CLAUDE.md`** — regenerated via `metta refresh`. claude-statusline requirement count corrected from 86 to 49 (count = MUST/SHALL keywords; the stale 86 predated PR #71's edits). Other sections refreshed as a side effect of the standard refresh flow (project, conventions, specs table, reference, workflow).

## Repo-wide duplicate scan (verification-only, per intent)

Scanned every `spec/specs/*/spec.md` for repeated `## Requirement:` names. Three additional corrupted specs found — out of scope for this change, to be logged as a follow-up issue:

- `spec/specs/fix-issues-command/spec.md` — 4 duplicated requirement names (cli-registration, fix-issue-cli-command, issues-store-archival, skill-template)
- `spec/specs/install-init/spec.md` — 2 duplicated requirement names (init-command-drives-discovery, init-skill-invokes-init-command)
- `spec/specs/user-stories/spec.md` — 7 duplicated requirement names (Finalize Stories Gate, metta-product Agent, Spec Parser Fulfills Field, Stories Artifact Phase in Standard Workflow, Stories Document Format, Stories Zod Schema and Parser, validate-stories CLI Command)

## Verification

- Halves of the original file diffed identical (modulo one trailing blank line) before deletion.
- Post-dedupe: `grep -c '^## Requirement:'` = 9; `parseSpec` parses 9 requirements.
- Lock regenerated by the real code path, not hand-edited.
- No TypeScript source or tests changed; gates (tests, typecheck, lint, build) run at verification.

## Root cause disposition

Confirmed legacy corruption predating the idempotent-ADDED merger guard (`src/finalize/spec-merger.ts:177`). No merger code change needed — the current append path returns 'noop' for already-present requirement names.

## Verification results (2026-08-11)

- `npx tsc --noEmit` — clean
- `npm run lint` — exit 0
- `npm test` — 118 files, 2085/2085 tests passed
- `npm run build` — exit 0
- Spec-content check: deduped spec diffed byte-identical to first copy of pre-change file; lock has 9 entries with unchanged per-requirement hashes; CLAUDE.md count regenerated by `metta refresh`.

Verdict: PASS (3/3 verification lenses — gates, static checks, spec-content evidence).
