# Tasks for fix-automatic-token-recording-via-posttooluse-hook-remove

All paths relative to the change worktree root: `/home/utx0/Code/metta/.metta/worktrees/fix-automatic-token-recording-via-posttooluse-hook-remove/`.

Batches are sequential; tasks within a batch touch disjoint file sets and run in parallel. One atomic commit per task.

## Batch 1 (no dependencies)

- [x] **Task 1.1: Add optional `source` provenance field to TokenUsageRecordSchema**
  - **Files**: `src/schemas/change-metadata.ts`, `tests/schemas.test.ts`
  - **Action**: In the strict `TokenUsageRecordSchema`, add `source: z.enum(['hook', 'prose']).optional()` as the final field. Nothing else changes: schema stays `.strict()`, `tokens` stays positive-int, `model` stays `ModelAliasEnum`, and `artifact_tokens` / the rest of `ChangeMetadataSchema` are untouched. Extend `tests/schemas.test.ts` with cases: (a) hook-sourced record `{ task: "impl", agent: "executor", model: "haiku", tokens: 41250, timestamp: "2026-08-08T12:00:00.000Z", source: "hook" }` parses and round-trips inside a `token_usage` array through `ChangeMetadataSchema`; (b) `source: "manual"`, `source: 1`, and an unknown extra key alongside a valid `source` each throw a Zod error; (c) a legacy record with no `source` field parses with `source` undefined; (d) `.metta.yaml`-shaped metadata without `token_usage` remains valid.
  - **Verify**: `npx vitest run tests/schemas.test.ts` passes; `npx tsc --noEmit` clean.
  - **Done**: `TokenUsageRecordSchema` accepts optional `source: 'hook' | 'prose'`, rejects out-of-enum sources and unknown keys, legacy records validate unchanged, and all new schema tests pass.

- [x] **Task 1.2: Pure `detectWorktreeChangeName` in git-worktree util**
  - **Files**: `src/util/git-worktree.ts`, `tests/git-worktree.test.ts`
  - **Action**: Export `detectWorktreeChangeName(cwd: string, worktreeDir: string = DEFAULT_WORKTREE_DIR): string | null` from `src/util/git-worktree.ts` (which already owns `DEFAULT_WORKTREE_DIR = '.metta/worktrees'`). Pure path-segment math, no I/O (functional core — the caller does any `realpathSync`): resolve/normalize `cwd`, split into segments, split `worktreeDir` into its segment pair (`.metta`, `worktrees`), find the **last** adjacent occurrence of that pair that has a following segment, and return that following segment; otherwise return `null`. Extend `tests/git-worktree.test.ts`: cwd exactly at `<root>/.metta/worktrees/beta` returns `beta`; cwd nested below (`.../worktrees/beta/src/deep`) returns `beta`; a path containing two worktree-pair occurrences returns the change name from the last occurrence; repo-root and unrelated cwds return `null`; pair segments must be adjacent (`/x/.metta/other/worktrees/y` returns `null`); trailing separators tolerated.
  - **Verify**: `npx vitest run tests/git-worktree.test.ts` passes; `npx tsc --noEmit` clean.
  - **Done**: The exported pure function matches the design signature, last-occurrence-wins semantics are test-covered, and no I/O is performed inside the function.

## Batch 2 (depends on Batch 1)

- [x] **Task 2.1: Four-rule change resolution + `--source` option in `metta tokens record`**
  - **Depends on**: Task 1.1 (schema `source` field), Task 1.2 (`detectWorktreeChangeName`)
  - **Files**: `src/cli/commands/tokens.ts`, `tests/tokens-command.test.ts`
  - **Action**: One coherent edit to `tokens.ts` (this is the single task allowed to touch it). (1) Replace the current two-rule resolution block (lines 35–44) with the normative four-rule ordering: explicit `--change` wins verbatim; else `detectWorktreeChangeName(bestEffortRealpath(process.cwd()))` — wrap `fs.realpathSync` in try/catch falling back to the raw cwd — where a non-null candidate **binds unconditionally with no fall-through**: if the candidate is not in `ctx.artifactStore.listChanges()`, throw the typed error with message naming the derivation (`worktree cwd names change '<x>' but it is not an active change`) and write nothing; else auto-select when exactly one active change exists; else the existing typed error naming candidates, `exit(4)`, nothing written. (2) Add `.option('--source <origin>', ...)` accepting `hook|prose`; pass `source: options.source` into the `TokenUsageRecordSchema.parse` input so an invalid value fails Zod validation before any write and an omitted flag persists **no** `source` field (do not write `'prose'`). (3) Include the effective source in outputs: `--json` payload gains `source: record.source ?? 'prose'`; the human confirmation line mentions it. Update the file's doc comment (skills no longer call it after each subagent — hook-driven with prose fallback). Extend `tests/tokens-command.test.ts`: worktree-cwd recording with two active changes appends to the cwd's change and leaves the other untouched; repo-root single-active-change legacy invocation (no `--source`) still succeeds with a record carrying no `source` field; repo-root with two active changes and no `--change` emits `error: { code: 4, ... }` naming candidates, exits 4, no `token_usage` modified; worktree cwd naming an inactive change errors without falling through to the single-active change; `--source hook` persists `source: 'hook'` and the `--json` payload includes it; `--source guessed` exits 4 with nothing written.
  - **Verify**: `npx vitest run tests/tokens-command.test.ts` passes; `npx tsc --noEmit` clean.
  - **Done**: Resolution follows the four-rule ordering with no misattribution fall-through, `--source` is validated/persisted/reported per design, every pre-delta invocation shape behaves identically at a repo root with one active change, and all listed scenarios are test-covered.

- [x] **Task 2.2: Report-time dedupe, provenance column, GAPS/hook-health wording**
  - **Depends on**: Task 1.1 (records carry optional `source`)
  - **Files**: `src/finalize/tokens-report-generator.ts`, `src/templates/artifacts/tokens.md`, `tests/tokens-report-generator.test.ts`
  - **Action**: In `tokens-report-generator.ts` (signature and `TokensReportInput`/`TokensReportResult` unchanged; `finalizer.ts` call site untouched): (1) apply the normative pure dedupe filter before the total, per-artifact table, per-role rollup, per-model rollup, and inherit split — `key(r) = task + agent`; keep every `source === 'hook'` record; drop a record only when it is prose-sourced (`source` absent or `'prose'`) and a same-key hook record exists; never mutate the input array. (2) Add a `Provenance` column to the per-artifact table rendering `hook (exact)` / `prose (estimate)` via `r.source ?? 'prose'`. (3) Reword each GAPS line to hook-coverage-miss attribution, e.g. `` - `<key>` — run evidence with no token record; the recording hook missed this run ``, with no orchestrator/model non-compliance wording; keep the exact-string-match against `artifact_timings` keys and keep the `No gaps found.` sentence when coverage is complete. (4) In `src/templates/artifacts/tokens.md`, replace the blanket approximate-figures header wording with the hook=harness-measured-exact vs prose=orchestrator-estimated distinction. Seven-section structure and order stay unchanged. Extend `tests/tokens-report-generator.test.ts`: duplicate hook (41250) + prose (40000) records for `impl`/`executor` count once at 41250 in the total, table, and every rollup; prose-only legacy record (no `source`) is retained everywhere; two hook records sharing a key both count; a prose record with a different task id does not collapse; GAPS lists `implementation` with hook-miss wording when `artifact_timings` has the key but `token_usage` does not; complete coverage renders `No gaps found.`; section order is the pre-delta seven sections; the caller's `tokenUsage` array is not mutated by generation.
  - **Verify**: `npx vitest run tests/tokens-report-generator.test.ts` passes; `npx tsc --noEmit` clean.
  - **Done**: Dedupe is a pure report-time filter applied to all totals/rollups, provenance is distinguishable per row, GAPS reads as a hook-health indicator, header wording updated, and section structure is otherwise byte-compatible with the pre-delta format.

## Batch 3 (depends on Batch 2)

- [x] **Task 3.1: SubagentStop recording hook, settings registration, and hook test**
  - **Depends on**: Task 2.1 (`metta tokens record` accepts `--source hook` and resolves via `payload.cwd`)
  - **Files**: `src/templates/hooks/metta-tokens-record.mjs` (new), `.claude/hooks/metta-tokens-record.mjs` (new, byte-identical copy), `.claude/settings.json`, `tests/metta-tokens-record-hook.test.ts` (new)
  - **Action**: Author the hook in the template location, then copy byte-identically to `.claude/hooks/` and `chmod +x` both. Standalone `.mjs`, `#!/usr/bin/env node` shebang, Node builtins only (`node:fs/promises`, `node:child_process`, `node:util`, `node:path`), no shared lib; header comment notes the Claude Code >= 2.1.226 SubagentStop coupling and re-verify-on-upgrade. Pipeline (ADR-5, single guarded `main().catch(logStderr).finally(() => process.exit(0))`): read stdin JSON → exit silently unless `agent_type` is a string starting with `metta-` (ADR-3) → `readFile(payload.agent_transcript_path)` whole-file → per-line `JSON.parse` skipping malformed lines → collect assistant records with a `message.usage` object → parse all four usage components but sum only `input_tokens + output_tokens` with missing numerics as 0 (ADR-1) → short-circuit if no usage records or the sum is not a positive integer → derive flags: `--agent` = `agent_type`; `--task` = `AGENT_TASK_MAP[agent_type] ?? agent_type` with the static map `metta-proposer→intent, metta-specifier→spec, metta-product→stories, metta-researcher→research, metta-architect→design, metta-planner→tasks, metta-executor→implementation, metta-reviewer→implementation, metta-verifier→verification` (ADR-2); `--model` from the **last** usage-bearing record's `message.model`, mapped by case-insensitive substring containment of `haiku`/`sonnet`/`opus`/`fable`, else `inherit` (ADR-6) → `execFile('metta', ['tokens','record','--task',...,'--agent',...,'--model',...,'--tokens',String(sum),'--source','hook'], { cwd: payload.cwd ?? process.cwd(), timeout: 30_000 })` under its own try/catch, non-zero exit becomes a one-line stderr note. Never write stdout (no decision JSON at all), never write into `.metta/`, no `--change`, no retry; stderr diagnostics prefixed `metta-tokens-record:` including component sums. In `.claude/settings.json`, add a `SubagentStop` block (no tool matcher) invoking `.claude/hooks/metta-tokens-record.mjs`; do not touch the existing `PreToolUse` entries (guard-edit, guard-bash) or the `statusLine` block. New `tests/metta-tokens-record-hook.test.ts` (1:1 with the template hook) executes the **template** hook as a child process against a temp dir with fixture payloads/transcripts and a stub `metta` shim script prepended to PATH that captures argv to a file. Cover: exact-sum recording (fixture transcript whose input+output components sum to 42000 → shim received `--tokens 42000 --source hook`, `--agent metta-executor`, `--task implementation`, `--model haiku` from a haiku model id); unmapped agent type falls back to `agent_type` as `--task`; unmapped/absent model → `--model inherit`; non-`metta-*` agent_type → no shim invocation; missing transcript path, unreadable file, empty file, and usage-free transcript → no shim invocation; malformed lines skipped while valid lines still sum; shim exiting non-zero → hook still exits 0; `metta` absent from PATH → exits 0; **every** case asserts exit code 0 and empty stdout; plus template/deployed byte-identity (`readFile` compare) and `node --check` on both copies.
  - **Verify**: `npx vitest run tests/metta-tokens-record-hook.test.ts` passes; `node --check src/templates/hooks/metta-tokens-record.mjs && node --check .claude/hooks/metta-tokens-record.mjs`; `cmp src/templates/hooks/metta-tokens-record.mjs .claude/hooks/metta-tokens-record.mjs`; `node -e "const s=require('/home/utx0/Code/metta/.metta/worktrees/fix-automatic-token-recording-via-posttooluse-hook-remove/.claude/settings.json'); const h=JSON.stringify(s.hooks.SubagentStop); if(!h.includes('metta-tokens-record.mjs')) process.exit(1); if(!JSON.stringify(s.hooks.PreToolUse).includes('metta-guard-bash.mjs')||!JSON.stringify(s.hooks.PreToolUse).includes('metta-guard-edit.mjs')) process.exit(1)"`; `npx vitest run tests/metta-guard-bash.test.ts tests/metta-guard-edit.test.ts` still green (guard decisions unchanged).
  - **Done**: SubagentStop registration exists with the byte-identical, syntactically valid hook pair; the hook records exact ADR-1 sums for `metta-*` agents only, swallows every failure mode with exit 0 and empty stdout, and existing guard registrations/decisions are unchanged.

- [x] **Task 3.2: Demote skill prose mandate, refresh guard comment, invert skill test**
  - **Depends on**: Task 2.1 (`--source prose` fallback flag exists), Task 3.1 conceptually (hook is the default the fallback sentence references) — file sets are disjoint so this runs in parallel within Batch 3
  - **Files**: `src/templates/skills/metta-plan/SKILL.md`, `src/templates/skills/metta-execute/SKILL.md`, `src/templates/skills/metta-verify/SKILL.md`, `src/templates/skills/metta-next/SKILL.md`, `.claude/skills/metta-plan/SKILL.md`, `.claude/skills/metta-execute/SKILL.md`, `.claude/skills/metta-verify/SKILL.md`, `.claude/skills/metta-next/SKILL.md`, `src/templates/hooks/metta-guard-bash.mjs`, `.claude/hooks/metta-guard-bash.mjs`, `tests/skill-tokens-record.test.ts`
  - **Action**: In each of the four skills (template and deployed copies), replace the per-subagent recording mandate sentence in place — metta-plan line 24 (step 2d), metta-execute line 50 (standalone paragraph), metta-verify line 26 (step 3), metta-next line 30 (Rules bullet) — with the ADR-4 sentence, verbatim-identical in all four (preserving each site's list/step prefix so lettering/numbering is stable): `Token recording is automatic — a SubagentStop hook records each subagent's harness-measured usage; do not run \`metta tokens record\` after subagent returns. Only if the hook is unavailable, record manually: \`metta tokens record --task <artifact-or-task-id> --agent <subagent-type> --model <alias> --tokens <count> --change <name> --source prose\`.` No other diff — model routing, agent selection, and all other wording unchanged. In `metta-guard-bash.mjs` (both copies), rewrite only the line-24 comment on the retained `'tokens'` allowlist entry to describe hook-driven recording plus the manual `--source prose` fallback; the entry itself and all logic are untouched and the pair stays byte-identical. Invert `tests/skill-tokens-record.test.ts`: for all eight skill files assert the old mandate string (`After each subagent returns, record its reported token usage`) is absent, the ADR-4 sentence is present exactly once verbatim, and each template/deployed pair is byte-identical; add a byte-identity assertion for the guard-bash pair and an assertion that its `ALLOWED_SUBCOMMANDS` still contains `'tokens'`.
  - **Verify**: `npx vitest run tests/skill-tokens-record.test.ts` passes; `for s in metta-plan metta-execute metta-verify metta-next; do cmp src/templates/skills/$s/SKILL.md .claude/skills/$s/SKILL.md || exit 1; done`; `cmp src/templates/hooks/metta-guard-bash.mjs .claude/hooks/metta-guard-bash.mjs`; `npx vitest run tests/metta-guard-bash.test.ts tests/cli-metta-guard-bash-integration.test.ts` still green (comment-only change).
  - **Done**: No skill mandates per-subagent recording; the only remaining mention is the verbatim ADR-4 fallback sentence; all four skill pairs and the guard pair are byte-identical; guard behavior and the `tokens` allowlist entry are unchanged; the inverted test locks all of this in.

## Batch 4 (depends on Batch 3)

- [x] **Task 4.1: Full gate — build, typecheck, complete test suite**
  - **Depends on**: Tasks 3.1, 3.2 (and transitively all prior tasks)
  - **Files**: none expected (fix-forward only if a gate fails; any fix stays within files owned by the failing task's scope)
  - **Action**: Run the full verification gate from the worktree root: `npm run build`, then confirm the template copy step shipped the new hook (`test -f dist/templates/hooks/metta-tokens-record.mjs`) and that it matches the source template (`cmp src/templates/hooks/metta-tokens-record.mjs dist/templates/hooks/metta-tokens-record.mjs`), then `npx tsc --noEmit`, then the complete suite `npx vitest run`. Investigate and fix any failure (including cross-task interactions such as report-generator fixtures vs the new schema field) before declaring done.
  - **Verify**: `npm run build && test -f dist/templates/hooks/metta-tokens-record.mjs && npx tsc --noEmit && npx vitest run` — all exit 0 with zero failing tests.
  - **Done**: Build, typecheck, and the entire test suite are green in one run from a clean state, and the built `dist/` includes the byte-identical recording hook template.
