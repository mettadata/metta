# Tasks for fix-guard-edit-worktree-write-friction-caused-cross-repo

change_root = `/home/utx0/Code/metta/.metta/worktrees/fix-guard-edit-worktree-write-friction-caused-cross-repo`
All paths below are relative to change_root. All Verify commands run with `cd` into change_root.

## Batch 1 (no dependencies)

- [x] **Task 1.1: Layer 1 — template/skill shell-write path discipline (+ discipline test)**
  - **Files**: `src/templates/agents/metta-executor.md`, `.claude/agents/metta-executor.md`, `src/templates/agents/metta-verifier.md`, `.claude/agents/metta-verifier.md`, `src/templates/skills/metta-execute/SKILL.md`, `.claude/skills/metta-execute/SKILL.md`, `src/templates/skills/{metta-quick,metta-auto,metta-fix-issues,metta-fix-gap,metta-propose,metta-verify}/SKILL.md` + their six `.claude/skills/...` twins, `tests/shell-write-path-discipline.test.ts` (new)
  - **Action**: Per design C1, all edits additive. Executor template: append Deviation Rule 6 (silent-write anomaly → STOP-and-report, never bash rewrite) and insert the `## Shell-Write Path Discipline` section (prompt-provided `change_root` authoritative; all bash-mediated writes — `>`, `>>`, heredoc, `tee`, `cp`, `mv`, scripts — MUST target absolute paths under `change_root`; no `change_root` → no bash writes; git-status step doubles as write verification; scope note excluding build/test side-writes). Verifier template: same section adapted (verification artifact is the only sanctioned write) plus the appended line-63 heredoc-fallback clarification — append only, do not reword the strings pinned by `tests/agents-byte-identity.test.ts:23-27`. `metta-execute` SKILL.md: extend the spawn-contract paragraph (change_root binding, include change_root in every executor prompt), add the Silent-write STOP handling (orchestrator) paragraph, add Rule 6's one-line form to the Deviation Rules prompt block. Six sibling SKILL.md files: insert the byte-identical escalation sentence from design C1 in each Rules/STOP section. Every `src/templates/...` edit lands byte-identically in its `.claude/` twin. Write `tests/shell-write-path-discipline.test.ts` pinning: executor markers in both copies, verifier refusal/silent-success split marker, execute-skill escalation paragraph in both trees, and cross-file byte-identity of the escalation sentence across all six sibling skills in both trees.
  - **Verify**: `npx vitest run tests/shell-write-path-discipline.test.ts tests/agents-byte-identity.test.ts tests/template-deploy-sync.test.ts tests/skill-template-anchoring.test.ts`
  - **Done**: New test passes; `agents-byte-identity` and `template-deploy-sync` pass unmodified; all 18 template/twin files edited; existing Deviation Rules 1–5, Rules list, and completion contract byte-unchanged.

- [x] **Task 1.2: Layer 2 — guard-bash write-target check (+ hook test extension)**
  - **Files**: `.claude/hooks/metta-guard-bash.mjs`, `src/templates/hooks/metta-guard-bash.mjs` (byte-identical pair), `tests/metta-guard-bash.test.ts` (extend)
  - **Action**: Per design C2. Add `node:child_process`/`node:util` imports only. Implement `extractWriteTargets`, `toPhysicalPath`, `resolveTargetRoot`, `deriveProbeRoot`, `probeWorktreeContext` (cached per event, keyed by probeRoot), `checkWriteTargets`, with the KNOWN LIMITATION header comment and "port of metta-guard-edit.mjs — keep in sync" annotations. Extraction rules and confidence gate exactly as designed (plain absolute paths only; unterminated quoting and `$`/backtick/glob/relative targets fail open). Classification: block iff physical target inside probe root H AND NOT inside worktree W AND NOT inside `<H>/.metta/`. Insert in `main()` immediately after the background-Bash rejection (after line 425), before tokenize/offender scan (D8), with audit reason `'worktree-write-target'`, actionable stderr (target, mainRoot, expected change_root prefix, Edit-tool suggestion, bypass hint), exit 2, whole check in try/catch failing open. Extend the black-box test: PATH-shimmed `metta` fixture (guard-edit shim pattern), blocked matrix (`>`/`>>`, heredoc+`>`, `tee`/`tee -a`, `cp`, `mv`, `cp -t`), allowed matrix (worktree targets, `<H>/.metta/scratch/`, relative, `/tmp`, `$VAR`/`$(...)`, non-write commands, no active change, main-hosted change, `metta` absent, `2>&1`), and the Tier-2 token-untouched invariant pin for a compound authorized-metta + blocked-write command.
  - **Verify**: `npx vitest run tests/metta-guard-bash.test.ts` (full file — pre-existing suite must pass unmodified, including the hook byte-identity test at line 1206)
  - **Done**: Blocked matrix exits 2 with target + worktree prefix in stderr; allowed matrix exits 0; token file byte-unchanged on the compound-command pin; both hook copies byte-identical; zero pre-existing test modifications.

- [x] **Task 1.3: Layer 3 core — tree-baseline schema + module + barrels (+ module test)**
  - **Files**: `src/schemas/tree-baseline.ts` (new), `src/util/git-tree-baseline.ts` (new), `src/schemas/index.ts` (barrel export), `src/index.ts` (barrel export), `tests/git-tree-baseline.test.ts` (new)
  - **Action**: Schema per design Data Model: strict `TreeEntrySchema` (`path`, 2-char `status`, optional `renamed_from`) and `MainTreeBaselineSchema` (`change`, `main_root`, `recorded_at` datetime, `entries`). Module per design API: pure `parsePorcelain` (NUL-delimited `--porcelain=v1 -z -uno` output incl. two-field rename records), pure `diffTreeState` (new dirt = path absent from baseline OR XY status differs; else pre-existing), `BASELINE_DIR`/`baselineRelPath`, imperative `readMainTreeStatus`, `captureMainTreeBaseline` (write-once: `created=false` if file exists), `compareMainTree` (`main_root` mismatch ⇒ `hasBaseline: false`), `deleteMainTreeBaseline` (best-effort), and `MainTreeContaminationError` with `newDirt`. execFile-git pattern from `git-worktree.ts`; storage via `StateStore(join(mainRoot, '.metta'))` at `scratch/tree-baselines/<change>.yaml`. Add both barrel exports. Test: `parsePorcelain` matrix (clean, ` M`, `MM`, renames, paths with spaces), `diffTreeState` matrix (new path, ` M`→`MM` counts as new, pre-existing attribution, clean), shell capture/compare against a real mkdtemp git repo incl. write-once semantics, `main_root` mismatch as missing baseline, and StateStore schema round-trip.
  - **Verify**: `npx vitest run tests/git-tree-baseline.test.ts && npx tsc --noEmit`
  - **Done**: Module + schema compile and are barrel-exported; all pure-matrix and shell-fixture tests pass; write-once semantics proven by test.

- [x] **Task 1.4: Root-resolution helper `resolveMainCheckoutRoot` (+ helper test)**
  - **Files**: `src/cli/helpers.ts`, `tests/cli-helpers.test.ts` (extend)
  - **Action**: Per design API: add `resolveMainCheckoutRoot(projectRoot, changeName, metadata)` next to `resolveChangeRoot` (line 92). Logic: (1) `resolveChangeRoot(...) !== projectRoot` → return `projectRoot` (main-checkout invocation); (2) `detectWorktreeChangeName(projectRoot) === changeName` → strip the `<worktreeDir>/<name>` suffix, cross-checked/fallback via `git rev-parse --path-format=absolute --git-common-dir`; (3) otherwise `null` (layer 3 disengaged — covers `git.enabled: false` and worktree-fallback). Extend `tests/cli-helpers.test.ts` with a case per branch (main-checkout invocation, inside-worktree invocation, non-worktree change → null, name mismatch → null).
  - **Verify**: `npx vitest run tests/cli-helpers.test.ts && npx tsc --noEmit`
  - **Done**: All three resolution branches covered by passing tests; no imports from the new tree-baseline module (helper stands alone).

## Batch 2 (depends on Batch 1)

- [x] **Task 2.1: Baseline capture wiring in `instructions.ts` (+ capture tests)**
  - **Depends on**: Task 1.3, Task 1.4
  - **Files**: `src/cli/commands/instructions.ts`, `tests/instructions-stamps-timings.test.ts` (extend; or the cli-worktree instructions harness if a better fit)
  - **Action**: Per design C3 capture: inside the existing `preStatus === 'ready' || 'in_progress'` guard (lines 149–150), beside the started-stamp try/catch (lines 151–189) — when `artifactId === 'implementation'` and `resolveMainCheckoutRoot(...)` returns a root, call `captureMainTreeBaseline(mainRoot, changeName)`. Write-once (retries keep the original snapshot), stderr warning listing pre-existing dirty paths, own try/catch that warns and never blocks (mirror timings-stamp semantics). Tests: baseline recorded write-once for a worktree-hosted implementation instruction, pre-existing-dirt warning surfaced, no baseline for non-worktree changes, git failure warns and does not block instruction emission.
  - **Verify**: `npx vitest run tests/instructions-stamps-timings.test.ts && npx tsc --noEmit`
  - **Done**: Baseline file appears exactly once under `<mainRoot>/.metta/scratch/tree-baselines/` for worktree-hosted implementation instructions; non-worktree path byte-identical to pre-change behavior; capture failures never fail the command.

- [x] **Task 2.2: Complete gate — main-tree contamination check (+ cli-complete tests)**
  - **Depends on**: Task 1.3, Task 1.4
  - **Files**: `src/cli/commands/complete.ts`, `tests/cli-complete.test.ts` (extend)
  - **Action**: Per design C3 complete gate: insert immediately before `markArtifact` (line 230), outside the `if (!isWildcard)` block. For `artifactId === 'implementation'` with a resolved main root: `compareMainTree`; no baseline → stderr warn and pass; `newDirt.length > 0` → throw `MainTreeContaminationError` listing ONLY newDirt paths + status codes with the commit-or-stash remediation text; pre-existing only → stderr warning. Error boundary (lines 701–705): `type: err instanceof MainTreeContaminationError ? 'main_tree_contamination' : 'complete_error'`, exit code stays 4. Gate performs `git status` only — no mutation. Tests: contamination fails completion listing only new paths (demonstrably fails against pre-change behavior), no-baseline warn-and-pass, pre-existing-only passes, non-worktree change unchanged, JSON error carries `type: 'main_tree_contamination'` with exit 4, failure path performs no git mutation of main.
  - **Verify**: `npx vitest run tests/cli-complete.test.ts && npx tsc --noEmit`
  - **Done**: Contaminated worktree-hosted completion exits 4 with `main_tree_contamination` JSON naming only new dirt; all other complete paths (non-worktree, clean, pre-existing-only, no-baseline) behave exactly as pre-change.

- [x] **Task 2.3: Ship preflight — `main-checkout-clean` step + caller-fed wiring (+ merge-safety tests)**
  - **Depends on**: Task 1.3, Task 1.4
  - **Files**: `src/ship/merge-safety.ts`, `src/cli/commands/ship.ts`, `tests/merge-safety.test.ts` (extend)
  - **Action**: Per design C3/D7. `merge-safety.ts`: additive constructor options param with `MainCheckoutCleanInput { root, baselineEntries: TreeEntry[] | null }`; new step id `'main-checkout-clean'` emitted only when the input is provided, placed after `finalize-check` (line 112) and before the existing `preflight` (line 114); runs `git status --porcelain=v1 -z -uno` at `root`, applies pure `diffTreeState` (import pure functions only from `../util/git-tree-baseline.js` — pipeline stays StateStore-free); null baseline → `skip`, new dirt → `fail` with detail naming paths and `{ status: 'failure' }` return, pre-existing only → `pass` with warning detail. `run()` signature unchanged. `ship.ts` (before pipeline construction, line 38): derive change name from `sourceBranch.match(/^metta\/(.+)$/)`; via `ctx.artifactStore.getChange(name)` + `resolveMainCheckoutRoot` + `StateStore(join(mainRoot, '.metta'))`/`baselineRelPath(name)` build the `mainCheckout` input; any failure ⇒ omit input (non-worktree behavior); on non-dry-run success, best-effort `deleteMainTreeBaseline`. Tests: worktree fixture via `git worktree add`; fail/pass+warn/skip matrix; non-worktree step-list byte-identity against pre-change expectations.
  - **Verify**: `npx vitest run tests/merge-safety.test.ts && npx tsc --noEmit`
  - **Done**: Worktree-hosted ships get the new step with correct fail/pass/skip semantics; non-worktree ships produce a byte-identical step list; baseline deleted on successful non-dry-run ship.

## Batch 3 (depends on Batch 2)

- [x] **Task 3.1: Full gate suite**
  - **Depends on**: Task 2.1, Task 2.2, Task 2.3 (and all of Batch 1)
  - **Files**: none (verification only; fix-forward any failures in the files above)
  - **Action**: Run the complete gate suite in change_root: full test run, typecheck, lint, build (which exercises `copy-templates` — confirm `dist/` hook and templates regenerate; never hand-edit `dist/`). Also confirm the install/init gitignore template covers `.metta/scratch/` for consumer projects (design checklist item) — if missing, add it with a matching test tweak.
  - **Verify**: `npm test && npx tsc --noEmit && npm run lint && npm run build` (all from change_root)
  - **Done**: All four gates green; no pre-existing test modified to pass; template byte-identity and hook byte-identity gates green post-build.
