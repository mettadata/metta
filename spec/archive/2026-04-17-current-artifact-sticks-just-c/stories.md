# current-artifact-sticks-just-c — User Stories

No user stories — internal/infrastructure change

**Justification:** One-line bug fix to `markArtifact`'s `current_artifact` update condition. Visible impact is that the statusline (already shipped) now shows the correct active stage; no new user capability.
