# Tasks for rework-backlog-around-issue-store-as-single-source-truth

All Verify commands run from the worktree root: `/home/utx0/Code/metta/.metta/worktrees/rework-backlog-around-issue-store-as-single-source-truth`. Batches are sequential; tasks within a batch have fully disjoint file sets and run in parallel. One commit per task. Note: resolving the absorbed issue file `spec/issues/backlog-feature-duplicates-data-instead-of-referencing.md` is handled by finalize — no task here touches it.

## Batch 1 (no dependencies)

- [x] **Task 1.1: Issue and milestone frontmatter Zod schemas**
  - **Files**: `src/schemas/issue-frontmatter.ts` (new), `src/schemas/milestone-frontmatter.ts` (new), `src/schemas/index.ts` (add both exports)
  - **Action**: Create `IssueFrontmatterSchema` exactly per design Data Model: `.strict()` z.object with `type` (enum `issue`|`idea`, default `issue`), `backlog` (boolean, default `false`), `priority` (enum `high`|`medium`|`low`, optional), `milestone` (string matching `SLUG_RE` from `src/util/slug.ts`, optional), `order` (number, optional). Export `IssueFrontmatter = z.infer<...>` and `IssueFrontmatterPatch = Partial<z.input<...>>`. Create `MilestoneFrontmatterSchema`: `.strict()` with `name` (string, min 1, required), `target` (optional string matching `/^\d{4}-\d{2}-\d{2}$/`, refined to a real calendar date), `status` (enum `open`|`closed`, default `open`), plus inferred `MilestoneFrontmatter` type. Follow the `.strict()` pattern of `src/schemas/agent-definition.ts`. Use `.js` import extensions. Export both modules from `src/schemas/index.ts`.
  - **Verify**: `npx tsc --noEmit` passes. (Behavioral coverage lands with the consumers in Tasks 2.1 and 2.2 — the design test plan assigns schema edge cases to `tests/issue-frontmatter.test.ts` and `tests/milestones-store.test.ts`.)
  - **Done**: Both schemas compile, are strict (unknown keys produce `unrecognized_keys` Zod issues), apply the documented defaults, and are exported from the schemas barrel. Traces: spec "Issue frontmatter schema is Zod-validated and strict" and "Milestone store with Zod-validated frontmatter and CLI".

- [x] **Task 1.2: isArchivedChangeDir predicate applied in progress and release-pipeline**
  - **Files**: `src/util/archive-dirs.ts` (new), `tests/archive-dirs.test.ts` (new), `src/cli/commands/progress.ts` (filter archive listing), `src/release/release-pipeline.ts` (filter `listArchiveDirs()` at ~:161)
  - **Action**: Create `export function isArchivedChangeDir(name: string): boolean` implementing `/^\d{4}-\d{2}-\d{2}-/.test(name)` with the doc comment from design §6. In `progress.ts` (~:90) filter the archive-directory listing through the predicate so non-date-prefixed dirs (e.g. the future `spec/archive/backlog-legacy/`) are not rendered as completed changes. In `release-pipeline.ts` `listArchiveDirs()` apply the same filter so `release cut` never claims non-change archive dirs. Write `tests/archive-dirs.test.ts`: unit cases for the predicate (valid date prefix true; `backlog-legacy`, `done`, bare names false) plus fixture-based integration cases asserting both consumers skip a seeded `spec/archive/backlog-legacy/` directory.
  - **Verify**: `npx vitest run tests/archive-dirs.test.ts` green; `npx tsc --noEmit` passes; `npx vitest run tests/cli-status.test.ts tests/cli-release.test.ts` still green (no regression in existing consumers).
  - **Done**: A seeded `spec/archive/backlog-legacy/` directory appears in neither `metta progress` completed-change output nor release-pipeline unreleased-change detection. Mitigates design Risk 1.

- [x] **Task 1.3: Guard and mint hook tiers for milestone and backlog migrate (both copies)**
  - **Files**: `src/templates/hooks/metta-guard-bash.mjs`, `.claude/hooks/metta-guard-bash.mjs`, `src/templates/hooks/metta-session-mint.mjs`, `.claude/hooks/metta-session-mint.mjs`, `tests/cli-metta-guard-bash-integration.test.ts`
  - **Action**: In BOTH guard-bash copies (must stay byte-identical): add `['milestone', new Set(['list', 'show'])]` to `ALLOWED_TWO_WORD`; add `'migrate'` to the existing `backlog` entry in `BLOCKED_TWO_WORD`; add `['milestone', new Set(['create'])]` to `BLOCKED_TWO_WORD`. Do NOT add `milestone` to any bare-command allow list (bare `metta milestone` stays fail-closed). In BOTH session-mint copies: extend `SKILL_SCOPES['metta-backlog']` to `['backlog:add', 'backlog:done', 'backlog:promote', 'backlog:migrate', 'milestone:create']`. Extend `tests/cli-metta-guard-bash-integration.test.ts`: `metta milestone list`/`show` allowed without credential; `metta milestone create` and `metta backlog migrate` blocked without a session credential and allowed with the matching scope; bare `metta milestone` denied.
  - **Verify**: `npx vitest run tests/cli-metta-guard-bash-integration.test.ts tests/hooks-byte-identity.test.ts` green; `diff src/templates/hooks/metta-guard-bash.mjs .claude/hooks/metta-guard-bash.mjs && diff src/templates/hooks/metta-session-mint.mjs .claude/hooks/metta-session-mint.mjs` reports no differences.
  - **Done**: All four hook files updated in this single commit with template/live copies identical (design Risk 2); guard classifications match design ADR-5 and the spec's Tier-2 requirements for `milestone create` and `backlog migrate`.

## Batch 2 (depends on Batch 1)

- [x] **Task 2.1: Pure frontmatter round-trip module (issue-frontmatter.ts)**
  - **Depends on**: Task 1.1
  - **Files**: `src/issues/issue-frontmatter.ts` (new), `tests/issue-frontmatter.test.ts` (new)
  - **Action**: Implement the exact contract from design §Components 1: `IssueFrontmatterError` (filePath, message, cause), `splitFrontmatter(content)` returning `{ rawFrontmatter, body, eol }` where frontmatter exists iff content starts with `---\n`/`---\r\n` at offset 0 and `body` is a verbatim substring slice; `parseIssueFrontmatter(content, filePath)` = split + `YAML.parse` + strict Zod with defaults (`frontmatter: undefined` for legacy files); `applyFrontmatterPatch(content, patch, filePath)` returning complete new file content — existing block mutated via `YAML.parseDocument()`/`doc.set()`/`doc.toString()` per defined patch key (untouched keys keep value text, quoting, relative order; new keys append); no block → canonical-order block (`type`, `backlog`, `priority`, `milestone`, `order`; absent fields omitted) prepended above the original content unchanged; the resulting field set is schema-validated BEFORE returning. Note the yaml trailing-comment caveat (design Risk 7) in the module doc comment. Implement the full research edge-case table: CRLF files, empty frontmatter block, unclosed fence → `IssueFrontmatterError`, non-map YAML → error, mid-body `---` ignored, BOM → legacy path. Prior art for the Document API pattern: `src/config/config-writer.ts`. Tests cover every edge case plus: byte-preserved body across a patch, key order preservation, unknown-key rejection, enum error rendering (field/received/allowed via Zod), patch idempotency (second identical patch output === input), partial-frontmatter defaults (`backlog: true` only → `type: issue` default).
  - **Verify**: `npx vitest run tests/issue-frontmatter.test.ts` green; `npx tsc --noEmit` passes.
  - **Done**: All spec scenarios under "Issue frontmatter schema is Zod-validated and strict" and the round-trip guarantees of "Frontmatter writes round-trip the body and untouched fields" are demonstrated at module level; body preservation is structural (verbatim slice, never re-serialized).

- [x] **Task 2.2: MilestonesStore**
  - **Depends on**: Task 1.1
  - **Files**: `src/milestones/milestones-store.ts` (new), `tests/milestones-store.test.ts` (new)
  - **Action**: Create `MilestonesStore` class per design §Components 4: constructor takes `specDir`, wraps `StateStore` following the `IssuesStore` sibling pattern; `create(slug, { name, target?, description? })` writes `spec/milestones/<slug>.md` with schema-validated frontmatter (default `status: open`) and description body, throwing when the file already exists (never overwrites); `list()` returns `Milestone[]` (`[]` when the directory is absent); `show(slug)` throws a not-found error for missing slugs; `exists(slug)`. Reads parse frontmatter with `yaml` + `MilestoneFrontmatterSchema`; invalid `status`/`target` values produce clear validation errors naming field and allowed values (use `src/util/format-zod-error.ts`). Tests: create/list/show/exists round-trip, duplicate-create refusal leaving the existing file unmodified, invalid `status: shipped` rejection naming allowed values `open`/`closed`, malformed `target` rejection, absent-directory `list()` returning `[]`.
  - **Verify**: `npx vitest run tests/milestones-store.test.ts` green; `npx tsc --noEmit` passes.
  - **Done**: Spec scenarios "Milestone created with defaults", "Creating a duplicate milestone is refused", "Invalid milestone status is rejected" pass at store level.

## Batch 3 (depends on Batch 2)

- [x] **Task 3.1: IssuesStore frontmatter awareness and new methods**
  - **Depends on**: Task 2.1
  - **Files**: `src/issues/issues-store.ts`, `src/issues/issues-store.test.ts`
  - **Action**: Extend per design §Components 2, preserving every existing method signature (zero call-site changes in `fix-issue.ts`). Add `frontmatter?: IssueFrontmatter` to `Issue` (body/`description` is the content BELOW the frontmatter). Add the `IssueRecord` interface (slug, title, severity, captured — `**Captured**` falling back to `**Added**` for migrated ideas — type, backlog, priority?, milestone?, order?). Extend `create()` with an optional 5th param `frontmatter?: Pick<IssueFrontmatterPatch, 'priority' | 'milestone'>` written via `applyFrontmatterPatch`. Add `createIdea(title, description, fields?)` minting a `type: idea`, `backlog: true` frontmatter block above a standard `formatIssue` body (Captured/Status/Severity: minor). Add `updateFrontmatter(slug, patch)` returning `{ changed: boolean }` (`changed: false` when output === input). Widen `list()` to `IssueRecord[]`; add `listResolved()` over `spec/issues/resolved/`. Make `show()` frontmatter-aware (strips block, populates field). Extend `archive(slug, changeName?)` additively: copies raw content verbatim (frontmatter carried into resolved/ unchanged) and appends the `**Shipped-in**` stamp AFTER the body when `changeName` is given. Parsing pipeline: prefix check (files not starting with `---` skip YAML entirely) → `splitFrontmatter` → validate when present → legacy bold-label `parseIssue` on the body slice. Extend the existing co-located test file: frontmatter-aware list/show, `updateFrontmatter` idempotency and byte-preserved body, `createIdea` output shape, `listResolved`, archive frontmatter carry-through + Shipped-in stamp, and legacy frontmatter-less files parsing byte-unchanged with `type: issue`/`backlog: false` defaults (no frontmatter added as a side effect).
  - **Verify**: `npx vitest run src/issues/issues-store.test.ts` green; `npx tsc --noEmit` passes.
  - **Done**: Spec scenarios "Legacy issue lists and resolves unchanged", "Partial frontmatter is accepted with documented defaults", "Archive preserves frontmatter end to end", and "Targeted frontmatter update leaves everything else intact" pass at store level; existing consumers of `list()` rows (slug/title/severity) are unaffected.

- [x] **Task 3.2: Legacy backlog migration module (backlog-migrate.ts)**
  - **Depends on**: Task 2.1
  - **Files**: `src/backlog/backlog-migrate.ts` (new), `tests/backlog-migrate.test.ts` (new)
  - **Action**: Implement `migrateLegacyBacklog(specDir): Promise<MigrationResult>` with the `MigrationCollision`/`MigrationResult` shapes and the exact algorithm from design §Components 5: (1) enumerate `spec/backlog/*.md` and `spec/backlog/done/*.md`; both empty/absent → `{ nothingToDo: true }` with zero writes; (2) active items — collision against `spec/issues/<slug>.md` OR `spec/issues/resolved/<slug>.md` → record and skip; else write `spec/issues/<slug>.md` = frontmatter (`type: idea`, `backlog: true`, `priority` when the legacy `**Priority**` line parses to high/medium/low) + original content verbatim, then fs-rename the original to `spec/archive/backlog-legacy/<slug>.md`; (3) done items → `spec/issues/resolved/<slug>.md` with `type: idea` only, originals to `spec/archive/backlog-legacy/done/<slug>.md`; (4) remove `spec/backlog/done/` then `spec/backlog/` only when empty; (5) the legacy `**Priority**` parser is a private pure helper here (extracted from `backlog-store.ts` `parseItem` — copy it now; the store itself is deleted in Task 6.1). Never overwrite, never delete originals. Tests: active + done conversion with byte-verbatim bodies, priority carry (and non-carry for unparsable priority), collision reported with existing file byte-identical and legacy file retained, second-run no-op (`nothingToDo` or zero writes with collisions re-reported), legacy-dir archival preserving `done/`, dir removal only when emptied.
  - **Verify**: `npx vitest run tests/backlog-migrate.test.ts` green; `npx tsc --noEmit` passes.
  - **Done**: Spec scenarios "Active and done items convert with content preserved", "Second run is a no-op", and "Slug collision is reported, not overwritten" pass at module level.

## Batch 4 (depends on Batch 3)

- [x] **Task 4.1: Pure backlog view (filter + sort)**
  - **Depends on**: Task 3.1
  - **Files**: `src/backlog/backlog-view.ts` (new), `tests/backlog-view.test.ts` (new)
  - **Action**: Implement per design §Components 3: `BacklogEntry` interface; `toBacklogEntries(records: IssueRecord[])` selecting exactly the records with `backlog === true` (any type — legacy/frontmatter-less records are excluded structurally because `backlog` defaults false); `sortBacklogEntries(entries)` ordering by priority (`high` < `medium` < `low` < none) → `order` ascending within a priority bucket (undefined after defined) → captured date ascending → slug ascending (determinism tiebreak). Import `IssueRecord` from `../issues/issues-store.js`. Tests: the spec's C, B, A, D deterministic ordering scenario (A `low`/`order: 1`, B `high`/`order: 2`, C `high`/`order: 1`, D `backlog: true` only), exclusion of `backlog: false` and frontmatter-less records, defaults rendering (entry with only `backlog: true` yields a valid entry with no priority/order), captured-date and slug tiebreaks.
  - **Verify**: `npx vitest run tests/backlog-view.test.ts` green; `npx tsc --noEmit` passes.
  - **Done**: Spec scenarios "Mixed priorities and orders sort deterministically", "Missing optional fields render with defaults", and the exclusion half of "Legacy issue is excluded from backlog and milestone views" pass as pure-function tests.

- [x] **Task 4.2: Pure milestone rollup computation**
  - **Depends on**: Task 2.2, Task 3.1
  - **Files**: `src/milestones/milestone-rollup.ts` (new), `tests/milestone-rollup.test.ts` (new)
  - **Action**: Implement `MilestoneRollup` interface and `computeMilestoneRollups(milestones, openIssues, resolvedIssues)` per design §Components 4: pure single-pass bucketing, O(issues + milestones); `percent = Math.round(resolved/total*100)` with 0 when `total === 0`; issues referencing a milestone slug with no milestone file produce a warning string (never a failure); milestones with zero issues roll up 0/0/0 at 0%; rollups sorted open-first then slug ascending; frontmatter-less/milestone-less issues contribute to no bucket and no warning. Tests: bucketing across open + resolved records, percent rounding (1/3 → 33), zero-total guard, dangling-reference warning text naming the issue and unknown slug, closed-after-open sort, empty inputs.
  - **Verify**: `npx vitest run tests/milestone-rollup.test.ts` green; `npx tsc --noEmit` passes.
  - **Done**: Spec scenarios "Rollup counts resolved against open", "Empty milestone renders without failing", "Dangling milestone reference warns but succeeds" (computation half), and the milestone half of "Legacy issue is excluded from backlog and milestone views" pass as pure-function tests.

- [x] **Task 4.3: Skill template rework (metta-backlog, metta-issue, metta-fix-issues — template and deployed copies)**
  - **Depends on**: Task 1.3
  - **Files**: `src/templates/skills/metta-backlog/SKILL.md`, `.claude/skills/metta-backlog/SKILL.md`, `src/templates/skills/metta-issue/SKILL.md`, `.claude/skills/metta-issue/SKILL.md`, `src/templates/skills/metta-fix-issues/SKILL.md`, `.claude/skills/metta-fix-issues/SKILL.md`, `tests/cli-skills.test.ts` (update assertions if they pin current metta-backlog content)
  - **Action**: Per design §API Design "Skill template updates". `metta-backlog`: menu becomes `list | show | add | promote | done | migrate | milestone`; `add` first asks "existing issue or new idea?" — existing: pick a slug from `metta issues list --json` then `metta backlog add <slug> [--priority] [--order] [--milestone]`; new idea: collect title/description then `metta backlog add "<title>" --new ...`; `promote` surfaces the `/metta-fix-issues <slug>` handoff (rule text becomes "do not invoke the fix-issues flow yourself; surface the command"); `done` echoes `spec/issues/resolved/<slug>.md`; `migrate` runs `metta backlog migrate --json` and reports converted counts + collisions; `milestone` sub-menu drives `metta milestone create|list|show`; replace "CLI owns spec/backlog/" with "the backlog is a view over spec/issues/ frontmatter". `metta-issue` (additive): after severity, one optional AskUserQuestion for priority and milestone (options from `metta milestone list --json` when any exist; skip entirely when none), forwarded as `--priority`/`--milestone` in step 7. `metta-fix-issues` (touch points): step 1 notes `metta issues show --json` may carry a `frontmatter` field and `type: idea` entries are valid fix targets; step 11 notes resolution preserves frontmatter through `spec/issues/resolved/`; add a line that `metta backlog promote` hands off into this skill. Keep each template/deployed pair byte-identical.
  - **Verify**: `npx vitest run tests/cli-skills.test.ts` green (includes the template↔deployed identity assertion for metta-backlog); `for s in metta-backlog metta-issue metta-fix-issues; do diff src/templates/skills/$s/SKILL.md .claude/skills/$s/SKILL.md; done` reports no differences.
  - **Done**: All six SKILL.md files updated in one commit, pairs identical, content matches the design's skill-update spec verbatim; no skill references `spec/backlog/` as a store or `/metta-propose` as the promote target.

## Batch 5 (depends on Batch 4)

- [x] **Task 5.1: metta milestone command group and CliContext wiring**
  - **Depends on**: Task 2.2, Task 4.2
  - **Files**: `src/cli/commands/milestone.ts` (new), `src/cli/index.ts` (add `registerMilestoneCommand(program)`), `src/cli/helpers.ts` (add `milestonesStore: MilestonesStore` to `CliContext` and its construction — do NOT touch `backlogStore` yet; Task 6.1 removes it), `tests/cli-milestone.test.ts` (new)
  - **Action**: Implement `metta milestone create <slug> --name <name> [--target <date>] [--description <text>] [--on-branch <name>]` (existing file → exit 4 with `milestone_exists`; JSON `{ "slug", "created": true, "committed", "commit_sha" }`; auto-commit staging `spec/milestones`), `metta milestone list` (counts-only rollup JSON `{ "milestones": [...], "milestone_warnings": [...] }` with `milestone_warnings` present only when non-empty, mirrored to stderr in text mode), and `metta milestone show <slug>` (per-issue detail JSON per design; unknown slug → exit 4 not-found; zero-issue milestone → exit 0 with empty `issues`, 0/0 at 0%). Export the shared helper `loadMilestoneRollups(ctx): Promise<{ rollups, warnings } | null>` returning `null` when `spec/milestones/` has no milestone files (the omit-section signal for status/progress). Compose `MilestonesStore` + `issuesStore.list()`/`listResolved()` + `computeMilestoneRollups` at the CLI edge (no store-to-store dependency). Tests: all three subcommands' text + JSON shapes, duplicate-create exit 4, not-found show exit 4, dangling-reference warning surfaced with exit 0, `loadMilestoneRollups` null vs populated.
  - **Verify**: `npx vitest run tests/cli-milestone.test.ts` green; `npx tsc --noEmit` passes; `npx vitest run tests/cli-helpers.test.ts` green.
  - **Done**: Spec CLI scenarios for milestone create/list/show pass end to end; JSON shapes match design §API Design byte-for-byte in structure; `CliContext` exposes `milestonesStore`.

## Batch 6 (depends on Batch 5)

- [x] **Task 6.1: Backlog/issue CLI rework, roadmap repoint, BacklogStore deletion, barrel update**
  - **Depends on**: Task 3.1, Task 3.2, Task 4.1, Task 5.1
  - **Files**: `src/cli/commands/backlog.ts`, `src/cli/commands/issue.ts`, `src/cli/commands/roadmap.ts`, `src/cli/helpers.ts` (remove `backlogStore` field, import, and construction), `src/cli/promote-handoff.ts` (comment: roadmap is now the sole consumer), `src/index.ts` (barrel: remove `backlog/backlog-store.js`; add `backlog/backlog-view.js`, `backlog/backlog-migrate.js`, `issues/issue-frontmatter.js`, `milestones/milestones-store.js`, `milestones/milestone-rollup.js`, `util/archive-dirs.js`), DELETE `src/backlog/backlog-store.ts`, DELETE `tests/backlog-store.test.ts`, `tests/cli-issue-backlog.test.ts` (rework), `tests/cli-roadmap.test.ts` (fixtures repointed)
  - **Action**: Rewire `backlog.ts` per design §API Design: `add <slug-or-title>` — existing slug → `updateFrontmatter(slug, { backlog: true, ...opts })` with `changed: false` → "already backlogged" exit 0; `--new` → `issuesStore.createIdea` (positional is the title, `--description` defaults to title); unresolved slug without `--new` → exit 4 naming the slug and suggesting `--new`; options `--priority`/`--order`/`--milestone`/`--on-branch`; JSON `{ "slug", "status": "backlogged"|"already_backlogged"|"created", "type", "committed", "commit_sha" }`; auto-commit stages `spec/issues`. `list` — `issuesStore.list()` → `toBacklogEntries` → `sortBacklogEntries`, never reads `spec/backlog/`; JSON per design. `show <slug>` — `issuesStore.show`, exit 4 not-found. `promote <slug>` — `exists` check (exit 4), emits only the `/metta-fix-issues <slug>` handoff (text + JSON per design), zero writes; drop the `buildPromoteHandoff` import. `done <slug> [--change] [--on-branch]` — `issuesStore.archive(slug, changeName)` + `issuesStore.remove(slug)`; git stage targets `spec/issues` + `spec/issues/resolved`; JSON unchanged. New `migrate [--on-branch]` subcommand — `assertOnMainBranch`, `migrateLegacyBacklog(specDir)`, auto-commit staging `spec/backlog`, `spec/archive/backlog-legacy`, `spec/issues`; JSON per design (snake_case keys, exit 0 with or without collisions). `issue.ts`: add `--priority` (enum-validated pre-create, invalid → non-zero naming allowed values, no file created) and `--milestone` (slug-validated; dangling reference → stderr warning, issue still created) forwarded as `create()`'s 5th param; `issues list` prefixes `type: idea` rows with `[idea]` and adds the additive `IssueRecord` JSON fields. `roadmap.ts`: repoint `backlogStore.show/exists` at :52/:84/:154 to `issuesStore`; update dangling-entry remedy text `spec/backlog/<slug>.md` → `spec/issues/<slug>.md`. Rework `tests/cli-issue-backlog.test.ts` (add/list/show/promote/done/migrate behavior, `--new` failure path exit 4, idempotent re-add, `--milestone`/`--priority` on issue log including invalid-priority rejection and dangling-milestone warning) and repoint `tests/cli-roadmap.test.ts` fixtures to seed `spec/issues/` entries.
  - **Verify**: `npx vitest run tests/cli-issue-backlog.test.ts tests/cli-roadmap.test.ts` green; `npx tsc --noEmit` passes (compile-time proof that no `backlogStore` call site survives — design Risk 4); `grep -rn 'backlog-store' src tests` returns nothing.
  - **Done**: All spec scenarios under "Backlogging an issue mutates the existing issue file in place", "New ideas are minted as typed entries", "Backlog list is a sorted view over issue frontmatter", "Backlog promote hands off to fix-issues", "Backlog done resolves through the issue store archive", "Milestone and priority assignment via issue frontmatter" (CLI half), and the migrate CLI surface pass. `BacklogStore` is gone from the barrel (intended breaking export — design Risk 3, flag for release notes) with the replacement surface exported in the same commit.

- [x] **Task 6.2: Milestone rollups in status and progress surfaces**
  - **Depends on**: Task 5.1
  - **Files**: `src/cli/commands/status.ts`, `src/cli/commands/progress.ts`, `tests/cli-status.test.ts`
  - **Action**: Both commands call `loadMilestoneRollups(ctx)` from `src/cli/commands/milestone.js`; `null` → no section, no JSON keys (output byte-compatible with pre-change structure). `progress`: text `Milestones:` block after `Completed (N):` with one line per milestone (`v0-6  ▸ 1/3 resolved (33%)  target 2026-09-30`; closed marked `✓`, sorted after open); JSON gains optional top-level `milestones` (same element shape as `milestone list`) + optional `milestone_warnings` (conditional-key pattern as with `artifact_timings`). `status`: same optional top-level keys appended to whichever envelope is emitted (single `ChangeStatusJson`, `{ changes: [...] }`, or `{ changes: [], message }`) — never embedded per change; text prints the same `Milestones:` section after the change block(s). Extend `tests/cli-status.test.ts` both directions: key/section ABSENT with no milestone files (envelope structurally identical to pre-change), PRESENT with correct counts when milestones + assigned issues exist, across all three status envelopes and progress.
  - **Verify**: `npx vitest run tests/cli-status.test.ts` green; `npx tsc --noEmit` passes.
  - **Done**: Spec scenarios "Progress shows per-milestone counts" and "No milestones means no milestone section" pass; no-milestone JSON output is structurally identical to pre-change (design Risk 5).

## Batch 7 (depends on Batch 6)

- [x] **Task 7.1: Run the migration on this repo's own backlog data + full-suite gate**
  - **Depends on**: Task 6.1
  - **Files**: `spec/backlog/done/*.md` (8 files, relocated), `spec/issues/resolved/<8 slugs>.md` (new, `type: idea` frontmatter), `spec/archive/backlog-legacy/done/*.md` (new), `spec/backlog/` (removed)
  - **Action**: From the worktree root run the new CLI against this repo's own data: `BRANCH=$(git rev-parse --abbrev-ref HEAD) && npx tsx src/cli/index.ts backlog migrate --json --on-branch "$BRANCH"` (the `--on-branch` flag satisfies `assertOnMainBranch` inside the worktree; the command's auto-commit may create the commit itself — that is acceptable, do not commit a second time if it did). Then run it once more and confirm the second invocation reports `"nothing_to_do": true` with zero changes. Confirm the backlog view is empty: `npx tsx src/cli/index.ts backlog list --json` exits 0 with `"backlog": []`. Finally run the full-suite gate for the whole change.
  - **Verify**: First migrate run reports `converted: { active: 0, done: 8 }` with no collisions and `archived_to: "spec/archive/backlog-legacy"`; second run is a no-op exiting 0; all 8 files exist under `spec/issues/resolved/` with `type: idea` frontmatter and byte-verbatim legacy bodies, originals under `spec/archive/backlog-legacy/done/`, `spec/backlog/` gone; `git -C . status --porcelain` shows only the expected spec-tree moves; then `npx vitest run` fully green and `npx tsc --noEmit` clean.
  - **Done**: Spec scenario "Active and done items convert with content preserved" and "Second run is a no-op" demonstrated on real data (the metta repo's 8 archived items — the migration target named in the spec); the entire test suite and typecheck pass on the completed change.
