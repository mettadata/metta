# finalize-surfaces-failing-gate — User Stories

No user stories — internal/infrastructure change

**Justification:** Developer-ergonomics fix to the `metta finalize` CLI output. No end-user-facing behavior and no new capability; the existing `--json` mode already exposes this data.
