# Scenario Verification — when-starting-change-propose-quick-create-git-worktree

Quick workflow: `intent.md` Proposal items 1–9 are the requirements source.
Targeted test runs: `tests/git-worktree.test.ts` + `tests/cli-propose-worktree.test.ts` — 22/22 passed; `tests/schemas.test.ts -t worktree` — 9/9 passed.

## Verdict: PASS (9/9 items verified)

| # | Requirement | Implementation evidence | Test evidence | Status |
|---|-------------|-------------------------|---------------|--------|
| 1 | Worktree creation via `git worktree add <dir>/<name> -b metta/<name>`; main checkout never changes branch | `src/util/git-worktree.ts:80-90` (`setupChangeWorktree` runs `git worktree add … -b metta/<name>`); wired in `src/cli/commands/propose.ts:60` and `src/cli/commands/quick.ts:33` | `tests/git-worktree.test.ts` "creates a new branch and worktree; main checkout stays on main" (lines 42-57); `tests/cli-propose-worktree.test.ts` "propose creates the worktree… leaves main alone" (lines 34-66) | PASS |
| 2 | Location `.metta/worktrees/<change-name>`, gitignored, configurable via `git.worktree.dir` (default `.metta/worktrees`) | `src/util/git-worktree.ts:10` (`DEFAULT_WORKTREE_DIR`), `:72-73` (config dir resolution), `:86` + `:128-149` (`ensureGitignoreEntry`) | `tests/git-worktree.test.ts` "respects a custom git.worktree.dir" (89-97), "adds the worktree base dir to .gitignore on first worktree creation" (149-154), full `ensureGitignoreEntry` describe (157-210); `tests/cli-propose-worktree.test.ts:63-65` (gitignore assertion end-to-end) | PASS |
| 3 | `GitConfigSchema` gains `worktree: { enabled: true, dir: '.metta/worktrees' }`; top-level `git.enabled` guard unchanged | `src/schemas/project-config.ts:29-32` (strict `worktree` sub-object with defaults, `.default({})` keeps old configs valid); `git.enabled === false` still short-circuits all git work at `src/util/git-worktree.ts:62-64` | `tests/schemas.test.ts` describe "GitConfigSchema worktree sub-object" (1649-1699): defaults, backward compat, partial fill, custom dir, strict rejection, ProjectConfigSchema pass-through; `tests/git-worktree.test.ts` "does nothing when git is disabled entirely" (139-147) | PASS |
| 4 | Opt-out (`git.worktree.enabled: false`) and graceful fallback to `git checkout -b` on failure; fallback reported in output | `src/util/git-worktree.ts:68-69` (opt-out), `:91-102` (failure → branch cleanup → fallback), `:110-121` (`fallbackCheckout`, never throws); reported at `src/cli/commands/propose.ts:96-98` and `src/cli/commands/quick.ts:65-67` | `tests/git-worktree.test.ts` "falls back to in-place checkout when worktrees are disabled" (99-113), "falls back … when git worktree add fails" (115-130), "returns branch null … outside a git repo" (132-137); `tests/cli-propose-worktree.test.ts` "falls back to in-place checkout when git.worktree.enabled is false" (97-118), "never fails propose when worktree creation fails" (120-138) | PASS |
| 5 | Order of operations: branch + worktree first, then change state written inside the worktree; main checkout stays clean | `src/cli/commands/propose.ts:56-76` (`setupChangeWorktree` before `createChange`; `workCtx = createCliContext(gitSetup.worktree)` at line 63); same in `src/cli/commands/quick.ts:29-48`; `deriveChangeName` exposed for pre-state naming at `src/artifacts/artifact-store.ts:25-27` | `tests/cli-propose-worktree.test.ts:47-50` (`.metta.yaml` exists in worktree, absent in main checkout) plus lines 80-82 for quick | PASS |
| 6 | Worktree path persisted on `.metta.yaml` (Zod-validated); `--json` gains `worktree` field (absolute path or `null`) | `src/schemas/change-metadata.ts:91` (`worktree: z.string().optional()` inside strict schema); persisted via `src/artifacts/artifact-store.ts:37,73-75`; JSON field at `src/cli/commands/propose.ts:85` and `src/cli/commands/quick.ts:57` | `tests/cli-propose-worktree.test.ts:45,53-57` (JSON field + persisted `worktree:` in `.metta.yaml`), `:105` and `:134` (`worktree` is `null` in fallback); `tests/schemas.test.ts` describe "ChangeMetadataSchema worktree field" (1701-1731+) | PASS |
| 7 | Collision handling: existing branch → attach; existing worktree dir → reuse and report | `src/util/git-worktree.ts:75-77` (reuse), `:79-83` (attach without `-b` when branch exists), `:90` (mode `attached`); reuse reported at `src/cli/commands/propose.ts:94` and `src/cli/commands/quick.ts:63` | `tests/git-worktree.test.ts` "attaches to an existing branch instead of erroring" (67-76), "reuses an existing worktree directory for the same change" (78-87) | PASS |
| 8 | No stash / clean-tree precondition on the main checkout | `src/util/git-worktree.ts` contains no status/stash/clean check (documented at lines 54-55); no cleanliness code in propose/quick | `tests/git-worktree.test.ts` "works with a dirty main checkout" (59-65); `tests/cli-propose-worktree.test.ts` "propose works with a dirty main checkout" (85-95) | PASS |
| 9 | Ship compatibility: main checkout stays on main; ship/`MergeSafetyPipeline` untouched; only the worktree path is recorded for future cleanup | `git diff --stat main...HEAD -- src/` touches only `src/util/git-worktree.ts`, `src/cli/commands/propose.ts`, `src/cli/commands/quick.ts`, `src/schemas/project-config.ts`, `src/schemas/change-metadata.ts`, `src/artifacts/artifact-store.ts` — no finalize/ship/merge-safety source changes; worktree path recorded per item 6 | Main-stays-on-main asserted in `tests/cli-propose-worktree.test.ts:60,81`; existing finalize suite (`tests/cli-finalize.test.ts`) only adapted, not behaviorally changed | PASS |

## Notes

- Fallback branch-cleanup detail (`src/util/git-worktree.ts:94-100`): a failed `worktree add -b` deletes the half-created branch before falling back to `checkout -b`, preserving exact pre-call semantics — covered by the "git worktree add fails" tests.
- `.gitignore` write failure is best-effort by design (`src/util/git-worktree.ts:85-89`) and cannot fail change creation.
- Full-suite gates (npm test / lint / tsc) are delegated to the other verifier per task scope; only targeted tests were run here (all passed).

Verified by: scenario-compliance verifier, 2026-07-25.
