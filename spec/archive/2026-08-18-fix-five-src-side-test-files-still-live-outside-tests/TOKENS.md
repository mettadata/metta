# Token usage: fix-five-src-side-test-files-still-live-outside-tests

- **Change**: fix-five-src-side-test-files-still-live-outside-tests
- **Generated**: 2026-08-18

> Provenance per row: `hook (exact)` rows are harness-measured token counts
> recorded automatically by the token-recording hook; `prose (estimate)` rows
> are orchestrator-estimated figures and may under- or over-count actual
> provider usage. When both exist for the same task and agent, the exact hook
> figure is used.

## Total

**~0 tokens** across 0 record(s).

## Per artifact

_No token usage recorded._

## Per role

_No token usage recorded._

## Per model

_No token usage recorded._

## Cheap/pinned (non-inherit) vs inherit

- **Cheap/pinned (non-inherit)**: ~0 tokens
- **Inherit**: ~0 tokens

## Gaps

- **Hook health failure**: 0 automatic (hook-sourced) token records despite 3 completed artifact(s) — the SubagentStop token-recording hook likely failed to reach the CLI (stale globally-linked dist or hook-to-CLI path failure). Token data for this change was not captured automatically.
- `implementation` — run evidence with no token record; the recording hook missed this run
- `intent` — run evidence with no token record; the recording hook missed this run
- `verification` — run evidence with no token record; the recording hook missed this run
