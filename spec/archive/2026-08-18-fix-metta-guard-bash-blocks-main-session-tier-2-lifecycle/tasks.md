# Tasks for fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle

All paths are absolute under the change root
`/home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle/` (referred to as `{root}` in prose; every Files/Verify entry spells the full path).
Authoritative design: `/home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle/spec/changes/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle/design.md` — section names cited per task.

Shared constant (ADR-2/ADR-4): `GRACE_MS = 3_600_000` (60 min), duplicated verbatim in both hooks and both template mirrors, pinned by tests. `TTL_MS = 300_000` unchanged.

## Batch 1 (no dependencies — Tasks 1.1 and 1.2 touch disjoint files and run in parallel)

- [x] **Task 1.1: Mint hook — sessionId stamping, GRACE_MS cleanup horizon, atomic writes (+ mirror + mint-suite tests)**
  - **Files**:
    - `/home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle/.claude/hooks/metta-session-mint.mjs` (modify)
    - `/home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle/src/templates/hooks/metta-session-mint.mjs` (byte-identical copy of the above)
    - `/home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle/tests/metta-session-mint.test.ts` (extend)
  - **Action**: Implement design.md **Components §1 ("`.claude/hooks/metta-session-mint.mjs` (minting half)")** exactly:
    1. Add `const GRACE_MS = 3_600_000;` next to `TTL_MS` (currently line 36) with the comment: "MUST equal GRACE_MS in metta-guard-bash.mjs — the guard's re-prime horizon and this hook's sibling-cleanup threshold are one policy; seam tests pin the equality."
    2. Session stamping: the token object (currently lines 101–107) gains `sessionId: event.session_id ?? null`. Do NOT type-check it in this hook (the guard's strict `===` against a string makes non-string values inert, fail-closed).
    3. Cleanup horizon: in `cleanupSiblings` (currently line 59) change only the age comparison from `now - tok.mintedAt >= tok.ttlMs` to `now - tok.mintedAt >= tok.ttlMs + GRACE_MS`; the structural-malformation branches still mark a sibling expired. (Design rationale: R7 — cleanup must never delete a token the guard would still re-prime.)
    4. Atomic write: replace the direct `writeFileSync(tokenPath, ...)` (currently line 112) with write to `${tokenPath}.${randomUUID()}.tmp` (mode `0o600`) then `renameSync` onto `tokenPath`, inside the existing swallow-all try/catch. Add to `cleanupSiblings`: best-effort unlink of `*.tmp` files in the token dir whose mtime (via `statSync`) is older than `TTL_MS` (orphans from a crashed writer). The existing `.token` suffix filters already ignore in-flight `.tmp` files — do not change them.
    5. Header comment (lines 2–13): rewrite to describe the two-band freshness model, the `sessionId` binding, and that the guard is now the re-priming half during delegation windows while this hook remains the sole *minting* half (spec: "Hook headers describe the corrected freshness model").
    6. Mirror: copy the finished hook byte-identically onto `src/templates/hooks/metta-session-mint.mjs` (e.g. `cp`).
    7. Tests, per design.md **Components §4, `tests/metta-session-mint.test.ts` bullet** — extend (mirror `GRACE_MS = 3_600_000` as a test-file constant; extend the suite's `bashEvent` helper and `TokenFile` type to carry an optional `session_id`/`sessionId`):
       (a) the exact-keys payload test (currently lines 125–143): expected key set gains `sessionId`; assert `tok.sessionId` equals the `session_id` sent in the event, and add a case where the event omits `session_id` → `sessionId` is `null`;
       (b) the sibling-cleanup test (currently line 235): deepen the deleted-sibling seed to past `ttlMs + GRACE_MS` (e.g. `Date.now() - TTL_MS - GRACE_MS - 60_000`) and add a KEPT case for a sibling backdated between `ttlMs` and `ttlMs + GRACE_MS` (e.g. `Date.now() - TTL_MS - 60_000` — previously deleted, now kept);
       (c) atomic-write sanity: after a mint, no `*.tmp` residue remains in the token dir and the token file parses as JSON.
  - **Verify**: `cd /home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle && cmp .claude/hooks/metta-session-mint.mjs src/templates/hooks/metta-session-mint.mjs && npx vitest run tests/metta-session-mint.test.ts tests/hooks-byte-identity.test.ts` — all green, `cmp` silent.
  - **Done**: Mint hook stamps `sessionId`, keeps siblings alive through `ttlMs + GRACE_MS`, writes atomically (temp+rename, 0o600), cleans stale `.tmp` orphans, header describes the corrected lifecycle; template mirror byte-identical; extended mint suite green; hook still always exits 0 and swallows all errors.

- [x] **Task 1.2: Guard hook — two-band freshness, re-prime writer, audit staleness (+ mirror + guard-suite seed deepening)**
  - **Files**:
    - `/home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle/.claude/hooks/metta-guard-bash.mjs` (modify)
    - `/home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle/src/templates/hooks/metta-guard-bash.mjs` (byte-identical copy of the above)
    - `/home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle/tests/metta-guard-bash.test.ts` (fixture deepening only)
  - **Action**: Implement design.md **Components §2 ("`.claude/hooks/metta-guard-bash.mjs` (validating half)")** exactly:
    1. Imports: add `writeFileSync`, `renameSync`, `unlinkSync` to the existing `node:fs` import (currently line 18) and add `import { randomUUID } from 'node:crypto';`.
    2. Add `const GRACE_MS = 3_600_000;` in the constants region after `SKILL_HINT_MAP` (after currently line 97), with the mirror comment: "MUST equal GRACE_MS in metta-session-mint.mjs — one policy; seam tests pin the equality."
    3. `validateToken` (currently lines 297–304): **unchanged** — `sessionId` is deliberately not a structural requirement (old-format tokens still validate, work in the fresh band, are never re-primable).
    4. `readSessionTokens` (currently lines 314–329): annotate each returned token with the source filename it was read from — return `{ tok, file: name }` pairs. Adapt all consumers to `t.tok`. The re-prime write targets exactly the annotated path (read/write symmetry, risk R6).
    5. Tier-2 evaluation (offender predicate, currently lines 400–417): replace the single fresh filter with the two-band logic from the design's code sketch in Components §2 — fresh band `now - t.tok.mintedAt < t.tok.ttlMs`; re-primable band `sessionId !== null && t.tok.sessionId === sessionId && now - t.tok.mintedAt < t.tok.ttlMs + GRACE_MS` where `sessionId = typeof event.session_id === 'string' ? event.session_id : null`; `eligible = dedupe([...fresh, ...reprimable])`; empty eligible → `tier2Reason = 'credential-expired'` with `staleness_ms` (age of youngest structurally valid token) threaded to the audit entry; scope key derivation and `subcommands.includes(key)` filtering over `eligible` unchanged in semantics (`subcommand-not-in-scope` on miss); on acceptance, if NO in-scope token is fresh, call `reprimeToken(event.cwd, inScope[0], now)` and record reason `session-credential-reprimed`, else `session-credential-verified`; push `{ inv, reason, staleness_ms: now - inScope[0].tok.mintedAt }` onto `tier2Accepted`. Ordering invariance by construction: no branch consults anything the mint hook may or may not have done on this event (spec: "No new runtime ordering guarantee is assumed").
    6. New `reprimeToken(cwd, entry, now)`: builds `{ ...entry.tok, token: randomUUID(), mintedAt: now }`, strips annotation fields, validates the target filename shape (must be a plain `<name>.token` directory entry — no path separators) before writing, writes temp-file (`.tmp` suffix, mode `0o600`) + `renameSync` into the same `skill-session` dir `readSessionTokens` resolved from `event.cwd`, swallows ALL errors — authorize-then-write, write failure never revokes authorization (ADR-5).
    7. `appendAuditLog` (currently lines 333–352): add optional trailing `extra = {}` parameter spread into the entry. Acceptance loop (currently lines 420–427): log each `tier2Accepted` entry with its per-call reason and `staleness_ms`. The fork-caller Tier-2 path (currently lines 396–398) keeps reason `session-credential-verified` with `staleness_ms: null` (behavior-preserving; make its `tier2Accepted` push shape-consistent). `credential-expired` blocks log `staleness_ms` of the youngest structurally valid token considered.
    8. Header comment (lines 2–16): describe the two-band freshness model, the `sessionId` binding, both audit reasons, and the unchanged emergency bypass.
    9. Everything else unchanged (design "Explicitly unchanged surfaces"): Tier-1 fork identity, `SKILL_ENFORCED_SUBCOMMANDS`, background-Bash rejection, allow/block/bare lists, tokenizer/segmentation/quote handling, `--` fail-closed path, `missing-credential` and `subcommand-not-in-scope` reasons and messages, retired single-file credential unhonored, generic Tier-2 block message text (lines 479–484) untouched.
    10. Mirror: copy the finished hook byte-identically onto `src/templates/hooks/metta-guard-bash.mjs`.
    11. Tests, per design.md **Components §4, `tests/metta-guard-bash.test.ts` bullet** — fixture deepening ONLY (outcome-preserving; flag in the commit message that this is fixture deepening, not weakened coverage). Define `GRACE_MS = 3_600_000` in the test file. Deepen exactly three expiry seeds from `Date.now() - TTL_MS - 1000`/`- 5000` to `Date.now() - TTL_MS - GRACE_MS - 60_000`: the seed at currently line 832 (test "blocks with an expired token"), and the two seeds at currently lines 909 and 914 (test "blocks when every token file in the directory is expired"). **Do NOT touch** the seed at currently line 891 (inside the `subcommand-not-in-scope` test) — it is load-bearing legacy-token coverage. **Do NOT** add automatic `sessionId` stamping to this suite's `seedToken`; old-format seeds are deliberate. No expected outcome changes anywhere in the suite.
  - **Verify**: `cd /home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle && cmp .claude/hooks/metta-guard-bash.mjs src/templates/hooks/metta-guard-bash.mjs && npx vitest run tests/metta-guard-bash.test.ts tests/cli-metta-guard-bash-integration.test.ts tests/metta-guard-agent-dispatch.test.ts tests/metta-guard-edit.test.ts tests/hooks-byte-identity.test.ts` — all green with no expected-outcome modifications, `cmp` silent.
  - **Done**: Guard verdict is a pure function of (token file state, event fields, clock); re-prime band engages only on string `session_id` match within `ttlMs + GRACE_MS`; re-prime rewrites the authorizing token best-effort/atomically and logs `session-credential-reprimed`; `credential-expired` fires only when no fresh AND no re-primable token exists, with `staleness_ms`; missing/non-string `session_id` degrades to exact pre-fix behavior; template mirror byte-identical; every pre-existing guard test passes unmodified except the three deepened seeds.

## Batch 2 (depends on Batch 1 — Tasks 2.1 and 2.2 touch disjoint files and run in parallel)

- [x] **Task 2.1: Seam integration test suite `metta-guard-mint-seam.test.ts`**
  - **Depends on**: Task 1.1, Task 1.2
  - **Files**:
    - `/home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle/tests/metta-guard-mint-seam.test.ts` (create)
  - **Action**: Implement design.md **Components §4, first bullet ("New: `tests/metta-guard-mint-seam.test.ts`")**, following the harness idioms of the existing `tests/metta-guard-bash.test.ts` and `tests/metta-session-mint.test.ts` suites (spawnSync subprocess runs, synthetic PreToolUse JSON on stdin, temp cwds, audit-log JSONL parsing):
    1. Harness: `PAIRS` runs every case against both hook locations (source `.claude/hooks/` + deployed `src/templates/hooks/`), mint always paired with the same-tier guard copy; `bashEvent(command, cwd, extra)` **carries `session_id`** (both mint warm-up and guard events must send the same `session_id` for the re-prime band to engage); `runMint(slug, event)` / `runGuard(event)` via `spawnSync` with separate `procCwd` vs `eventCwd`; `makeMainAndWorktree()` topology builder for worktree-cwd cases; reuse the `readAuditEntries` pattern.
    2. Time control (ADR-3 — no clock override, ever): `backdate(cwd, deltaMs)` helper rewrites `TIMESTAMP_FIELDS = ['mintedAt']` in real mint-written token files (MUST NOT touch `sessionId`) plus `utimesSync` mtime backdating. Warm-up always uses the real mint hook, never hand-modeled tokens (risk R10). Constants mirrored in the test file: `RAW_TTL = 300_000`, `GRACE_MS = 3_600_000`, `DELEGATION = 900_000` (15 min, inside the band), `DEAD = RAW_TTL + GRACE_MS + 60_000`.
    3. Constant-drift pin (ADR-4): one test reads all four hook files — `/home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle/.claude/hooks/metta-session-mint.mjs`, `.../.claude/hooks/metta-guard-bash.mjs`, `.../src/templates/hooks/metta-session-mint.mjs`, `.../src/templates/hooks/metta-guard-bash.mjs` — and asserts each contains the identical `GRACE_MS = 3_600_000` literal.
    4. Case matrix (IDs per design):
       - **A1–A4** regression armor: fresh-token immediate Tier-2 call from main cwd (exit 0); same from a consistent worktree cwd (exit 0); split-cwd sentinel → `missing-credential` (documented asymmetry — any deeper defect it reveals is LOGGED as a separate issue, never patched here); event.cwd (not procCwd) drives token resolution.
       - **B1** bug pin: mint warm-up, `backdate(DELEGATION)`, in-scope Tier-2 call with matching `session_id` → exit 0, audit reason `session-credential-reprimed`, zero `credential-expired` entries, and the token file was rewritten (new `token` value, `mintedAt` ≈ now).
       - **B2** sliding-refresh sanity: fresh token + Bash call past 80% TTL → mint rotates on its sliding schedule exactly as pre-fix.
       - **C1/C2** ordering-invariance pair: same expired-but-re-primable state evaluated guard-first (no mint refresh landed) and mint-first — identical authorized verdict in both orderings (spec scenario "Authorization outcome is invariant under mint/guard hook ordering").
       - **C3** (optional, env-flag-gated e.g. `METTA_SEAM_STRESS=1`): concurrent mint+guard ×25 smoke, non-load-bearing.
       - **E1–E5** fail-closed armor: no mint at all → `missing-credential` with skill hint in stderr; `backdate(DEAD)` → exit 2 `credential-expired`; fresh token out of scope → `subcommand-not-in-scope`; well-formed retired single-file credential at `.metta/scratch/skill-session.token` ignored; hand-fabricated token/activity signal (orchestrator-authored content, no mint) fails closed (spec: "Fabricated activity evidence from command text does not authorize").
       - **E6** session binding: token minted under `session_id` A, guard event carries B, `backdate(DELEGATION)` → exit 2 `credential-expired`.
       - **E7** degradation: guard event carries no `session_id`, `backdate(DELEGATION)` → exit 2 (re-prime disabled, exact pre-fix behavior, fail-closed).
    5. Red-first adaptation: the hooks land before this suite in this change, so B1/C1 cannot be committed red against HEAD. The spec's "demonstrably capable of failing" requirement is discharged by (a) the E-series fail-closed complements plus the C1/C2 invariance pair in the same suite, and (b) a one-off pre-commit check: extract the pre-fix guard via `git show $(git merge-base HEAD main):.claude/hooks/metta-guard-bash.mjs` into the scratchpad, run the B1/C1 event fixtures against it, and record in the task/commit notes that both exit 2 with `credential-expired` (read-only git — no commits, no state changes). B1/C1 also assert the reason string `session-credential-reprimed`, which cannot be produced by the pre-fix guard.
  - **Verify**: `cd /home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle && npx vitest run tests/metta-guard-mint-seam.test.ts && npx vitest run tests/metta-guard-bash.test.ts tests/metta-session-mint.test.ts tests/hooks-byte-identity.test.ts` — all green; pre-fix B1/C1 red-check recorded.
  - **Done**: Seam suite covers A1–A4, B1–B2, C1–C2 (C3 optional/gated), E1–E7, and the ADR-4 constant pin; all deterministic via fixture backdating; suite green against fixed hooks and B1/C1 demonstrated red against the pre-fix guard.

- [x] **Task 2.2: Documentation sync — CLAUDE.md Tier-2 wording and guard-hooks internals doc**
  - **Depends on**: Task 1.1, Task 1.2 (describes shipped behavior; no file overlap with Task 2.1)
  - **Files**:
    - `/home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle/CLAUDE.md` (modify — workflow section Tier-2 bullet only; workflow-section edits are an allowed exception per that section's own rules)
    - `/home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle/docs/internals/guard-hooks.md` (modify)
  - **Action**: Implement design.md **Components §5 ("Documentation")** minus the hook headers (owned by Tasks 1.1/1.2):
    1. `CLAUDE.md`, "Metta Workflow → How to work", the **Tier 2 (session-tier)** bullet: it currently cites the retired single-file `.metta/scratch/skill-session.token`. Rewrite to describe: per-skill credentials at `.metta/scratch/skill-session/<slug>.token`, minted at skill invocation, slide-rotated on active use (80% TTL), session-bound re-prime by the guard across delegation windows, bounded effective lifetime of `TTL + GRACE` after the last mint or re-prime, and that the credential value never appears in any skill file. Touch nothing else in CLAUDE.md.
    2. `docs/internals/guard-hooks.md`: update the Tier-2 credential lifecycle description to the two-band model (fresh band `age < ttlMs` → `session-credential-verified`; re-primable band `sessionId === event.session_id && age < ttlMs + GRACE_MS` → `session-credential-reprimed` with token rewrite); document the new audit surface (`session-credential-reprimed` reason, `staleness_ms` field on session-tier acceptances and `credential-expired` blocks); state the corrected, narrowed meaning of `credential-expired` (genuinely dead only: ≥ `TTL + GRACE` stale or session-mismatched, with no fresh token — same reason string, stricter semantic, per risk R9); keep the emergency-bypass and tier descriptions accurate. No text may present the retired bare minted-timestamp-vs-raw-TTL racing refresh, single-tier, or inline-token model as current (spec: "Generated workflow guidance matches the documented model").
  - **Verify**: `cd /home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle && grep -n "skill-session/<slug>.token" CLAUDE.md && ! grep -n "at \`.metta/scratch/skill-session.token\`" CLAUDE.md && grep -n "session-credential-reprimed" docs/internals/guard-hooks.md && grep -n "staleness_ms" docs/internals/guard-hooks.md` — first, third, fourth greps match; second finds no remaining claim that the single-file path is the live credential (mentioning it as retired/unhonored is fine).
  - **Done**: CLAUDE.md Tier-2 bullet describes the per-slug two-band credential lifecycle with no retired single-file reference as current mechanism; guard-hooks.md documents the two-band model, both audit reasons, `staleness_ms`, and the narrowed `credential-expired` semantic.

## Batch 3 (depends on Batch 2)

- [x] **Task 3.1: Full gate run**
  - **Depends on**: Task 1.1, Task 1.2, Task 2.1, Task 2.2
  - **Files**: none (verification only; fix-forward only if a gate fails, scoped to files owned by this change)
  - **Action**: Run every project gate from the change root and confirm the whole change is green together: full test suite (including the untouched suites the design requires stay green: `tests/cli-metta-guard-bash-integration.test.ts`, `tests/hooks-byte-identity.test.ts`, `tests/metta-guard-agent-dispatch.test.ts`, `tests/metta-guard-edit.test.ts`), type check, lint, build (which exercises the template→`dist/` copy step for both hooks). If any gate fails, fix within this change's file set and re-run all gates.
  - **Verify**: `cd /home/utx0/Code/metta/.metta/worktrees/fix-metta-guard-bash-blocks-main-session-tier-2-lifecycle && npm test && npx tsc --noEmit && npm run lint && npm run build` — all four exit 0. Then `cmp .claude/hooks/metta-session-mint.mjs src/templates/hooks/metta-session-mint.mjs && cmp .claude/hooks/metta-guard-bash.mjs src/templates/hooks/metta-guard-bash.mjs` — silent.
  - **Done**: Full suite, typecheck, lint, and build all pass from the change root; hook/template byte-identity holds; no pre-existing test's expected outcome was modified anywhere in the change except the three sanctioned seed deepenings from Task 1.2.
