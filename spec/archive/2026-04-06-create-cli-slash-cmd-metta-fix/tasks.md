# Tasks: metta fix-gap — Automated Gap Resolution Pipeline

**Change:** create-cli-slash-cmd-metta-fix  
**Date:** 2026-04-06  
**Status:** Draft

---

## Batch 1 (no dependencies — pure additions, no file overlap)

- [ ] **Task 1.1: Implement `src/cli/commands/fix-gap.ts`**
  - **Files**: `src/cli/commands/fix-gap.ts`
  - **Action**: Create the CLI command module. Export `registerFixGapCommand(program: Command): void`. Export `parseSeverity(rawContent: string): 'critical' | 'medium' | 'low'` as a pure function using word-boundary regex (`\b(p1|high|critical|bug)\b` → critical, `\b(p2|medium)\b` → medium, else low). Export `SeverityTier`, `GapWithSeverity`, and `sortBySeverity(gaps: GapWithSeverity[]): GapWithSeverity[]` (stable sort, weight critical=0/medium=1/low=2, no mutation). Register a `fix-gap` command on `program` with: positional `[gap-name]`, option `--all`, option `--remove-gap <slug>`. Implement four action branches: (a) `--remove-gap <slug>`: call `GapsStore.remove(slug)`, run `git add spec/gaps/<slug>.md && git commit -m "chore(<slug>): gap resolved"`, exit 0; (b) `<gap-name>` provided: check `GapsStore.exists`, exit 4 with error message if missing, else call `GapsStore.show` for title/claim, print start message (or emit stub JSON if `--json`), print pipeline delegation message pointing to `metta-fix-gap` skill; (c) `--all`: call `GapsStore.list()`, exit 0 with "No gaps found." if empty, else read raw content for each slug via `node:fs/promises`, compute severity per `parseSeverity`, sort via `sortBySeverity`, print sorted gap list with severity labels, print pipeline delegation message; (d) neither argument nor flag: Commander shows help. Follow `propose.ts` patterns for imports (`commander`, `node:child_process`, `promisify`, `createCliContext`, `outputJson`). Use `node:fs/promises` `readFile` for raw gap content reads (not `GapsStore.show`, to preserve ephemeral-severity invariant per ADR-2).
  - **Verify**: `npx tsc --noEmit` passes. Running `metta fix-gap --help` prints usage. Running `metta fix-gap nonexistent-slug` prints `Gap 'nonexistent-slug' not found` and exits 4.
  - **Done**: Module compiles, all four action branches are present, `parseSeverity` and `sortBySeverity` are exported, no runtime errors on basic invocations.

- [ ] **Task 1.2: Create skill file `.claude/skills/metta-fix-gap/SKILL.md`**
  - **Files**: `.claude/skills/metta-fix-gap/SKILL.md`
  - **Action**: Create the skill orchestration file following the structure of `.claude/skills/metta-auto/SKILL.md`. Include YAML front matter with `name: metta:fix-gap`, `description: Resolve a reconciliation gap through the full metta change lifecycle`, `argument-hint: "<gap-slug>"`, `allowed-tools: [Read, Write, Edit, Bash, Grep, Glob, Agent]`. Include the IMPORTANT note about using metta agent types (not gsd-executor). Write the following sections: (1) **No-argument invocation**: run `metta gaps list --json`, read raw content per slug to compute severity using keyword rules from the spec (case-insensitive; P1/High/Critical/Bug → critical; P2/Medium → medium; else low), display ranked table (Severity, Slug, Title), ask user which gap(s) to fix using AskUserQuestion before running any pipeline. (2) **Single gap pipeline** (steps numbered): step 1: `metta fix-gap <slug> --json` to validate gap exists (exit on error); step 2: `metta propose --from-gap <slug> --discovery batch --json` — extract `change` name from JSON; step 3: per-artifact loop (intent, spec, research, design, tasks) — one subagent per artifact with `isolation: "worktree"`, then `metta complete <artifact> --json --change <change>`; step 4: read `spec/changes/<change>/tasks.md`, parse batches, spawn parallel `metta-executor` subagents per task in each batch (same pattern as `metta-auto` step 4); step 5: spawn 3 `metta-reviewer` subagents in parallel (correctness, security, quality); merge into `review.md`; review-fix loop up to 3 iterations; step 6: spawn 3 `metta-verifier` subagents in parallel (npm test, tsc+lint, spec scenario coverage); merge into `summary.md`; verify-fix loop if gates fail; step 7: `metta finalize --json --change <change>`; step 8: `git checkout main && git merge metta/<change> --no-ff -m "chore: merge <change>"`; step 9: `metta fix-gap --remove-gap <slug>` — removes gap file and commits. (3) **On phase failure**: halt pipeline for that gap, do NOT call `--remove-gap`, log which phase failed. (4) **`--all` mode**: iterate severity-sorted list (critical first), call single-gap pipeline per gap, log `[N/M] <slug>: resolved` or `[N/M] <slug>: failed at phase: <phase>` after each, print final summary `Resolved: X / Failed: Y / Total: Z`, continue on failure (do not abort batch). (5) **Rules**: discovery mode is always batch for fix-gap (no AskUserQuestion during the pipeline, only before it in no-arg mode); every subagent must write files and commit; `--remove-gap` is only called after successful ship.
  - **Verify**: File exists at `.claude/skills/metta-fix-gap/SKILL.md`. Front matter is valid YAML. All nine pipeline steps are present and numbered. The `--all` summary format matches the spec exactly.
  - **Done**: Skill file created and readable. Structure mirrors `metta-auto/SKILL.md`. No pipeline logic is duplicated from the CLI command.

- [ ] **Task 1.3: Create slash command `.claude/commands/fix-gap.md`**
  - **Files**: `.claude/commands/fix-gap.md`
  - **Action**: Create the directory `.claude/commands/` if it does not exist. Create `fix-gap.md` as a thin wrapper slash command. The file must describe two invocation modes: (a) **No-argument**: invoke the `metta-fix-gap` skill with no slug; the skill will run `metta gaps list --json`, display the ranked severity table, and prompt the user before starting any pipeline; (b) **Slug argument** (`/metta:fix-gap <slug>`): invoke the `metta-fix-gap` skill with the slug; the skill calls `metta fix-gap <slug> --json` and drives the pipeline. Include a usage section explaining: the `--all` option (pass as argument to the skill), that the slash command delegates entirely to `metta fix-gap` CLI invocations (no fix logic in this file), and that `--json` output is streamed back into the conversation. Do NOT implement any severity classification, gap reading, or pipeline steps in this file.
  - **Verify**: File exists at `/home/utx0/Code/metta/.claude/commands/fix-gap.md`. File contains instructions for both no-argument and slug-argument modes. File does not contain severity keyword logic or pipeline phase references.
  - **Done**: Slash command file created. Invocation modes documented. File is thin — delegates entirely to the skill.

---

## Batch 2 (depends on Task 1.1 — modifies `src/cli/index.ts` which imports the new module)

- [ ] **Task 2.1: Register `fix-gap` command in `src/cli/index.ts`**
  - **Files**: `src/cli/index.ts`
  - **Action**: Read `src/cli/index.ts` first. Add one import line and one registration call. Insert the import alphabetically among the existing register imports: `import { registerFixGapCommand } from './commands/fix-gap.js'`. Insert the registration call after `registerGapsCommand(program)` and before `registerReconcileCommand(program)`: `registerFixGapCommand(program)`. No other changes to the file.
  - **Verify**: `npx tsc --noEmit` passes. `metta fix-gap --help` prints usage without error. `metta --help` lists `fix-gap` in the command list.
  - **Done**: Import line added, registration call added, file compiles cleanly, `metta fix-gap` is reachable from the CLI.

---

## Batch 3 (depends on Tasks 1.1 and 2.1 — tests import the compiled module)

- [ ] **Task 3.1: Write unit tests `src/cli/commands/fix-gap.test.ts`**
  - **Files**: `src/cli/commands/fix-gap.test.ts`
  - **Action**: Create the Vitest test file using `describe`/`it`/`expect`. Use temp-dir isolation (`import { mkdtemp, writeFile, mkdir } from 'node:fs/promises'`) to create a real `spec/gaps/` subtree; do not invoke subprocesses or AI agents. Implement the following test groups: (1) `parseSeverity` — critical keywords (`P1`, `High`, `Critical`, `Bug`, `**Severity**: High`); medium keywords (`P2`, `Medium`); low default (no matching keyword, empty string); precedence (`High` + `Medium` in same content → critical); case-insensitivity (`HIGH`, `BUG`, `medium`); word boundary (`p10` must not match `p1`). (2) `sortBySeverity` — mixed tiers sort to critical → medium → low; stable sort preserves within-tier input order; empty input returns empty array; does not mutate the input. (3) `--remove-gap` integration path — mock `GapsStore.remove` and `execFile`/`execAsync`, assert `remove` is called with correct slug, assert git commit message is `chore(<slug>): gap resolved`, assert exit code 0. (4) Single gap not found — when `GapsStore.exists` returns false, assert error output contains `Gap '<slug>' not found` and process exits 4; assert no `GapsStore.show` call occurs. (5) `--all` with no gaps — when `GapsStore.list()` returns `[]`, assert output contains `No gaps found.` and exits 0. (6) `--all` severity ordering — three temp gap files with `P3`, `High`, and `Medium` content; assert the slugs are returned in critical → medium → low order after `sortBySeverity`. (7) `--json` output schema — success output has keys `gap`, `change`, `phases`, `status: "resolved"` and no `failed_phase` key; failure output has `failed_phase` present and `status: "failed"`; `--all` output has `gaps` array and `summary` object; not-found with `--json` emits `{ error: { code: 4, type: "not_found", message: ... } }`.
  - **Verify**: `npm test` passes all new test cases. No test uses `execFile` against real binaries. Coverage covers `parseSeverity`, `sortBySeverity`, not-found path, and `--all` empty path.
  - **Done**: All test groups pass. No existing tests broken. File follows project test conventions (temp-dir isolation, `describe`/`it`/`expect`, no CommonJS).
