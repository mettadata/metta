# Tasks: fix-intent-time-workflow-auto-downscale-misfires-file-count

Decomposition per `design.md` (ADR-1..4, test matrix T-S/T-I/T-R/T-C/T-D/T-H, risks R1–R7).
Batches are sequential; tasks within a batch touch disjoint files and may run in parallel.
One commit per task.

Cross-cutting constraints from design Dependencies:
- Fixes 2+3 edit the same ~60 lines of `complete.ts` — implemented as ONE task (Task 2.1), never split.
- `DownscaleDecisionSchema` (Task 1.2) and `askYesNoDetailed` (Task 1.3) MUST land before Task 2.1: writing the record against the un-extended strict schema is silently swallowed by the advisory try/catch and loses the workflow rewrite too (Risk R1).
- Task 1.3 is the one sanctioned scope addition beyond intent.md's Impact list (ADR-4 / Risk R2) — flagged here so verify/review treats it as designed, not drift.

## Batch 1 (no dependencies — 3 parallel tasks, disjoint files)

- [x] **Task 1.1: Null-weight zero-file intent scoring (Fix 1, ADR-1)**
  - **Files**: `src/complexity/scorer.ts`, `tests/complexity-scorer.test.ts`
  - **Action**: Per ADR-1 and Components §1: change `scoreFromIntentImpact` to return `null` whenever `parseFileCountFromSection(intentMd, '## Impact')` yields 0 — merging the missing-heading and present-but-empty cases. Drop the `hasH2Heading` pre-check from this function only (`parseFileCountFromSection` already returns 0 for an absent heading); keep `hasH2Heading` exported — `scoreFromSummaryFiles` still uses it. Leave `tierFromFileCount` (including `0 → trivial`), `buildScore`, `scoreFromSummaryFiles`, and `isScorePresent` untouched. Rewrite doc comments (required deliverable, Risk R7): `scoreFromIntentImpact` — new contract: `null` for any 0-file parse, zero files at intent time is absence of evidence not evidence of triviality, contrast explicitly with `scoreFromSummaryFiles`; `scoreFromSummaryFiles` — add one line: 0 files here IS a real `trivial` signal (intentional asymmetry); `buildScore` — note it maps raw counts including 0 through canonical thresholds and that zero-file no-signal gating is the intent scorer's responsibility. Tests: invert T-S1 (`tests/complexity-scorer.test.ts:68–83`, "file_count 0 when heading exists but section is empty") to `expect(scoreFromIntentImpact(md)).toBeNull()` and rename it to state the no-signal contract; keep the missing-heading → `null` test (lines 85–96) as a distinct case documenting the collapsed distinction; add T-S2 (1-file intent → `file_count: 1`, `recommended_workflow: 'trivial'` — proves no-signal is strictly `count === 0`); add T-S3 (`scoreFromSummaryFiles` with `## Files` present but empty → `file_count: 0`, `recommended_workflow: 'trivial'`). All existing `tierFromFileCount`, `scoreFromSummaryFiles`, `isScorePresent` tests remain unchanged and green.
  - **Verify**: `cd /home/utx0/Code/metta/.metta/worktrees/fix-intent-time-workflow-auto-downscale-misfires-file-count && npx vitest run tests/complexity-scorer.test.ts && npm run lint`
  - **Done**: `scoreFromIntentImpact` returns `null` on 0 parsed files; T-S1 inverted, T-S2/T-S3 added and passing; missing-heading test retained; doc comments no longer promise zero-maps-to-trivial at intent time; no other scorer function changed; scorer test file fully green; `tsc --noEmit` clean.

- [x] **Task 1.2: DownscaleDecisionSchema + optional metadata field (Fix 3 schema half, ADR-3)**
  - **Files**: `src/schemas/change-metadata.ts`, `tests/schemas.test.ts`
  - **Action**: Per ADR-3 and Data Model: add a standalone strict schema adjacent to `EscalationSchema` (currently `change-metadata.ts:48`):
    `DownscaleDecisionSchema = z.object({ from_tier: z.enum(['trivial','quick','standard','full']), to_tier: z.enum([...same]), justification: z.string().min(1), timestamp: z.string().datetime() }).strict()` plus `export type DownscaleDecision = z.infer<...>`. Mount on `ChangeMetadataSchema` directly next to `escalation` (line ~109): `downscale_decision: DownscaleDecisionSchema.optional()`. Inline tier enums (no shared-enum extraction — ADR-3 rejected option); single object, not array. Exports flow through the existing `export * from './change-metadata.js'` in `src/schemas/index.ts` — no barrel edit needed. Do NOT touch `complete.ts` in this task. Tests in `tests/schemas.test.ts`: T-D1 populated `downscale_decision` (`standard`→`quick`, non-empty justification, ISO timestamp) parses and round-trips; T-D2 legacy document omitting the field parses with field absent; T-D3 `downscale_decision` present with `escalation` absent parses (independent parallel fields); T-D4 rejections — empty `justification`, invalid tier value, non-datetime `timestamp`, unknown key inside the record (`.strict()`).
  - **Verify**: `cd /home/utx0/Code/metta/.metta/worktrees/fix-intent-time-workflow-auto-downscale-misfires-file-count && npx vitest run tests/schemas.test.ts && npm run lint`
  - **Done**: `DownscaleDecisionSchema`/`DownscaleDecision` exported; `ChangeMetadataSchema` carries the optional field; `escalation` untouched; T-D1–T-D4 passing; legacy-shape parse proven; `tsc --noEmit` clean.

- [x] **Task 1.3: askYesNoDetailed helper (ADR-4 — sanctioned scope addition)**
  - **Files**: `src/cli/helpers.ts`, `tests/cli-helpers.test.ts`
  - **Action**: Per ADR-4 and Components §4: add `export async function askYesNoDetailed(question: string, opts?: { defaultYes?: boolean; jsonMode?: boolean }): Promise<{ value: boolean; viaDefault: boolean }>` carrying the current `askYesNo` body (readline mechanics and prompt-suffix rendering byte-identical, so the suite's `vi.mock('node:readline')` harness keeps working). `viaDefault` mapping: non-TTY/jsonMode early return → `{ value: defaultYes, viaDefault: true }`; empty answer → `{ value: defaultYes, viaDefault: true }`; explicit y/Y → `{ value: true, viaDefault: false }`; explicit n/N → `{ value: false, viaDefault: false }`; unrecognized answer → `{ value: defaultYes, viaDefault: true }`. Rewrite `askYesNo` as a thin wrapper returning `(await askYesNoDetailed(question, opts)).value` — exact signature and behavior preserved for all other call sites (upscale ×2, release-cut, install). Do NOT change any call site in this task. Tests (T-H1) in `tests/cli-helpers.test.ts`: `viaDefault` true for empty answer, false for explicit `y`, false for explicit `n`, true for garbage input, true with `value === defaultYes` for the non-TTY/jsonMode early return; plus `askYesNo` wrapper still returns the bare boolean.
  - **Verify**: `cd /home/utx0/Code/metta/.metta/worktrees/fix-intent-time-workflow-auto-downscale-misfires-file-count && npx vitest run tests/cli-helpers.test.ts && npm run lint`
  - **Done**: `askYesNoDetailed` exported with the specified `viaDefault` semantics; `askYesNo` delegates and is behavior-identical; T-H1 cases passing; no call-site edits; existing cli-helpers tests green; `tsc --noEmit` clean.

## Batch 2 (depends on Tasks 1.2 and 1.3; Task 1.1 must also be merged so the integration suite sees the null-return scorer)

- [x] **Task 2.1: Fail-closed downscale + decision record in complete.ts (Fixes 2+3, ADR-2/ADR-3/ADR-4 — single pass)**
  - **Files**: `src/cli/commands/complete.ts`, `tests/cli-complete.test.ts` (+ minimal harness tweak inside that test file per T-C2)
  - **Action**: Per ADR-2 (decision matrix in API Design is authoritative) and ADR-3/ADR-4, restructure ONLY the intent-time downscale branch (~lines 273–358):
    1. Compute `const nonInteractive = !process.stdin.isTTY || json` (predicate identical to `askYesNo`'s internal check — Risk R6) and track `takeYes` / `acceptCause` per Components §2. Ordering is hard: `autoAccept` check FIRST (unchanged — sole sanctioned non-interactive Yes, cause `auto_accept_recommendation`), then `else if (nonInteractive) { takeYes = false }` fail-closed, then the interactive `askYesNoDetailed` call with options unchanged (`defaultYes: currentMetadata.workflow_locked !== true, jsonMode: json`); on `value === true`, `acceptCause = viaDefault ? 'interactive default-Yes' : 'interactive explicit yes'`. Replace the misleading comment block near line 292 (it documents the bug) with the fail-closed rationale citing `AutoDownscalePromptAtIntent`.
    2. Accept path: fold the record into the EXISTING `updateChange` call (~line 331) — one atomic validated write alongside `workflow: recommendedTier` and `artifacts: rebuilt`: `downscale_decision: { from_tier: currentWorkflow, to_tier: recommendedTier, justification: `collapsed ${currentWorkflow} -> ${recommendedTier}: ${acceptCause}`, timestamp: new Date().toISOString() }`. No second `updateChange` call (ADR-3 rejected — creates a record-less-collapse window).
    3. Decline path: code otherwise unchanged; extend the escalation justification ternary with `workflow_locked` keeping precedence (Risk R4): `workflow_locked === true` → `kept ${currentWorkflow}: workflow_locked`; else `nonInteractive` → `kept ${currentWorkflow}: non-interactive fail-closed`; else `kept ${currentWorkflow}: declined downscale`. Never write `downscale_decision` on any No path.
    Untouched: upscale branch, full-tier cap, summary-time recompute (~line 444), escalation write shape, `renderBanner` advisory emission, artifact-map rebuild logic, the advisory `try/catch`, prompt/banner strings. Optionally add the reader-aid comment near line 251 that a `null` score also covers the greenfield 0-file case.
    Tests in `tests/cli-complete.test.ts` per the design test matrix (line refs are from research — re-verify at implementation time):
    - Invert T-I1 (~:290 non-TTY unlocked): workflow stays `standard`; `stories`/`spec` artifacts kept; stderr contains `Advisory:` + `downscale recommended` and does NOT contain `collapse workflow`; escalation `{ from_tier: 'trivial', to_tier: 'standard', justification ~ 'non-interactive fail-closed' }`; `downscale_decision` undefined.
    - Invert T-I2 (~:325 json mode): workflow stays `standard`; stdout still `JSON.parse`-able; stderr contains `Advisory:`; escalation defined; `downscale_decision` undefined.
    - Invert T-I3 (~:347 three-file standard): workflow stays `standard`; artifacts kept; escalation `{ from_tier: 'quick', to_tier: 'standard' }`.
    - Invert T-I4 (~:836): workflow stays `standard`; stderr CONTAINS the downscale Advisory; keep `not.toContain('upscale recommended')`.
    - Regression guards kept green: T-R3 (~:376 locked non-TTY — passes only with `workflow_locked` precedence; add `downscale_decision` undefined), T-R5 (~:458 interactive decline `n` — `[Y/n]` suffix, `declined downscale` escalation; add `downscale_decision` undefined), T-R6 (~:519,543 quick-tier never enters branch), T-R7 (~:707–834 upscale suite untouched).
    - Accept-path record assertions (Risk R1 — MANDATORY on every accept test, asserted via a single re-read of `.metta.yaml` together with the `workflow` assertion; "nothing crashed" proves nothing under the advisory try/catch): T-R1 (~:250 auto_accept non-TTY) — add `downscale_decision` defined with `from_tier: 'standard'`, `to_tier: 'trivial'`, justification containing `auto_accept_recommendation`, parseable ISO timestamp, `escalation` undefined (this test alone lands the `auto_accept_opt_in_still_collapses_non_interactively` record assertion — not duplicated elsewhere); T-R2 (~:581 auto_accept via propose fixture) — same record assertions; T-R4 (~:492 interactive empty-answer default-Yes) — add record present with justification containing `interactive default-Yes`.
    - New tests: T-C1 greenfield integration — intent whose `## Impact` has prose/inline-code but no file tokens → after `complete intent`, `.metta.yaml` has no `complexity_score` key, `workflow` unchanged, stderr has no downscale prompt and no `Advisory:` line. T-C2 in-process TTY + `--json` fail-closed — reuse `runCompleteInteractive` mechanics (forced `isTTY = true` via `Object.defineProperty`) with `['--json', 'complete', 'intent', …]` and NO queued readline answer; requires the small harness tweak (helper accepts pre-args or a sibling helper — `--json` is already registered on the in-process program); assert `ttyPrompt.questions` stays empty, workflow kept, escalation written, `downscale_decision` undefined. T-C3 interactive explicit yes (`runCompleteInteractive`, answer `'y'`) — workflow collapses; record present with justification containing `interactive explicit yes`.
    Fixture note: existing `oneFileIntent`/`threeFileIntent` fixtures still score under zero-is-no-signal — no fixture changes; `metta instructions` banner tests pre-write `complexity_score` and are unaffected.
  - **Verify**: `cd /home/utx0/Code/metta/.metta/worktrees/fix-intent-time-workflow-auto-downscale-misfires-file-count && npx vitest run tests/cli-complete.test.ts tests/cli-helpers.test.ts tests/complexity-scorer.test.ts tests/schemas.test.ts && npm run lint`
  - **Done**: Decision matrix holds exactly (autoAccept Yes-with-record; non-TTY or `--json` fail-closed keep with advisory + `non-interactive fail-closed`/`workflow_locked` escalation; TTY prompt defaults unchanged); every Yes path writes a validated `downscale_decision` in the same atomic `updateChange` as the `workflow` rewrite; no No path writes one; T-I1–T-I4 inverted, T-R1–T-R7 green with record assertions added to T-R1/T-R2/T-R4, T-C1–T-C3 added and passing; upscale/summary-time/quick paths byte-untouched; `tsc --noEmit` clean.

## Batch 3 (depends on Batch 2)

- [x] **Task 3.1: Full gates run + closeout**
  - **Files**: none modified (fix-forward edits only if gates fail; any fix stays within the files listed in Tasks 1.1–2.1)
  - **Action**: Run the full verification gates over the assembled change: complete test suite, typecheck, and build (template copy + build stamp included). Confirm the design's spec-conformance table is fully landed: every row maps to a merged component and a passing test ID; confirm the two documented deltas are in place for reviewers — (a) `askYesNoDetailed` as the sanctioned ADR-4 scope addition, (b) the intended breaking behavior change for pipelines relying on silent auto-collapse (Risk R3, opt-in via `auto_accept_recommendation: true`). If any gate fails, fix within the change's file set and re-run until green.
  - **Verify**: `cd /home/utx0/Code/metta/.metta/worktrees/fix-intent-time-workflow-auto-downscale-misfires-file-count && npm test && npm run lint && npm run build`
  - **Done**: `npm test` fully green (entire suite, not just touched files); `tsc --noEmit` and `npm run build` clean; all test-matrix IDs (T-S1–S3, T-I1–I4, T-R1–R7, T-C1–C3, T-D1–D4, T-H1) accounted for; no files outside the declared task file sets modified; change ready for verify/finalize.
