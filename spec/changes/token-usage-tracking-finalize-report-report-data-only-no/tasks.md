# Tasks for token-usage-tracking-finalize-report-report-data-only-no

## Batch 1 (no dependencies — foundations)

- [ ] **Task 1.1: TokenUsageRecordSchema + TokensConfigSchema**
  - **Files**: `src/schemas/change-metadata.ts`, `src/schemas/project-config.ts`, `tests/schemas.test.ts`
  - **Action**: In `change-metadata.ts`, add `TokenUsageRecordSchema` as a strict Zod object sibling of `ModelEscalationSchema` with exactly `task` (`z.string().min(1)`), `agent` (`z.string().min(1)`), `model` (`ModelAliasEnum`), `tokens` (`z.number().int().positive()`), `timestamp` (`z.string().datetime()`); add `token_usage: z.array(TokenUsageRecordSchema).optional()` to `ChangeMetadataSchema`; add cross-referencing doc comments on both `token_usage` and the existing `artifact_tokens` field (which stays byte-for-byte untouched) disambiguating the two. In `project-config.ts`, add `TokensConfigSchema` as a strict sibling of `UatConfigSchema` with `enabled: z.boolean().default(true)` and wire `tokens: TokensConfigSchema.default({})` onto `ProjectConfigSchema`. Export both new schemas. In `tests/schemas.test.ts`, add a `TokenUsageRecordSchema` block (valid record with `model: "haiku"` round-trips through `ChangeMetadataSchema` inside `token_usage`; rejections for `tokens: 0`, `tokens: 12.5`, `tokens: -5`, non-enum `model`, unknown extra key; metadata without `token_usage` still valid; existing `artifact_tokens` fixture parses unchanged) and a `TokensConfigSchema` block (omitted `tokens` key defaults to `{enabled: true}`; unknown key and non-boolean `enabled` rejected).
  - **Verify**: `npx vitest run tests/schemas.test.ts` and `npx tsc --noEmit`
  - **Done**: Both schemas exported, strict, additive; all new schema test cases pass; no change to `artifact_tokens` or any existing schema behavior.

- [ ] **Task 1.2: Guard-hook allowlist entry (byte-identical pair)**
  - **Files**: `.claude/hooks/metta-guard-bash.mjs`, `src/templates/hooks/metta-guard-bash.mjs`, `tests/metta-guard-bash.test.ts`
  - **Action**: Add one `'tokens'` entry to `ALLOWED_SUBCOMMANDS` immediately after `'model-escalation'` in BOTH hook copies, with an inline comment describing it as append-only usage instrumentation (mirroring the existing entries' comment style). No other tier classification changes. In `tests/metta-guard-bash.test.ts`, add an allow case for an orchestrator-issued `metta tokens record --task impl --agent executor --model haiku --tokens 1000`, byte-parallel to the existing model-escalation allow case.
  - **Verify**: `node --check .claude/hooks/metta-guard-bash.mjs && node --check src/templates/hooks/metta-guard-bash.mjs && diff .claude/hooks/metta-guard-bash.mjs src/templates/hooks/metta-guard-bash.mjs && npx vitest run tests/metta-guard-bash.test.ts`
  - **Done**: Both copies byte-identical, both pass `node --check`, `tokens` is allowed, all pre-existing guard classifications unchanged (existing guard tests still green).

- [ ] **Task 1.3: External tokens report template**
  - **Files**: `src/templates/artifacts/tokens.md`
  - **Action**: Create the external template skeleton rendered by `TemplateEngine`: title, `{change_name}` and `{generated_date}` metadata lines, fixed approximate-figures disclaimer prose ("all figures are approximate orchestrator-reported counts, not billing-grade accounting"), then section slots in report order: `{total}`, `{per_artifact_table}`, `{per_role_rollup}`, `{per_model_rollup}`, `{split}` (under the heading "Cheap/pinned (non-inherit) vs inherit"), `{gaps}`. Follow the structure/conventions of the existing `src/templates/artifacts/uat.md`. No build changes — `copy-templates` already ships this directory.
  - **Verify**: `cat src/templates/artifacts/tokens.md` shows all six placeholders plus `{change_name}`/`{generated_date}`; `npm run build` copies it to `dist/templates/artifacts/tokens.md`
  - **Done**: Template exists with every placeholder the generator will substitute; disclaimer prose present; delivered to `dist/` by the existing build step.

- [ ] **Task 1.4: Spec scenario datum fix (ADR-1: cheap → haiku)**
  - **Files**: `spec/changes/token-usage-tracking-finalize-report-report-data-only-no/spec.md`
  - **Action**: Per ADR-1, correct every `model: "cheap"` / `--model cheap` scenario datum to `haiku`: (a) the Token Usage Record Schema valid-record scenario (`model: "cheap"` → `model: "haiku"`); (b) the two Tokens Record CLI scenario command lines (`--model cheap` → `--model haiku`, including the invalid-tokens scenario); (c) the guard-hook scenario command line; (d) the Tokens Report Content requirement and scenarios — align the split wording from "cheap-vs-inherit split contrasting tokens recorded at the `cheap` alias" to the non-inherit definition: "cheap/pinned (non-inherit) vs inherit split contrasting tokens recorded at any concrete alias (`model !== 'inherit'`) against tokens recorded at `inherit`", and update the Full-report scenario's "both `cheap` and `inherit` models" datum accordingly (e.g. `haiku` and `inherit`). No other spec wording changes.
  - **Verify**: `grep -n "cheap" spec/changes/token-usage-tracking-finalize-report-report-data-only-no/spec.md` shows only the "Cheap/pinned (non-inherit)" label wording; no `model: "cheap"` or `--model cheap` datums remain
  - **Done**: Spec is self-consistent with `ModelAliasEnum` — every scenario datum uses a real alias; split requirement matches the non-inherit definition the implementation computes.

## Batch 2 (depends on Batch 1 — features on the schema)

- [ ] **Task 2.1: `metta tokens record` CLI command + registration**
  - **Depends on**: Task 1.1 (schema), Task 1.2 (guard allowlist for skill flows)
  - **Files**: `src/cli/commands/tokens.ts`, `src/cli/index.ts`, `tests/tokens-command.test.ts`
  - **Action**: Create `registerTokensCommand` as a structural clone of `src/cli/commands/model-escalation.ts`: `tokens` group → `record` subcommand with required `--task`, `--agent`, `--model`, `--tokens`, optional `--change`; `createCliContext`; verbatim auto-selection block (exactly one active change auto-selects; zero → "No active changes." typed error; multiple → typed error naming candidates); build record with `new Date().toISOString()`, `TokenUsageRecordSchema.parse` BEFORE any write, append via a single `ctx.artifactStore.updateChange(changeName, { token_usage: [...(meta.token_usage ?? []), record] })`; success `--json` → `outputJson({ change, task, agent, model, tokens })`, human → one confirmation line; failure → `outputJson({ error: { code: 4, type: 'tokens_record_error', message } })` or `console.error`, then `process.exit(4)`. In `src/cli/index.ts`, add one import and one `registerTokensCommand(program)` call beside `registerModelEscalationCommand`; do NOT add `tokens` to `CONFIG_PARSE_EXEMPT_COMMANDS` or `DRIFT_CHECK_EXEMPT_COMMANDS`. Create `tests/tokens-command.test.ts` mirroring `tests/model-escalation-command.test.ts`: append + JSON payload + ISO timestamp round-trip; second append preserves first; `--tokens -5`, `--tokens 12.5`, and bad `--model` each exit 4 with no metadata mutation; auto-select single change; multi-change exit 4 naming candidates; nonexistent `--change` exit 4; explicit `--change beta` leaves `alpha` untouched.
  - **Verify**: `npx vitest run tests/tokens-command.test.ts` and `npx tsc --noEmit`
  - **Done**: Command registered and behaviorally identical in shape to model-escalation; validation precedes the single write (no partial state); all listed test cases pass.

- [ ] **Task 2.2: `generateTokensReport` generator**
  - **Depends on**: Task 1.1 (record type), Task 1.3 (template)
  - **Files**: `src/finalize/tokens-report-generator.ts`, `tests/tokens-report-generator.test.ts`
  - **Action**: Create the functional-core generator with the design's exact signature: `export interface TokensReportInput { changeName: string; generatedAt: string; tokenUsage: TokenUsageRecord[]; artifactTimings: Record<string, ArtifactTiming> }` and `export async function generateTokensReport(input: TokensReportInput): Promise<{ markdown: string }>`. It MUST NOT read the clock — `generatedAt` is caller-injected. Compute: total tokens; per-artifact table `| Artifact/task | Agent | Model | Tokens |` in record (append) order; per-role rollup and per-model rollup sorted lexicographically; non-inherit-vs-inherit split (sum where `model !== 'inherit'` vs `=== 'inherit'`, two figures summing to total) under the "Cheap/pinned (non-inherit) vs inherit" label; GAPS per ADR-2: `sortedKeys(artifactTimings).filter(k => !tokenUsage.some(r => r.task === k))` (exact string match), with the explicit "No gaps found" line when empty. Render via `new TemplateEngine([new URL('../templates/artifacts', import.meta.url).pathname])` → `engine.render('tokens.md', {...})` — the only I/O is the template read. Create `tests/tokens-report-generator.test.ts` mirroring `tests/uat-generator.test.ts`: all sections present; two runs over identical inputs with fixed date are byte-identical; per-artifact rows in record order; rollups and GAPS lexicographic; GAPS exact-match including fine-grained task id (`T1`) counting in totals but NOT clearing an artifact-level gap; empty usage + populated timings → fully-populated GAPS; empty timings + empty usage → "No gaps found"; split figures sum to total; template-contract case asserting all placeholders exist in the shipped `src/templates/artifacts/tokens.md` (per the `uat-template-contract` precedent).
  - **Verify**: `npx vitest run tests/tokens-report-generator.test.ts` and `npx tsc --noEmit`
  - **Done**: Deterministic, clock-free, template-driven generator; ADR-2 ordering and GAPS semantics unit-tested; no template body inlined in TypeScript.

- [ ] **Task 2.3: `getAvgTokensPerChangeByTier` ceremony metric**
  - **Depends on**: Task 1.1 (schema field)
  - **Files**: `src/util/ceremony-metrics.ts`, `tests/ceremony-metrics.test.ts`
  - **Action**: Add `getAvgTokensPerChangeByTier(specDir, artifactStore)` cloned from `getModelEscalationRate`'s active+archive iteration: active changes via `ArtifactStore`, archives via `StateStore.read('archive/<entry>/.metta.yaml', ChangeMetadataSchema)`, catch-and-skip per entry, never throws. Per-change total = sum of `token_usage[].tokens`; per ADR-3, absent AND present-but-empty `token_usage` arrays are excluded (never counted as 0). Group by the fixed four tiers; return `Record<'trivial'|'quick'|'standard'|'full', { mean: number; sample_size: number } | null>` with all four keys always emitted and `null` for no-data tiers; changes with a `workflow` outside the four tiers are ignored. Extend `tests/ceremony-metrics.test.ts`: tier grouping across active+archived (quick 10000+30000 → mean 20000, sample_size 2); absent-field exclusion; empty-array exclusion; unknown-tier ignore; wholly-empty data → all four tiers `null`; corrupt archive entry skipped without throwing.
  - **Verify**: `npx vitest run tests/ceremony-metrics.test.ts` and `npx tsc --noEmit`
  - **Done**: Helper returns stable four-key shape, ADR-3 exclusions enforced, never throws; all new unit cases pass.

## Batch 3 (depends on Batch 2 — integration)

- [ ] **Task 3.1: Finalizer Step 5c + finalize CLI output**
  - **Depends on**: Task 2.2 (generator), Task 1.1 (config schema)
  - **Files**: `src/finalize/finalizer.ts`, `src/cli/commands/finalize.ts`, `tests/finalizer.test.ts`, `tests/cli-finalize.test.ts`
  - **Action**: In `finalizer.ts`, insert Step 5c immediately after the UAT Step 5b block and before archive, mirroring 5b line-for-line: gate on `this.projectRoot` (ADR-4: no-projectRoot construction skips with `tokensPath: null`); reuse the shared `configLoader ??=` instance; check `config.tokens.enabled`; call `generateTokensReport({ changeName, generatedAt: new Date().toISOString().slice(0, 10), tokenUsage: metadata.token_usage ?? [], artifactTimings: metadata.artifact_timings ?? {} })`; write via `artifactStore.writeArtifact(changeName, 'TOKENS.md', markdown)`; independent try/catch → set `tokensError` and best-effort `rm` of the partial `TOKENS.md`; compute post-archive `tokensPath` (`join(specDir, 'archive', archiveName, 'TOKENS.md')`) beside `uatPath`. Extend `FinalizeResult` additively: `tokensPath: string | null` and optional `tokensError` spread as `...(tokensError ? { tokensError } : {})`; all early-return/dry-run/failure paths carry `tokensPath: null` (Step 5c structurally unreachable on those paths). In `src/cli/commands/finalize.ts`, add four lines parallel to the uat lines: JSON success payload gains `tokensPath` + conditional `tokensWarning`; human mode prints the tokens path line when present and a degradation warning when `tokensError` is set; error JSON shapes untouched. Extend `tests/finalizer.test.ts`: TOKENS.md written after spec merge and before archive, swept into archive beside UAT.md; generator-throw degrades (finalize succeeds, `tokensError` set, partial file removed, UAT unaffected); UAT-throw leaves tokens generation unaffected (independence both ways); `tokens.enabled: false` skips cleanly with `tokensPath: null` while UAT proceeds; dry-run, gate-failure, incomplete-artifacts, and conflict paths leave no TOKENS.md; empty/absent `token_usage` still produces a report with full GAPS; `tokensPath` in the success result. Extend `tests/cli-finalize.test.ts`: JSON success carries `tokensPath` (path or `null`) + `tokensWarning` only when degraded; human tokens line present when generated, absent when disabled; `gates_failed` and other error payloads byte-unchanged.
  - **Verify**: `npx vitest run tests/finalizer.test.ts tests/cli-finalize.test.ts` and `npx tsc --noEmit`
  - **Done**: Step 5c placement, degradation independence, cleanup, toggle, no-stray, and output-shape scenarios all pass; no pre-existing finalize behavior or error shape changed.

- [ ] **Task 3.2: Progress wiring**
  - **Depends on**: Task 2.3 (metric helper)
  - **Files**: `src/cli/commands/progress.ts`, `tests/progress-ceremony-metrics.test.ts`
  - **Action**: In `progress.ts`, call `getAvgTokensPerChangeByTier` and add JSON key `avg_tokens_per_change_by_tier` beside `model_escalation_rate` with verbatim `null` passthrough (never coerced to `0`); add one human line rendering all four tiers in order as `formatThousandsK` value or the explicit `no data` wording, e.g. `  Avg tokens per change: trivial no data · quick 20k · standard 50k · full no data`. All pre-existing progress metrics unchanged. Extend `tests/progress-ceremony-metrics.test.ts`: extend the `writeArchiveMetadata` helper with optional `token_usage`; tier averages computed across active+archived changes; per-tier `null` (not `0`) in JSON for no-data tiers; human output shows `no data` wording and `formatThousandsK` values; pre-feature archive (no `token_usage` field) skipped without error or mutation.
  - **Verify**: `npx vitest run tests/progress-ceremony-metrics.test.ts` and `npx tsc --noEmit`
  - **Done**: JSON and human progress output carry the tier metric per the ceremony-metric null conventions; existing metrics untouched.

- [ ] **Task 3.3: Skill recording instruction (four byte-identical pairs)**
  - **Depends on**: Task 2.1 (command exists), Task 1.2 (guard allows it)
  - **Files**: `src/templates/skills/metta-plan/SKILL.md`, `.claude/skills/metta-plan/SKILL.md`, `src/templates/skills/metta-execute/SKILL.md`, `.claude/skills/metta-execute/SKILL.md`, `src/templates/skills/metta-verify/SKILL.md`, `.claude/skills/metta-verify/SKILL.md`, `src/templates/skills/metta-next/SKILL.md`, `.claude/skills/metta-next/SKILL.md`, `tests/skill-tokens-record.test.ts`
  - **Action**: Insert the design's one verbatim instruction block (identical bytes at all four anchors): "After each subagent returns, record its reported token usage: `metta tokens record --task <artifact-or-task-id> --agent <subagent-type> --model <alias> --tokens <count> --change <name>` — `--task` is the artifact or task id it worked, `--agent` is the `subagent_type` you spawned, `--model` is the model alias you passed to `Agent(...)` (use `inherit` when you omitted the `model` parameter), and `--tokens` is the token count from its completion report. This applies to every spawn — planner, executor, reviewer, and verifier alike." Anchors: metta-plan — new step 2d before the `metta complete` step (renumber old 2d → 2e); metta-execute — new paragraph directly after the `agent.model` pass-through paragraph; metta-verify — new step between steps 2 and 3 (renumber subsequent steps); metta-next — new `## Rules` bullet beside the commit-ownership rule. No other wording changes — no model routing, agent selection, or tier wording may differ. Edit template and deployed copy of each pair in this same task, keeping every pair byte-identical. Create `tests/skill-tokens-record.test.ts` mirroring `tests/skill-iteration-record.test.ts`: assert the verbatim instruction string is present in all four template SKILL.md files.
  - **Verify**: `for s in metta-plan metta-execute metta-verify metta-next; do diff src/templates/skills/$s/SKILL.md .claude/skills/$s/SKILL.md || exit 1; done && npx vitest run tests/skill-tokens-record.test.ts tests/template-deploy-sync.test.ts`
  - **Done**: All four pairs byte-identical, instruction verbatim-identical across anchors, template-deploy-sync suite green, no routing wording changed.

## Batch 4 (depends on Batch 3 — full-system verification)

- [ ] **Task 4.1: Full-suite verification and build check**
  - **Depends on**: Tasks 3.1, 3.2, 3.3 (and transitively all prior batches)
  - **Files**: none expected — fix-forward only if the full run surfaces an integration failure (any file touched must be reported)
  - **Action**: Run the complete verification set over the integrated system: full test suite, strict type check, and a build to confirm `tokens.md` and the hook/skill templates ship to `dist/`. Confirm the byte-identity invariants one final time (guard-hook pair, four skill pairs). If any failure surfaces, apply the minimal fix and re-run until green.
  - **Verify**: `npm test && npx tsc --noEmit && npm run build && diff .claude/hooks/metta-guard-bash.mjs src/templates/hooks/metta-guard-bash.mjs && ls dist/templates/artifacts/tokens.md`
  - **Done**: Entire suite green, zero type errors, build ships all new templates, all byte-identity pairs hold; near-1:1 test-to-source ratio maintained (3 new source files ↔ 3 new test files; every modified source file has its matching test file updated).
