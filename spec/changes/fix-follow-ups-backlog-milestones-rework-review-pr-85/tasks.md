# Tasks for fix-follow-ups-backlog-milestones-rework-review-pr-85

Sequencing note: `src/cli/commands/backlog.ts` is touched by three fixes (C1 sanitize wrap, C2 commitPaths, C4 isDefault). All backlog.ts edits are consolidated into Task 2.1 so no two parallel tasks ever touch it. Likewise `tests/cli-issue-backlog.test.ts` (touched by C1/C2/C4 test additions) lives only in Task 2.1.

## Batch 1 (no dependencies)

- [x] **Task 1.1: Sanitize helper module + unit suite**
  - **Files**: `src/util/sanitize-text.ts` (new), `tests/sanitize-text.test.ts` (new)
  - **Action**: Create `src/util/sanitize-text.ts` exporting `stripControlSequences(text: string): string` — pure, total, idempotent. One private module constant `CONTROL_SEQUENCE_RE` with load-bearing alternation order (full ESC sequences before the bare-control class): `/\x1b\[[0-?]*[ -/]*[@-~]|\x1b\][^\x07\x1b]*(?:\x07|\x1b\\)?|\x1b[PX^_][^\x1b]*(?:\x1b\\)?|\x1b[@-Z\\-_]|[\x00-\x1f\x7f-\x9f]/g`. No `u` flag. Doc comment must explain the ordering and enumerate coverage: CSI, OSC (unterminated-tolerant), DCS/SOS/PM/APC, two-byte Fe escapes, bare C0/DEL/C1. No barrel export — direct import is the util norm. Create `tests/sanitize-text.test.ts` covering: color CSI, cursor-move CSI, `\x1b[2J`, OSC title-set (BEL- and ST-terminated), unterminated OSC, OSC-8 hyperlink, DCS, raw `\x9b` C1 CSI, BEL/backspace/CR, lone trailing ESC, plain unicode/emoji passthrough (byte-for-byte, all code points >= U+00A0), empty string.
  - **Verify**: `npx vitest run tests/sanitize-text.test.ts`
  - **Done**: Suite passes; helper strips all documented sequence classes and passes printable text through unchanged; 1:1 test ratio maintained; no new dependency added.

- [x] **Task 1.2: MigrationResult.changedPaths in backlog-migrate**
  - **Files**: `src/backlog/backlog-migrate.ts`, `tests/backlog-migrate.test.ts`
  - **Action**: Add `changedPaths: string[]` to the exported `MigrationResult` interface with the doc comment from design.md Data Model (project-relative posix display paths of every file created, rewritten, or removed; empty when nothingToDo or all collided; suitable as git pathspecs with cwd = projectRoot). Initialize `changedPaths: []` in the result object (~lines 197-202). After each successful `migrateItem` in both conversion loops (active ~223-232, done ~248-257), push the three display paths: `plan.targetDisplayPath`, the legacy origin display path, and the archive copy (`${ARCHIVED_TO}/<file>` for active, `${ARCHIVED_TO}/done/<file>` for done). Collision-skipped items push nothing. Do NOT touch `src/cli/commands/backlog.ts` (that wiring is Task 2.1). Update `tests/backlog-migrate.test.ts`: add `changedPaths: []` to the two full-object `toEqual` assertions (lines 57 and 264); add positive assertions that conversion tests populate `changedPaths` with the expected triples.
  - **Verify**: `npx vitest run tests/backlog-migrate.test.ts`
  - **Done**: Suite passes; `changedPaths` is populated only at the single source of truth inside `migrateLegacyBacklog`; additive change only — no other field, schema, or on-disk format altered.

- [x] **Task 1.3: Issues-store test consolidation + tsconfig dist hygiene**
  - **Files**: `tests/issues-store.test.ts`, `src/issues/issues-store.test.ts` (delete), `tsconfig.json`
  - **Action**: Fold the nine describe-blocks unique to `src/issues/issues-store.test.ts` into `tests/issues-store.test.ts` (parseIssue body tolerance; legacy frontmatter-less files; frontmatter-aware list/show; create-with-fields; createIdea; slug-collision guard; updateFrontmatter; listResolved; archive frontmatter carry-through + Shipped-in). Wrap all ported content in one scoping `describe` with its own `beforeEach`/`tmpDir` fixture so it does not clash with the existing suite's module-level setup. Rewrite the import path to `'../src/issues/issues-store.js'`; add `IssueSlugCollisionError` to the import; carry over the helpers (`issuePath`, `resolvedPath`, `seedIssueFile`) and sync-fs imports. Delete `src/issues/issues-store.test.ts`. Set tsconfig exclude to `["node_modules", "dist", "tests", "src/**/*.test.ts"]`.
  - **Verify**: `npx vitest run tests/issues-store.test.ts && npm run build && find dist -name '*.test.*' | wc -l` (must print 0); `find . -path ./node_modules -prune -o -name 'issues-store.test.*' -print` shows exactly `tests/issues-store.test.ts`
  - **Done**: Every unique src-side case exists in `tests/issues-store.test.ts` and passes; src copy deleted; after `tsc`, `dist/` contains no `*.test.js`/`*.test.d.ts`; the five remaining src-side test files still run under Vitest (config-driven discovery).

- [x] **Task 1.4: Tier advisory cap in renderBanner**
  - **Files**: `src/complexity/renderer.ts`, `tests/complexity-renderer.test.ts`
  - **Action**: In `renderBanner`, add module constant `const MAX_UPSCALE_TIER: Tier = 'standard'` and clamp the upscale branch (lines 44-48): when `recRank > chosenRank` and `recommended` outranks `standard`, render the target as `standard` while staying truthful about the score — e.g. `Advisory: current quick, scored full -- upscale to standard recommended (full upscale not supported)`. Must-implement edge case: current = `standard`, scored = `full` — capped target equals current tier, so render the cap without recommending a move, e.g. `Advisory: current standard, scored full -- full tier not supported; staying at standard`. Downscale and agreement branches untouched; `renderStatusLine` untouched; `src/cli/commands/complete.ts` untouched (both bespoke sites already self-cap). Update the function doc comment's output-forms list. In the same commit update `tests/complexity-renderer.test.ts`: rewrite :82-86 (scored full / chosen trivial → capped wording); add scored full / chosen quick (capped to standard), scored full / chosen standard (cap-equals-current edge), scored standard / chosen quick (byte-identical plain upscale — spec scenario "Standard-over-quick recommendation is unchanged").
  - **Verify**: `npx vitest run tests/complexity-renderer.test.ts`
  - **Done**: No renderer output can recommend upscaling to `full`; standard-over-quick wording unchanged; persisted/computed score values untouched (render-only cap); renderer change and test update land in one commit.

- [x] **Task 1.5: Guard-bash ALLOWED_BARE backlog (both hook copies) + hook tests**
  - **Files**: `src/templates/hooks/metta-guard-bash.mjs`, `.claude/hooks/metta-guard-bash.mjs`, `tests/metta-guard-bash.test.ts`
  - **Action**: In `src/templates/hooks/metta-guard-bash.mjs`, add `'backlog'` to `ALLOWED_BARE` (line 77 → `new Set(['roadmap', 'release', 'backlog'])`) and update the block comment (lines 72-76) to mention backlog; leave the deliberate bare-`milestone` exclusion note (lines 41-44) intact. `classify()` line 136 already handles bare+flags — no logic change. Hand-mirror the identical edit into `.claude/hooks/metta-guard-bash.mjs`; the two copies MUST remain byte-identical, edited as a pair in this one task/commit. In `tests/metta-guard-bash.test.ts`, mirror the release bare-form block (:809-882): bare `metta backlog` allowed; `metta backlog --json` allowed; `metta backlog frobnicate` fail-closed; existing Tier-2 gating tests for `backlog add/done/promote` unchanged and green.
  - **Verify**: `npx vitest run tests/metta-guard-bash.test.ts tests/hooks-byte-identity.test.ts`
  - **Done**: Bare `metta backlog` and `metta backlog --json` pass the hook without a session credential; `backlog <unknown-word>` and the write forms `add/done/promote` remain denied; byte-identity test green.

- [x] **Task 1.6: Docs sweep — workflows/architecture/state (items 4-7, 10)**
  - **Files**: `docs/workflows/README.md`, `docs/workflows/skills.md`, `docs/internals/architecture.md`, `docs/workflows/state.md`
  - **Action**: Per the C6 inventory: (README.md:20,23) reword — progress aggregates `spec/changes/` + `spec/issues/`; the backlog skill reads/writes issue frontmatter and manages `spec/milestones/`. (skills.md:470-505) rewrite the `/metta-backlog` section against the current model, using `src/templates/skills/metta-backlog/SKILL.md` + `src/cli/commands/backlog.ts` as source of truth: frontmatter flips / `--new` ideas, promote → `/metta-fix-issues`, done → `spec/issues/resolved/` with `--change` Shipped-in stamp, migrate for legacy, milestone subcommands. (architecture.md:45) backlog module row → `backlog-view.ts` (pure view over issue frontmatter) + `backlog-migrate.ts`; note `backlog-store.ts`/`BacklogStore` are deleted. (state.md:270-288) replace the stale `spec/backlog/` section with the view model (`backlog: true` / `priority` / `order` / `milestone` frontmatter; done via `spec/issues/resolved/`; legacy dir only as `migrate` input); add a `spec/milestones/` subsection if state.md lacks one. Leave-alone list from design.md applies (changelog, api.md:973, `docs/proposed/`, normative spec MUST-NOT lines, SKILL.md copies).
  - **Verify**: `grep -n 'spec/backlog/' docs/workflows/README.md docs/workflows/skills.md docs/internals/architecture.md docs/workflows/state.md` returns no matches
  - **Done**: All four files describe backlog storage as frontmatter over `spec/issues/` with zero `spec/backlog/` directory-store references (spec scenario "Docs describe the frontmatter-over-issues model").

- [x] **Task 1.7: Guard-edit allowlist tightening (both copies) + test flip + matching docs**
  - **Files**: `src/templates/hooks/metta-guard-edit.mjs`, `.claude/hooks/metta-guard-edit.mjs`, `tests/metta-guard-edit.test.ts`, `docs/guide/troubleshooting.md`, `docs/internals/guard-hooks.md`
  - **Action**: In `src/templates/hooks/metta-guard-edit.mjs:130`, set `ALLOW_PREFIXES = ['spec/issues/']` (drop `'spec/backlog/'`) and update the "issue/backlog bodies" comment (~124-127) to "issue bodies". Hand-mirror the identical edit into `.claude/hooks/metta-guard-edit.mjs`; copies MUST remain byte-identical, edited as a pair in this one task/commit. Flip `tests/metta-guard-edit.test.ts:87-94`: the `spec/backlog/<slug>.md` write assertion inverts from exit 0 (allowed) to exit 2 (blocked). Per design, the docs land in the same commit as the hook change: (troubleshooting.md:73) exceptions list → `.md` files under `spec/issues/` only; (guard-hooks.md:223 + preamble ~217) `ALLOW_PREFIXES` entry list → `spec/issues/` only; "enrichment of issue bodies".
  - **Verify**: `npx vitest run tests/metta-guard-edit.test.ts tests/hooks-byte-identity.test.ts` and `grep -n 'spec/backlog/' docs/guide/troubleshooting.md docs/internals/guard-hooks.md` returns no matches
  - **Done**: Out-of-band `.md` edits under `spec/backlog/` are denied (deliberate breaking tightening — surface in the change summary); documented allowlist matches enforced allowlist; byte-identity test green.

- [x] **Task 1.8: Refresh TOC row removal + worktree CLAUDE.md hand-edit + regression pin**
  - **Files**: `src/cli/commands/refresh.ts`, `CLAUDE.md` (worktree root, managed TOC section), `tests/refresh.test.ts`
  - **Action**: In `src/cli/commands/refresh.ts:176`, drop the Backlog TOC row; widen the Issues row description (line 175) to "Logged issues and backlog items (backlog is a frontmatter view)"; optionally add a `spec/milestones/` row. Hand-edit the worktree root `CLAUDE.md` inside the managed TOC markers to exactly match the regenerated output of the refresh.ts change (remove the `spec/backlog/` row; widened Issues description) — hand-edit, do NOT run the metta CLI. Add `expect(result).not.toContain('spec/backlog/')` to `tests/refresh.test.ts` as a regression pin, plus any assertion updates the existing suite needs for the changed row set.
  - **Verify**: `npx vitest run tests/refresh.test.ts` and `grep -n 'spec/backlog/' CLAUDE.md` returns no matches
  - **Done**: `metta refresh` output contains no `spec/backlog/` row (spec scenario "Refresh emits a TOC without a spec/backlog row"); hand-edited CLAUDE.md TOC is byte-identical to what the updated generator would emit; regression pin in place.

## Batch 2 (depends on Batch 1)

- [x] **Task 2.1: backlog.ts consolidated edits (sanitize wrap + commitPaths pathspecs + isDefault list) + CLI integration tests**
  - **Depends on**: Task 1.1, Task 1.2, Task 1.5
  - **Files**: `src/cli/commands/backlog.ts`, `tests/cli-issue-backlog.test.ts`
  - **Action**: All backlog.ts edits in one task. (a) Sanitize (C1): import `stripControlSequences` from `'../../util/sanitize-text.js'` and wrap the line-75 title interpolation: `${stripControlSequences(e.title)}`. Render-only — `--json` paths stay store-faithful. (b) Commit scoping (C2): line 181 (`add`) → `commitPaths(ctx.projectRoot, [join('spec', 'issues', `${slug}.md`)], ...)`; line ~269 (`done`) → `[join('spec', 'issues', `${slug}.md`), join('spec', 'issues', 'resolved', `${slug}.md`)]` (deletion + creation both staged by explicit path); line ~306 (`migrate`) → pass `result.changedPaths`. `commitPaths` itself (lines 28-44) unchanged. (c) Default subcommand (C4): line ~52 → `.command('list', { isDefault: true })` (release precedent, `release.ts:48`) so bare `metta backlog` renders the read-only list, exit 0. Tests in `tests/cli-issue-backlog.test.ts`: sanitize integration — seed an issue titled with `\x1b[31mEVIL\x1b[0m`, assert `backlog list` stdout contains `EVIL` and no `\x1b` byte, and the issue file on disk is byte-identical after render; sweep-regression — one case each for `add`, `done`, `migrate`: seed a dirty unrelated file under `spec/issues/`, run the command, assert the file is absent from `git show --name-status HEAD` and still dirty in `git status --porcelain`; bare-form — bare `metta backlog` exits 0 and prints list output. Existing done-commit assertion at :571-589 (rename or D+A pair) must remain satisfied.
  - **Verify**: `npx vitest run tests/cli-issue-backlog.test.ts`
  - **Done**: All three spec commit-scoping scenarios pass (no directory pathspecs at any of the three call sites; unrelated dirty files survive add/done/migrate); backlog list renders sanitized titles without rewriting the file; bare `metta backlog` is a working read-only view.

- [x] **Task 2.2: Milestone renderer sanitize wrap + integration test**
  - **Depends on**: Task 1.1
  - **Files**: `src/cli/commands/milestone.ts`, `tests/cli-milestone.test.ts`
  - **Action**: Import `stripControlSequences` from `'../../util/sanitize-text.js'` and wrap the line-176 title interpolation: `${stripControlSequences(issue.title)}`. One-word wrap only — no other milestone.ts changes (ADR-1: no other render sites in this change). In `tests/cli-milestone.test.ts`: seed an issue assigned to a milestone with a hostile title (ANSI CSI + control chars, e.g. `\x1b[31mEVIL\x1b[0m` / `\x07`), assert `metta milestone show <slug>` stdout contains the printable text and no `\x1b` or control byte, and assert the issue file on disk is byte-identical after render.
  - **Verify**: `npx vitest run tests/cli-milestone.test.ts`
  - **Done**: Milestone issue-list titles render sanitized (spec scenario "Control characters in a milestone issue title are stripped"); plain titles unchanged; no file rewrite.

---

**Note for the orchestrator (not code tasks — ship-time obligations per design.md Dependencies/ADR-1):** log these follow-up backlog issues when this change ships: (a) wrap the remaining ~13 title/description render sites with `stripControlSequences`, including a newline-preserving variant for multi-line bodies; (b) `spec/specs/roadmap-feature/spec.md` normative drift — still requires the deleted `BacklogStore` — route to its own gap/issue; (c) `--json` C1 (U+009B) passthrough; (d) relocate the five remaining src-side test files into `tests/`. Full gate suite runs in the verify phase, not as a task here.
