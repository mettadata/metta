# Tasks for fix-automatic-versioning-release-capability-metta

All executors: read `{change_root}/spec/changes/fix-automatic-versioning-release-capability-metta/design.md` before starting — it is authoritative for signatures, schemas, pipeline step order, and error wording. `{change_root}` = `/home/utx0/Code/metta/.metta/worktrees/fix-automatic-versioning-release-capability-metta`. All paths below are relative to `{change_root}`. Conventions: `.js` extensions on all TS imports (Node16 ESM), Zod validation on every state read/write, custom error classes, no string-literal templates in TS.

## Batch 1 (no dependencies)

- [x] **Task 1.1: Release config + releases-record schemas**
  - **Files**: `src/schemas/releases-record.ts` (new), `src/schemas/project-config.ts` (modify), `src/schemas/index.ts` (modify — barrel), `tests/schemas-releases-record.test.ts` (new), `tests/schemas.test.ts` (modify — release-config cases)
  - **Action**: Read design.md "Data Model". Create `src/schemas/releases-record.ts` with `BumpLevelEnum`/`BumpLevel`, `ReleaseEntrySchema`, `ReleasesRecordSchema`, `ReleasesRecord` exactly as specified (`.strict()` objects, `version` regex `^\d+\.\d+\.\d+$`, `date` regex `^\d{4}-\d{2}-\d{2}$`, `bump`/`bump_source` optional, `backfilled` default false, `changes: string[]`, `releases` newest-first). In `src/schemas/project-config.ts` add `ReleaseConfigSchema` (scheme literal `'semver'` with message "release.scheme: only 'semver' is supported"; `version_file` non-empty with message naming `release.version_file`; `tag_prefix` default `'v'`; `github_release` default `false`; `.strict()`) and add `release: ReleaseConfigSchema.optional()` to `ProjectConfigSchema` — no other changes to the parent. Export new symbols from `src/schemas/index.ts`. Tests: valid record parses; bad version/date rejected; unknown keys rejected; config scenarios from spec.md "Release Configuration Schema" — valid semver config accepted, `calver` rejected naming the scheme key, empty `version_file` rejected naming the key, defaults applied when only scheme+version_file given, existing configs without `release:` still parse.
  - **Verify**: `npx vitest run tests/schemas-releases-record.test.ts tests/schemas.test.ts` passes.
  - **Done**: Schemas match design.md byte-for-byte in shape; existing configs (no `release:` key) parse unchanged; all named-key error-message scenarios pass.

- [x] **Task 1.2: Guard + mint hooks (both copies) and guard tests**
  - **Files**: `.claude/hooks/metta-guard-bash.mjs` (modify), `src/templates/hooks/metta-guard-bash.mjs` (modify), `.claude/hooks/metta-session-mint.mjs` (modify), `src/templates/hooks/metta-session-mint.mjs` (modify), `tests/hooks-byte-identity.test.ts` (new), `tests/metta-guard-bash.test.ts` (modify), `tests/cli-metta-guard-bash-integration.test.ts` (modify)
  - **Action**: Read design.md "Guard, mint, and skill" and ADR-2. In BOTH guard-hook copies (must end byte-identical): add `['release', new Set(['status'])]` to `ALLOWED_TWO_WORD` (line ~29), add `'release'` to `ALLOWED_BARE` (bare `metta release` = status view, roadmap precedent, line ~60), add `['release', new Set(['cut'])]` to `BLOCKED_TWO_WORD` so `metta release cut` classifies as Tier-2 scope `release:cut` via the existing two-word scope keying in `classify()`/offender logic. In BOTH mint-hook copies: add `'metta-release': ['release:cut']` to `SKILL_SCOPES`. Create `tests/hooks-byte-identity.test.ts` following `tests/agents-byte-identity.test.ts` precedent, asserting every `.claude/hooks/*.mjs` file is byte-identical to its `src/templates/hooks/*.mjs` counterpart. Extend `tests/metta-guard-bash.test.ts` and `tests/cli-metta-guard-bash-integration.test.ts` with: `metta release status` allowed, bare `metta release` allowed, `metta release --json` allowed, `metta release cut` blocked without session credential, `metta release cut` allowed with a valid `release:cut`-scoped credential (mirror existing backlog/changes-abandon Tier-2 test patterns).
  - **Verify**: `npx vitest run tests/hooks-byte-identity.test.ts tests/metta-guard-bash.test.ts tests/cli-metta-guard-bash-integration.test.ts` passes.
  - **Done**: Both hook pairs byte-identical (pinned by test); guard scenarios from spec.md "Release Skill And Guard Authorization" covered.

- [x] **Task 1.3: metta-release skill template**
  - **Files**: `src/templates/skills/metta-release/SKILL.md` (new)
  - **Action**: Read design.md "Guard, mint, and skill" (frontmatter block is given verbatim) and ADR-2. Create the skill following the `src/templates/skills/metta-backlog/SKILL.md` pattern: main-session skill (NO `context: fork`), frontmatter exactly as design.md specifies (`name: metta:release`, `allowed-tools: [Bash, AskUserQuestion]`, PreToolUse mint hook `.claude/hooks/metta-session-mint.mjs metta-release`). Body steps: (1) run `metta release status --json` first (allow-listed; reports state and completes a Bash cycle so the mint credential is in place); (2) `AskUserQuestion` to accept/override bump level, confirm target version, and offer GitHub publication only when config enables it; (3) run `metta release cut --bump <level> --yes [--github] --json`; (4) echo results and suggest the manual `git push --follow-tags origin main` — the skill never pushes. No build changes needed (`copy-templates` copies `src/templates/skills` recursively).
  - **Verify**: File exists with valid YAML frontmatter; `npm run build` (or the copy-templates step) copies it under `dist/`; content appears in no TypeScript string literal.
  - **Done**: Skill template matches design frontmatter, drives the 4-step flow, and is delivered as a copied file per spec scenario "Skill delivered as template file".

- [x] **Task 1.4: gh edge module**
  - **Files**: `src/release/gh-release.ts` (new), `tests/release-gh-release.test.ts` (new)
  - **Action**: Read design.md "Module signatures" (gh-release) and the "Graceful Degradation When gh Unavailable" spec requirement. Implement `GhOutcome` union (`created` | `missing-binary` | `unauthenticated` | `failed`) and `createGithubRelease(cwd, tag, title, notes)` using `execFile` from `node:child_process` (arg arrays, no shell interpolation). Probe order: binary presence, then `gh auth status`, then `gh release create <tag> --title <title> --notes <notes>`. Never throws — every failure maps to a typed outcome; `remedy` strings include the manual retry command (`gh release create <tag> ...`). Tests: mock/stub execFile to cover all four outcomes, assert no gh invocation is attempted after a failed probe, assert remedy strings name the cause and retry command.
  - **Verify**: `npx vitest run tests/release-gh-release.test.ts` passes.
  - **Done**: All outcomes typed, function never rejects, probe distinguishes missing-binary vs unauthenticated per spec scenarios.

## Batch 2 (depends on Batch 1)

- [x] **Task 2.1: Semver core (pure)**
  - **Depends on**: Task 1.1 (imports `BumpLevel` from `../schemas/releases-record.js`)
  - **Files**: `src/release/semver.ts` (new), `tests/release-semver.test.ts` (new)
  - **Action**: Read design.md ADR-3 and "Module signatures". Implement `SemverParseError extends Error`, `parseSemver(v)` returning `{major, minor, patch}` and throwing `SemverParseError` on anything but strict `x.y.z` (reject prerelease/build metadata, leading `v`, whitespace, negative/non-numeric parts, leading zeros optional-strictness per your judgment but document it), and `bumpVersion(current, level: BumpLevel)` resetting lower components (major bump zeroes minor+patch, minor bump zeroes patch). Pure — no I/O. Error message must state the accepted `x.y.z` form. Tests cover parse success/failure matrix and all three bump levels.
  - **Verify**: `npx vitest run tests/release-semver.test.ts` passes.
  - **Done**: Strict `x.y.z` only; `SemverParseError` wording names the accepted form; bump arithmetic correct.

- [x] **Task 2.2: Bump derivation (pure)**
  - **Depends on**: Task 1.1 (imports `BumpLevel` type)
  - **Files**: `src/release/bump-derivation.ts` (new), `tests/release-bump-derivation.test.ts` (new)
  - **Action**: Read design.md "Module signatures" (bump-derivation) and spec.md "Bump Derivation From Shipped Changes". Implement `CommitInput {subject, body}`, `ParsedCommit {type, breaking}`, `parseConventionalCommit` (type = conventional prefix before `:`/`(`, null for non-conventional; breaking = `!` before `:` in subject, e.g. `feat(api)!:`, OR `BREAKING CHANGE:`/`BREAKING-CHANGE:` anywhere in body) and `deriveBump(commits)`: any breaking → `major`; else any type `feat` → `minor`; else `patch`. Non-conventional subjects count as patch-weight, never error. Deterministic, zero I/O. Tests: only-fix → patch, feat present → minor, `feat!:` and BREAKING-CHANGE footer → major, mixed sets, non-conventional subjects, empty array (→ patch), determinism (same input twice, same output).
  - **Verify**: `npx vitest run tests/release-bump-derivation.test.ts` passes.
  - **Done**: All four spec scenarios for bump derivation pass; function is pure.

- [x] **Task 2.3: Changelog grouping (pure)**
  - **Depends on**: Task 1.1 (imports `ReleasesRecord`)
  - **Files**: `src/release/changelog-grouping.ts` (new), `tests/release-changelog-grouping.test.ts` (new)
  - **Action**: Read design.md "Module signatures" (changelog-grouping) and "Releases record" semantics. Implement `ChangelogEntryInput {dirName, date, changeName, summaryContent}`, `ReleaseGroup {version, date, entries}` (`version: null` = Unreleased) and `groupEntriesByRelease(entries, record)`: an entry belongs to the release whose `changes` array lists its `dirName`; entries listed nowhere go to Unreleased; return order = `[Unreleased (only if non-empty), ...record.releases order (newest first)]`; every entry appears exactly once even if a corrupt record lists a dirName in two releases (first match wins — document the tiebreak). Empty release groups (record entry with no surviving archive dirs) still render as groups. Tests: split across boundary (A,B in 0.5.0; C unreleased), multiple versions in record order, no duplicates, unreleased omitted when empty, unknown dirNames in record ignored gracefully.
  - **Verify**: `npx vitest run tests/release-changelog-grouping.test.ts` passes.
  - **Done**: Exactly-once attribution guaranteed; group ordering matches design; pure.

- [x] **Task 2.4: Version file I/O (edge)**
  - **Depends on**: Task 1.1 (imports `ReleaseConfig`)
  - **Files**: `src/release/version-file.ts` (new), `tests/release-version-file.test.ts` (new)
  - **Action**: Read design.md components table (version-file row) and spec.md "Product Version Distinct From Installed Version". Implement `ProductVersionError extends Error`, `readProductVersion(projectRoot, config)` and `writeProductVersion(projectRoot, config, next)`. For paths ending `.json`: read/write the top-level `version` field, preserving the file's existing indentation and trailing-newline presence (detect indentation from the raw text, do not reformat other keys). Otherwise: treat the file as plain text — read trimmed, write `next` (preserve trailing newline convention). Errors (missing file, missing/non-string `version` field, unreadable) throw `ProductVersionError` whose message names the configured path and says "product version" — the string `installed_version` must appear nowhere in this module. Tests: package.json with 2-space and 4-space/tab indentation round-trips byte-identically except the version value; plain-text file; missing file error wording (names path, says "product version", no `installed_version` mention); missing version field error.
  - **Verify**: `npx vitest run tests/release-version-file.test.ts` passes.
  - **Done**: JSON writes preserve formatting; error wording matches spec scenario "Version file missing yields distinguishing error".

- [x] **Task 2.5: Releases record store (edge)**
  - **Depends on**: Task 1.1 (imports `ReleasesRecordSchema`)
  - **Files**: `src/release/releases-record-store.ts` (new), `tests/release-releases-record-store.test.ts` (new)
  - **Action**: Read design.md components table and "Releases record" section. Implement `loadReleasesRecord(specDir): Promise<ReleasesRecord | null>` (reads `{specDir}/releases.yaml`; returns `null` when file absent; parses YAML then `ReleasesRecordSchema.parse` — invalid content throws, callers decide degradation) and `saveReleasesRecord(specDir, record)` (validates via `ReleasesRecordSchema.parse` before serializing to YAML — no unvalidated writes). Use the same YAML library already used by the state store (check existing `src/` state modules for the import). Tests: round-trip save/load, absent file → null, invalid YAML/schema throws Zod error, write output is valid YAML re-parseable by the schema.
  - **Verify**: `npx vitest run tests/release-releases-record-store.test.ts` passes.
  - **Done**: No unvalidated read or write path exists; null-on-absent contract honored.

## Batch 3 (depends on Batch 2)

- [x] **Task 3.1: Git release-tags edge**
  - **Depends on**: Task 2.2 (imports `CommitInput` from `./bump-derivation.js`)
  - **Files**: `src/release/git-release-tags.ts` (new), `tests/release-git-release-tags.test.ts` (new)
  - **Action**: Read design.md "Module signatures" (git-release-tags). Implement with `execFile('git', [...])` arg arrays (no shell interpolation — follow the `src/` `git-log-timings.ts` precedent): `listReleaseTags(cwd, tagPrefix)` via `git tag --list '{prefix}[0-9]*' --sort=-version:refname` (newest first); `tagExists(cwd, tag)` via `git rev-parse -q --verify refs/tags/<tag>` (non-zero exit → false, not throw); `collectCommitsSince(cwd, tag | undefined)` via `git log [<tag>..]HEAD --format=%s%x1f%b%x1e` — full log, explicitly NOT `--first-parent` (bump signal lives on branch commits under merge commits) — parsing the 0x1f/0x1e-delimited stream into `CommitInput[]`; `attributeArchiveDirsToTags(cwd, tagsOldestFirst, dirNames)` mapping each dirName to the earliest tag whose tree contains it via `git ls-tree -d <tag> -- spec/archive/<dir>` (empty output = not contained; dirs in no tag omitted from the map). Tests use temp git repo fixtures (see `tests/cli-finalize.test.ts` for the temp-repo pattern): create commits/tags/archive dirs, assert tag ordering, undefined-tag full-history collection, delimiter parsing with multi-line bodies, ls-tree attribution to earliest tag.
  - **Verify**: `npx vitest run tests/release-git-release-tags.test.ts` passes.
  - **Done**: No shell string interpolation anywhere; all four functions behave per design against real temp repos.

- [x] **Task 3.2: Versioned changelog in DocGenerator**
  - **Depends on**: Task 2.3 (`groupEntriesByRelease`), Task 2.5 (record loading pattern), Task 1.1 (schema)
  - **Files**: `src/docs/doc-generator.ts` (modify), `tests/doc-generator-versioned-changelog.test.ts` (new)
  - **Action**: Read design.md "`generateChangelog` modification" and "Changelog output shape". In `generateChangelog` (around lines 205–235): after `loadArchiveEntries()`, add a private `loadReleasesRecord()` that reads `{specDir}/releases.yaml` Zod-validated, returning `null` when absent AND when invalid (invalid → push a warning, degrade, never throw — this must survive finalize's failure-tolerant path). Record `null` → existing flat rendering, byte-identical (do not touch that code path's output). Record present → call `groupEntriesByRelease` (import from `../release/changelog-grouping.js`) and render `## Unreleased` (only when non-empty) then `## {version} — {date}` per record order, each group containing the existing `### {date} — {changeName}\n{summaryContent}` blocks. Do NOT modify the constructor, `src/cli/commands/docs.ts`, or `src/finalize/finalizer.ts` — call sites pick this up automatically because the record lives under `specDir`. Tests (new file): record-absent output byte-identical to current flat format; versioned rendering with multiple versions newest-first; unreleased section with newly archived change; corrupt `releases.yaml` → warning + flat fallback, no throw; finalize-regeneration scenario — record present + a newly archived change regenerates with the new change under Unreleased and version sections intact.
  - **Verify**: `npx vitest run tests/doc-generator-versioned-changelog.test.ts` and existing `npx vitest run tests` doc-generator tests pass unchanged.
  - **Done**: Flat-format byte-identity pinned by test; corrupt record degrades with warning; spec scenario "Versioned shape survives finalize regeneration" covered.

## Batch 4 (depends on Batch 3)

- [x] **Task 4.1: ReleasePipeline + barrel exports**
  - **Depends on**: Tasks 1.1, 1.4, 2.1–2.5, 3.1, 3.2 (composes all of them)
  - **Files**: `src/release/release-pipeline.ts` (new), `tests/release-pipeline.test.ts` (new), `src/index.ts` (modify — barrel)
  - **Action**: Read design.md "Module signatures" (release-pipeline), "Pipeline step order", and Risks. Implement `ReleaseError` (root), `ReleaseConfigMissingError extends ReleaseError` (message names `release.scheme` and `release.version_file`), `ReleaseStep {step, status: 'pass'|'fail'|'skip', detail?}`, `ReleaseStatusResult`, `ReleaseCutOptions` (with injected `confirmVersion` callback), `ReleaseCutResult`, and `class ReleasePipeline { constructor(projectRoot, config); status(); cut(opts) }` in the `MergeSafetyPipeline` step-record style (ordered records, early return on failure). `status()`: version from version-file, last tag (null when none — not an error), commit count + recommended bump via git edge + deriveBump, unreleased archive count (archive dirNames minus union of record `changes`), warnings (git disabled/not a repo → version-only degradation). `cut()` step order exactly: `config-check → git-check → clean-tree → last-tag → collect-commits → derive-bump → confirm → target-tag-absent → backfill-record (first cut with pre-existing tags only, in memory) → write-version-file → write-releases-record → regen-changelog → commit → annotated-tag → gh`. Details that must hold: zero mutations before all abort points; `git-check` warns (not blocks) on `metta/*` branch and shallow clone; `clean-tree` uses `git status --porcelain --untracked-files=no`; decline in `confirm` → `status: 'aborted'`, nothing written; existing target tag → fail naming the tag, never `-f`/delete; new record entry prepended with `bump`/`bump_source` (`'override'` when `opts.bumpOverride` set); changelog regenerated via `DocGenerator.generate(['changelog'])`; mutation-group failure → best-effort restore of previously-read file contents then report failing step; `commit` = `git add -- <version_file> spec/releases.yaml docs/changelog.md` + `git commit -m "chore(release): {version}"`; `annotated-tag` = `git tag -a {tag_prefix}{version} -m "Release {version}"`; failure after commit but before tag → error text gives the single manual `git tag -a` command; `gh` runs only when `config.release.github_release === true` AND `opts.github === true`, its outcome lands in `result.gh` and never changes local `status: 'success'`; NO push anywhere; `--dry-run` stops after `target-tag-absent` with all mutation steps `skip`. Tests (temp git repos + injected `confirmVersion`): full successful cut asserting version file/record/changelog/commit/annotated tag consistency (spec: mutually consistent outputs); first cut with no tags; first cut with manual pre-existing tags exercising backfill (`backfilled: true` entries, unattributed dirs → Unreleased); existing-tag abort; dirty-tree abort; decline → aborted with no writes; dry-run; gh failure leaves local success; missing `release:` config → `ReleaseConfigMissingError` before any read/write. Add `export * from './release/...'` lines to `src/index.ts` for all `src/release/*` modules (semver, bump-derivation, changelog-grouping, version-file, releases-record-store, git-release-tags, gh-release, release-pipeline), watching for name collisions.
  - **Verify**: `npx vitest run tests/release-pipeline.test.ts` passes; `npx tsc --noEmit` clean.
  - **Done**: Step order and abort semantics match design exactly; all Release Cut Safety Constraints scenarios covered; barrel exports compile.

## Batch 5 (depends on Batch 4)

- [x] **Task 5.1: CLI command, registration, and full integration verification**
  - **Depends on**: Task 4.1 (imports `ReleasePipeline`), Task 1.2 (guard allows `release status`), Task 1.3 (skill in build output)
  - **Files**: `src/cli/commands/release.ts` (new), `src/cli/index.ts` (modify), `tests/cli-release.test.ts` (new)
  - **Action**: Read design.md "CLI surface". Create `registerReleaseCommand(program)` following the `src/cli/commands/docs.ts` shape (`createCliContext`, `outputJson`, `handleError`, `askYesNo` from `../helpers.js`): `release status [--json]` as the DEFAULT subcommand (bare `metta release` = status) printing current product version, last tag (or "none"), commit count since, recommended bump, unreleased archive-entry count; degraded git → version-only plus warning. `release cut [--bump <patch|minor|major>] [--yes] [--github] [--dry-run] [--json]`: `--bump` validated against the three levels; `--github` with `github_release: false` in config fails fast BEFORE any mutation with "release.github_release is disabled in config"; interactive confirm wires `askYesNo` (fail-closed non-TTY → clean abort without `--yes`); missing `release:` config → actionable error naming `release.scheme` and `release.version_file`; on success print version/tag/steps and the exact manual `git push --follow-tags` command (never push). Register in `src/cli/index.ts` with one import + `registerReleaseCommand(program)` after `registerDocsCommand` (line ~104); do NOT add to `CONFIG_PARSE_EXEMPT_COMMANDS`/`DRIFT_CHECK_EXEMPT_COMMANDS`. Tests (`tests/cli-release.test.ts`, temp-repo fixtures per `tests/cli-finalize.test.ts` precedent): status happy path + JSON output + no-config error + no files modified; cut with `--yes --bump minor` end-to-end; `--dry-run`; `--github` rejected when config disables it; non-TTY without `--yes` aborts. Then run the full integration gate: `npm test` (entire suite), `npx tsc --noEmit`, `npm run build`, and confirm `dist/` contains `templates/skills/metta-release/SKILL.md` and the updated `templates/hooks/*.mjs`.
  - **Verify**: `npx vitest run tests/cli-release.test.ts` passes; then `npm test`, `npx tsc --noEmit`, and `npm run build` all succeed with the skill and hooks present in `dist/`.
  - **Done**: Human-in-terminal flow works without skill/guard credential (spec: Release CLI Command Surface); whole suite green; build artifacts complete.
