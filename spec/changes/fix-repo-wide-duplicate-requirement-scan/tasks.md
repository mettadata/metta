# Tasks — fix-repo-wide-duplicate-requirement-scan

## Batch 1 — Dedupe spec files (independent files, parallelizable)

### Task 1.1: Dedupe fix-issues-command spec

- **Files:** `spec/specs/fix-issues-command/spec.md`
- **Action:** Re-verify lines 150–296 and 297–441 are byte-identical to lines 3–149 (modulo trailing blank lines), then keep lines 1–149 only. Normalize to exactly one trailing newline.
- **Verify:** `grep -c '^## Requirement:'` = 4; heading names unique; content of kept block unchanged.
- **Done:** File contains one copy each of `fix-issue-cli-command`, `issues-store-archival`, `skill-template`, `cli-registration`. Committed atomically.

### Task 1.2: Dedupe install-init spec

- **Files:** `spec/specs/install-init/spec.md`
- **Action:** Re-verify lines 68–92 == lines 3–27 and lines 93–107 == lines 28–42, then delete lines 68–107. Normalize trailing newline.
- **Verify:** `grep -c '^## Requirement:'` = 9; heading names unique.
- **Done:** Second copies of `init-command-drives-discovery` and `init-skill-invokes-init-command` removed; all other requirements untouched. Committed atomically.

### Task 1.3: Dedupe user-stories spec

- **Files:** `spec/specs/user-stories/spec.md`
- **Action:** Re-verify lines 190–374 are byte-identical to lines 3–189 (modulo trailing blank lines), then keep lines 1–189 only. Normalize trailing newline.
- **Verify:** `grep -c '^## Requirement:'` = 7; heading names unique.
- **Done:** File contains one copy each of the 7 requirements. Committed atomically.

## Batch 2 — Lock regeneration (depends on Batch 1)

### Task 2.1: Regenerate the three spec.lock files

- **Files:** `spec/specs/fix-issues-command/spec.lock`, `spec/specs/install-init/spec.lock`, `spec/specs/user-stories/spec.lock`
- **Action:** Run a tsx script (scratchpad, not committed) that for each capability parses the deduped spec.md with `parseSpec` and calls `SpecLockManager.update()` with specDir `<worktree>/spec`.
- **Verify:** Each lock has the expected unique requirement count (4 / 9 / 7), version incremented by 1, per-requirement hashes unchanged vs the first entries of the old locks, Zod-valid (written through StateStore).
- **Done:** Three locks regenerated by the real code path. Committed atomically.

## Batch 3 — Refresh CLAUDE.md (depends on Batch 2)

### Task 3.1: Regenerate CLAUDE.md counts

- **Files:** `CLAUDE.md`
- **Action:** Run `metta refresh` in the worktree.
- **Verify:** Active Specs rows for the three capabilities show counts derived from the deduped specs; diff shows no unrelated capability count changed unexpectedly.
- **Done:** CLAUDE.md committed atomically.
