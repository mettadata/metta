# Tasks for harden-metta-config-yaml-lifecycle-across-three-related-bugs

## Batch 1 — Foundations (parallel, 3 tasks; no upstream dependencies)

- [ ] **Task 1.1: Add VerificationConfigSchema to project-config.ts**
  - **Files**: `src/schemas/project-config.ts`
  - **Action**: Add three new exports directly above the `ProjectConfigSchema` declaration: `export const VerificationStrategyEnum = z.enum(['tmux_tui', 'playwright', 'cli_exit_codes', 'tests_only'])`, `export const VerificationConfigSchema = z.object({ strategy: VerificationStrategyEnum, instructions: z.string().optional() }).strict()`, and `export type VerificationConfig = z.infer<typeof VerificationConfigSchema>`. Add `verification: VerificationConfigSchema.optional()` as a new field inside the `ProjectConfigSchema` object literal (before the closing `.strict()`). The `verification` key is a top-level sibling of `project:`, not nested under `project:`.
  - **Verify**: `npx tsc --noEmit` passes. `npx vitest run tests/schemas.test.ts` still green. Manual check: `ProjectConfigSchema.parse({ project: { name: 'foo' }, verification: { strategy: 'playwright' } })` succeeds; `ProjectConfigSchema.safeParse({ verification: { strategy: 'magic' } }).success` is `false`.
  - **Done**: `VerificationStrategyEnum`, `VerificationConfigSchema`, and `VerificationConfig` are exported from `src/schemas/project-config.ts`; `ProjectConfigSchema` accepts an optional `verification` field with the four-value strategy enum; no existing tests regress.

- [ ] **Task 1.2: Create config-writer.ts with setProjectField**
  - **Files**: `src/config/config-writer.ts` (new), `src/config/config-writer.test.ts` (new), `src/index.ts` (modify)
  - **Action**: Create `src/config/config-writer.ts` exporting `async function setProjectField(root: string, path: string[], value: unknown): Promise<void>`. The function reads `<root>/.metta/config.yaml` via `readFile` — if the file is absent, it throws the ENOENT (does not create it). Parses with `YAML.parseDocument(raw)`. If `doc.errors.length > 0`, throws the first parse error. When `value` is an array, checks whether the existing node at `path` is a `YAMLSeq` with `flow === true`; if so, constructs a new `YAMLSeq` with `flow = true` and `Scalar` items to preserve inline style; otherwise calls `doc.setIn(path, value)` directly. Writes `doc.toString()` back with `writeFile`. Add `export * from './config/config-writer.js'` to `src/index.ts`. Create `src/config/config-writer.test.ts` with four unit tests: (a) idempotent re-write — calling twice with identical args produces byte-identical on-disk content, (b) comment preservation — `# project-specific override` above `stacks:` survives the write, (c) flow-style preservation — `stacks: ["rust"]` stays flow-style after update to `["rust", "py"]`, (d) ENOENT propagation — throws when file does not exist.
  - **Verify**: `npx tsc --noEmit` passes. `npx vitest run src/config/config-writer.test.ts` all four tests green.
  - **Done**: `setProjectField` exported from `src/config/config-writer.ts` and re-exported from `src/index.ts`; all four unit tests pass.

- [ ] **Task 1.3: Create repair-config.ts with pure repairProjectConfig**
  - **Files**: `src/config/repair-config.ts` (new), `src/config/repair-config.test.ts` (new), `src/index.ts` (modify)
  - **Action**: Create `src/config/repair-config.ts`. Export `RepairResult` interface: `{ source: string; duplicatesRemoved: string[]; invalidKeysRemoved: string[]; changed: boolean }`. Export `function repairProjectConfig(source: string): RepairResult`. The function: (1) parses leniently with `YAML.parseDocument(source, { uniqueKeys: false })`; on a fatal parse error (malformed YAML), returns `{ source, duplicatesRemoved: [], invalidKeysRemoved: [], changed: false }`; (2) walks `doc.contents` and all nested `YAMLMap.items` in reverse, tracking seen keys in a `Set<string>`, splicing out items whose key was already seen (last-occurrence-wins dedup), accumulating human-readable labels like `"removed duplicate key 'stacks' (kept last occurrence)"` in `duplicatesRemoved`; (3) calls `ProjectConfigSchema.safeParse(doc.toJSON())`; for each `unrecognized_keys` issue, calls `doc.deleteIn([...issue.path, badKey])` per offending key and records `"dropped unrecognized key '<key>'"` in `invalidKeysRemoved`; for other issue types (type or enum violations), calls `doc.deleteIn(issue.path)`; repeats step 3 up to 3 times total; (4) string-compares `doc.toString() !== source` to compute `changed`; returns `RepairResult`. Import `ProjectConfigSchema` from `'../schemas/project-config.js'`. Add `export * from './config/repair-config.js'` to `src/index.ts`. Create `src/config/repair-config.test.ts` with four tests: (a) three duplicate `stacks:` lines collapse to last occurrence; `duplicatesRemoved` has two entries; (b) schema-invalid top-level key `foo: bar` dropped; `invalidKeysRemoved` has one entry; (c) already-valid config: `changed === false`, `repairedSource === source`; (d) malformed YAML input: `changed === false`, result source equals input.
  - **Verify**: `npx tsc --noEmit` passes. `npx vitest run src/config/repair-config.test.ts` all four tests green.
  - **Done**: `repairProjectConfig` and `RepairResult` exported from `src/config/repair-config.ts` and re-exported from `src/index.ts`; all four unit tests pass.

## Batch 2 — Hard-fail loader (depends on Task 1.1; 1 task)

- [ ] **Task 2.1: Change loadYamlFile to throw ConfigParseError on parse failures**
  - **Depends on**: Task 1.1
  - **Files**: `src/config/config-loader.ts` (modify), `tests/config-loader.test.ts` (extend)
  - **Action**: Define and export `ConfigParseError` class at the top of `src/config/config-loader.ts` (before `loadYamlFile`): `export class ConfigParseError extends Error { constructor(public readonly path: string, public readonly cause: unknown) { const parserMsg = cause instanceof Error ? cause.message : String(cause); super(\`Failed to parse YAML config at ${path}: ${parserMsg}\`); this.name = 'ConfigParseError'; this.parserMessage = parserMsg } readonly parserMessage: string }`. In the catch block of `loadYamlFile` at lines 52-57, replace the `process.stderr.write(...)` + `return null` with `throw new ConfigParseError(filePath, err)`. The `null` return path (ENOENT) is unchanged. `ConfigLoader.load()` must not catch `ConfigParseError` — ensure the Zod error handler block (currently lines 136-154) only catches `ZodError` and does not swallow `ConfigParseError`. Extend `tests/config-loader.test.ts` with two new test cases: (a) write a `.metta/config.yaml` with duplicate top-level mapping keys (YAML strict parse error); assert `loader.load()` rejects with a `ConfigParseError` whose `path` contains `.metta/config.yaml`; (b) assert `loader.load()` returns a config object (not throwing) when `.metta/config.yaml` is absent (ENOENT path still returns empty merge result).
  - **Verify**: `npx tsc --noEmit` passes. `npx vitest run tests/config-loader.test.ts` all tests green including the two new ones.
  - **Done**: `ConfigParseError` exported from `src/config/config-loader.ts`; `loadYamlFile` throws on parse error; ENOENT path still returns `null`; both new test cases pass.

## Batch 3 — CLI wiring + install refactor (parallel, 3 tasks; depends on Batch 2)

- [ ] **Task 3.1: Replace writeStacksToConfig with setProjectField in install.ts**
  - **Depends on**: Task 1.2, Task 2.1
  - **Files**: `src/cli/commands/install.ts` (modify), `tests/cli.test.ts` (extend)
  - **Action**: In `src/cli/commands/install.ts`, delete the body of `writeStacksToConfig` (lines 177-209) — the const declarations, the lines/splice/append logic, and the `writeFile` call — and replace with a single `await setProjectField(root, ['project', 'stacks'], stacks)`. Add `import { setProjectField } from '../../config/config-writer.js'` at the top. Remove `writeFile` from the `node:fs/promises` import if it is no longer used after this change. Extend the existing `'is idempotent on an already-installed project'` test in `tests/cli.test.ts` (currently around line 102) to add: after the second `runCli(['--json', 'install'], tempDir)` call, read `.metta/config.yaml` and assert the file content contains exactly one line matching `/^\s*stacks:/m`.
  - **Verify**: `npx tsc --noEmit` passes. `npx vitest run tests/cli.test.ts -t "is idempotent"` passes with the new stacks-line assertion.
  - **Done**: `writeStacksToConfig` uses `setProjectField`; no regex/splice logic remains; idempotency test asserts single `stacks:` line.

- [ ] **Task 3.2: Add ConfigParseError handler to CLI error boundary**
  - **Depends on**: Task 2.1
  - **Files**: `src/cli/helpers.ts` (modify), `tests/cli.test.ts` (extend)
  - **Action**: In `src/cli/helpers.ts`, update `handleError` to detect `err instanceof ConfigParseError` (import `ConfigParseError` from `'../config/config-loader.js'`). When the error is a `ConfigParseError`, print to stderr: `${err.path}: ${err.parserMessage}\nRun 'metta doctor --fix' to repair.` and call `process.exit(4)`. Keep the existing `handleError` logic for all other error types. Audit all CLI command action handlers to ensure that errors thrown during `ConfigLoader.load()` propagate to `handleError` or a similar boundary — commands that currently catch all errors generically (via `catch (err)` in the action handler) and call `handleError` or `outputJson({ error })` will naturally propagate; verify none suppresses `ConfigParseError` silently. `metta doctor`'s action handler must NOT call `ctx.configLoader.load()` at any point (the current `doctor.ts` does not, so this is a guard). Add two new tests to `tests/cli.test.ts`: (a) write `.metta/config.yaml` with duplicate top-level `stacks:` keys in a git-initialized tempDir; run `metta status`; assert exit code is non-zero and stderr contains `.metta/config.yaml` and `metta doctor --fix`; (b) same corrupt config; run `metta doctor`; assert the command exits with code 0 or non-zero only from a failed diagnostic check, and stderr does NOT contain `metta doctor --fix` as remediation for the parse error.
  - **Verify**: `npx tsc --noEmit` passes. `npx vitest run tests/cli.test.ts -t "corrupt config"` new tests pass.
  - **Done**: `handleError` dispatches on `ConfigParseError`; `metta status` on corrupt config exits non-zero with actionable message; `metta doctor` on same corrupt config is not blocked by `ConfigParseError`.

- [ ] **Task 3.3: Inject verification context into instructions.ts for verification artifact**
  - **Depends on**: Task 1.1, Task 2.1
  - **Files**: `src/cli/commands/instructions.ts` (modify), `tests/cli.test.ts` (extend)
  - **Action**: In `registerInstructionsCommand` in `src/cli/commands/instructions.ts`, after the `const output = await ctx.instructionGenerator.generate({...})` call (line 55) and before the `if (json)` block (line 81), add a conditional block: `if (artifactId === 'verification') { const cfg = await ctx.configLoader.load(); const v = (cfg as Record<string, unknown>).verification as { strategy?: string; instructions?: string } | undefined; (output.context as Record<string, unknown>).verification_strategy = v?.strategy ?? null; (output.context as Record<string, unknown>).verification_instructions = v?.instructions ?? null }`. The `ConfigParseError` from `ctx.configLoader.load()` will propagate to the existing `catch` block which exits 4. Add two new tests to `tests/cli.test.ts`: (a) write `.metta/config.yaml` with `verification:\n  strategy: playwright\n  instructions: "http://localhost:3000"` and a valid change in place; run `metta --json instructions verification`; assert `output.context.verification_strategy === 'playwright'` and `output.context.verification_instructions === 'http://localhost:3000'`; (b) same project without a `verification:` block; assert both fields are `null`.
  - **Verify**: `npx tsc --noEmit` passes. `npx vitest run tests/cli.test.ts -t "instructions verification"` new tests pass.
  - **Done**: `metta instructions verification --json` emits `verification_strategy` and `verification_instructions` (null when absent) in `context`; both test cases pass.

## Batch 4 — Doctor --fix (depends on Batch 2 and Task 1.3; 1 task)

- [ ] **Task 4.1: Add --fix flag to metta doctor**
  - **Depends on**: Task 1.3, Task 2.1, Task 3.2
  - **Files**: `src/cli/commands/doctor.ts` (modify), `tests/cli.test.ts` (extend)
  - **Action**: In `registerDoctorCommand` in `src/cli/commands/doctor.ts`, add `.option('--fix', 'Repair duplicate keys and schema-invalid entries in .metta/config.yaml')` to the command chain. Change `.action(async () => {` to `.action(async (options) => {` and add `const options = ...` or use the commander-provided `options` argument. At the top of the action handler, if `options.fix` is truthy, execute the repair path exclusively (skip the diagnostic checks): (1) read `.metta/config.yaml` via `readFile`; if absent, print `No .metta/config.yaml found — nothing to repair.` and return; (2) call `repairProjectConfig(source)` (import from `'../../config/repair-config.js'`); (3) if `!result.changed`, print `.metta/config.yaml is already valid — no changes needed.` and return with exit 0; (4) write `result.source` back to `.metta/config.yaml` via `writeFile`; (5) call `autoCommitFile(ctx.projectRoot, configPath, 'chore: metta doctor repaired .metta/config.yaml')` (import from `'../helpers.js'`); (6) in human mode, print each entry in `result.duplicatesRemoved` and `result.invalidKeysRemoved` prefixed with `  - `; (7) in `--json` mode emit `{ repair: { duplicates_removed, invalid_keys_removed, committed: commitResult.committed, commit_sha: commitResult.sha ?? null } }`; (8) exit 0. The `--fix` path must NOT call `ctx.configLoader.load()` — it reads the raw file bytes directly. The existing diagnostic checks run only when `options.fix` is falsy. Add three new tests to `tests/cli.test.ts`: (a) write `.metta/config.yaml` with three `stacks:` entries under `project:` in a git-initialized tempDir; run `metta doctor --fix`; assert exit 0, exactly one line matching `/^\s*stacks:/m` in the written file, and `git log -1 --format=%s` outputs `chore: metta doctor repaired .metta/config.yaml`; (b) write `.metta/config.yaml` with `foo: "bar"` at top level; run `metta doctor --fix`; assert `foo:` is absent from the written file and stdout contains `dropped unrecognized key 'foo'`; (c) write a valid `.metta/config.yaml`; run `metta doctor --fix`; assert exit 0 and no git commit was created (git log count unchanged).
  - **Verify**: `npx tsc --noEmit` passes. `npx vitest run tests/cli.test.ts -t "doctor --fix"` all three new tests pass.
  - **Done**: `metta doctor --fix` deduplicates, drops schema-invalid keys, auto-commits, and exits 0; no-op on already-valid config; all three test cases pass.

## Batch 5 — Skill and agent template updates (parallel, 2 tasks; depends on Task 1.1)

- [ ] **Task 5.1: Add Round 4 to metta-init SKILL.md (both copies)**
  - **Depends on**: Task 1.1
  - **Files**: `.claude/skills/metta-init/SKILL.md` (modify), `src/templates/skills/metta-init/SKILL.md` (modify)
  - **Action**: Edit both files identically. Locate the `## Round 3` section and the between-round status line that follows it. After Round 3's close, add a new between-round status line: `Resolved: identity, stack, conventions. Open: verification — proceeding to Round 4.` Then add a `## Round 4 — Verification Strategy` section with two `AskUserQuestion` calls: (1) strategy selection with options `Run the test suite (tests_only)`, `CLI commands and exit codes (cli_exit_codes)`, `Playwright / browser end-to-end (playwright)`, `tmux TUI session observation (tmux_tui)`, and `I'm done — proceed with these answers`; (2) free-form instructions prompt: `Any additional verification instructions for the verifier agent? (optional)` with a free-text entry plus the `I'm done` exit option. After Round 4, add status line: `Resolved: all questions. Proceeding to metta-discovery subagent.` Update any exit-criterion text that says "three rounds" or "all three rounds have completed" to say "four rounds" / "all four rounds have completed". Extend the `<DISCOVERY_ANSWERS>` XML block (in the Step 3 section) with: `<verification>\n  strategy: <!-- one of: tmux_tui | playwright | cli_exit_codes | tests_only -->\n  instructions: <!-- free-form text or empty -->\n</verification>`. Add one clause to the discovery-agent task instruction in Step 4: "After writing `spec/project.md`, call `setProjectField(root, ['verification', 'strategy'], strategy)` and, when instructions are non-empty, `setProjectField(root, ['verification', 'instructions'], instructions)` to persist the verification block to `.metta/config.yaml`. If `<verification>` is empty (user exited before Round 4), omit the `verification:` block entirely." Apply this exact same edit to both file paths. Do not alter any other content.
  - **Verify**: `diff -q /home/utx0/Code/metta/.claude/skills/metta-init/SKILL.md /home/utx0/Code/metta/src/templates/skills/metta-init/SKILL.md` produces no output. `npx vitest run tests/skill-structure-metta-init.test.ts` still passes.
  - **Done**: Round 4 present in both copies with strategy and instructions prompts; exit-criterion updated to four rounds; `<DISCOVERY_ANSWERS>` extended; discovery-agent clause added; files are byte-identical.

- [ ] **Task 5.2: Update metta-verifier agent persona (both copies)**
  - **Depends on**: Task 1.1
  - **Files**: `.claude/agents/metta-verifier.md` (modify), `src/templates/agents/metta-verifier.md` (modify)
  - **Action**: Edit both files identically. Insert a new `## Verification Context` section between `## Your Role` and the first `## Rules` or equivalent subsequent section. The section content: explain that when spawned via `metta instructions verification --json` the JSON payload includes `context.verification_strategy` (one of `tests_only | cli_exit_codes | playwright | tmux_tui`, or `null`) and `context.verification_instructions` (free-form notes, or `null`). Then add the missing-strategy rules: (a) if `verification_strategy` is `null` AND both `spec/changes/` and `spec/archive/` are empty (first-run heuristic): default to `tests_only` and emit to stderr: `"No verification strategy configured. Defaulting to tests_only. Run /metta-init to set a project-specific strategy."`; (b) if `verification_strategy` is `null` AND either directory is non-empty (legacy project with history): emit a hard error to stderr and exit non-zero without running any verification step — the error message must contain `.metta/config.yaml`, list all four valid strategy values `tmux_tui | playwright | cli_exit_codes | tests_only`, include `/metta-init` as a remediation command, and include the literal YAML snippet: `verification:\n  strategy: tests_only  # or: cli_exit_codes | playwright | tmux_tui\n  instructions: ""`; (c) add a note: "Do NOT reference `metta config set` — that subcommand is a stub that writes nothing." Apply this exact same edit to both file paths.
  - **Verify**: `diff -q /home/utx0/Code/metta/.claude/agents/metta-verifier.md /home/utx0/Code/metta/src/templates/agents/metta-verifier.md` produces no output. `npx vitest run tests/agents-byte-identity.test.ts` still passes.
  - **Done**: `## Verification Context` section present in both copies with first-run defaulting behavior and legacy-project hard error; files are byte-identical.

## Batch 6 — Self-repair of metta's own config (depends on Task 4.1; 1 task)

- [ ] **Task 6.1: Repair .metta/config.yaml in the metta repo via metta doctor --fix**
  - **Depends on**: Task 4.1
  - **Files**: `.metta/config.yaml`
  - **Action**: After Task 4.1 passes and the build is green, run `node --loader tsx/esm src/cli/index.ts doctor --fix` (or equivalent invocation of the built CLI) from the metta project root. If the auto-commit path fires, verify the commit subject is `chore: metta doctor repaired .metta/config.yaml`. If the tree is dirty (uncommitted task changes), the auto-commit will be skipped by `autoCommitFile`'s dirty-tree guard — in that case, stage and commit `.metta/config.yaml` manually with subject `chore: metta doctor repaired .metta/config.yaml` as part of the final change commit. Either way, `.metta/config.yaml` must have exactly one `stacks:` line under `project:` after this task.
  - **Verify**: `grep -c '^\s*stacks:' .metta/config.yaml` outputs `1`. `npx tsc --noEmit` still passes. `npx vitest run` still passes.
  - **Done**: `.metta/config.yaml` has a single `stacks:` entry; the repair is committed; the fix is demonstrated end-to-end on the project that originally had the bug.

## Batch 7 — Summary (depends on all prior tasks; 1 task)

- [ ] **Task 7.1: Write summary.md**
  - **Depends on**: all prior tasks
  - **Files**: `spec/changes/harden-metta-config-yaml-lifecycle-across-three-related-bugs/summary.md`
  - **Action**: Author `summary.md` enumerating every file touched across all batches with one line of rationale each. Include: `src/schemas/project-config.ts` (added VerificationConfigSchema and wired optional verification field), `src/config/config-writer.ts` (new shared YAML write helper), `src/config/config-writer.test.ts` (unit tests for writer), `src/config/repair-config.ts` (pure dedup and schema-repair function), `src/config/repair-config.test.ts` (unit tests for repair), `src/config/config-loader.ts` (hard-fail on parse errors via ConfigParseError), `src/cli/helpers.ts` (ConfigParseError boundary in handleError), `src/cli/commands/install.ts` (replaced regex writer with setProjectField), `src/cli/commands/doctor.ts` (--fix flag), `src/cli/commands/instructions.ts` (verification context injection), `.claude/skills/metta-init/SKILL.md` (Round 4 added), `src/templates/skills/metta-init/SKILL.md` (Round 4, byte-identical copy), `.claude/agents/metta-verifier.md` (Verification Context section added), `src/templates/agents/metta-verifier.md` (byte-identical copy), `src/index.ts` (barrel exports for new modules), `tests/config-loader.test.ts` (extended), `tests/cli.test.ts` (extended with idempotency, corrupt-config, doctor --fix, and instructions-verification tests), `.metta/config.yaml` (self-repair of duplicate stacks lines).
  - **Verify**: File exists and is non-empty.
  - **Done**: `summary.md` committed alongside or as part of the final change commit.
